# Deferred Topics

Durable backlog for work intentionally deferred out of earlier phases. Keep this file focused on still-open topics; resolved items should move to the relevant build log or ADR.

| ID | Topic | Origin | Status | Recommended Phase | Notes |
|---|---|---|---|---|---|
| DEF-001 | `POST /accounts/me/invite` manual-share delivery | 5E-C | Deferred | Phase 5 follow-up | `POST /accounts/me/members/{id}/resend-invite` supports `manual_share`; initial invite send still only emails. |
| DEF-002 | Mobile invite accept | 5D | Deferred | Mobile auth/API follow-up | Add `clientType` parsing and bearer-token response for invite acceptance. Browser accept remains complete. |
| DEF-003 | Invite-accept session creation failure integration test | 5D | Deferred test coverage | Auth test follow-up | Requires overriding `IAccountSessionService`; service path should return 503 while leaving membership Active. |
| DEF-004 | New-account exchange session creation failure integration test | 5C | Deferred test coverage | Auth test follow-up | Similar to DEF-003, but for new-account commit after `/auth/exchange`; account graph remains committed. |
| DEF-005 | `SignupDefaultsSettings` startup validation | 5C | Follow-up | Config hardening | Add `IValidateOptions<SignupDefaultsSettings>` for `TrialDurationDays <= 0` and `MaxPilotAccounts <= 0`. |
| DEF-006 | Pilot account count commercial-state filtering | 5C | Follow-up | Pilot readiness | `CountActivePilotAccountsAsync` currently counts all `IsPilot = true`; conservative over-count is safe for now. |
| DEF-007 | Keep public-intake link creation during new-account registration | 5C | Deferred | Keep setup/account settings | New-account registration creates the account graph; Keep intake link setup remains later Keep/account setup work. G3 delivered `GET /keep/setup/intake` ensure/status/replacement (build-log/050–051), but auto-creation on account registration is still deferred. |
| DEF-008 | Off-season mode | 5E discovery | Deferred by ADR-083 | Billing/account posture phase | Model as account/commercial posture, not `MembershipStatus`; owner-only read-only access direction is documented, implementation deferred. |
| DEF-009 | Primary-owner transfer flow | 5E discovery | Deferred by ADR-080 | Account ownership phase | Phase 5E protects primary owner from demote/suspend/remove; transfer requires an explicit later flow. |
| DEF-010 | `ErrorHttpMapper` safety-net for internal previously-removed routing codes | 5E-C | Known limitation | Error contract cleanup | Safety-net maps internal routing codes to 409 but would expose internal code names if endpoint intercept failed; endpoint tests cover public contract. |
| DEF-011 | Device management table | 5A discovery | Deferred | Device/session management | `AccountSession` has client type/device fields; separate `AccountUserDevice` table waits until richer device management is needed. |
| DEF-012 | Notification preferences, quiet hours, and delivery | Build plan / Phase 7 notes / B5 decisions / Session 3 decisions / V1 lock / Session 6 prerequisites P6e / Session 8 / Session 9; amended by ADR-432 | Split: V1 foundation and Demo/InternalTest delivery gate implemented; heavy controls deferred | Post-V1 preference/delivery polish | V1 foundation is implemented: device registration/revocation, server-derived badge count (`GET /me/badge`), no-op push adapter with payload/display mapping, candidate/routing foundation, and explicit post-commit hooks for call/cancel/timing, assignment, and unresolved feedback (Session 8, build-log/063). Suppression rules implemented: actor exclusion, mute, OffSeason, stale/ineligible, and Session 9 account-classification delivery eligibility (`Production`/`Pilot` only; `Demo`/`InternalTest` suppressed). ADR-432 permits a narrow public-intake tracker-link transactional email through existing Resend/platform email. Still deferred: real APNs/FCM adapter, custom preference matrix, quiet-hours/business-hours delivery policies, durable notification rows/outbox/retries/dead-letter, broad email/push channel matrices, delivery/open analytics, delegated notification admin, temporary personal silence (DEF-077), customer SMS, and notification reporting. |
| DEF-013 | Attachments/file upload | Build plan | Deferred | Later Keep communication/file phase | Attachment posture is locked in the build plan, but actual file upload implementation is deferred. |
| DEF-014 | Internal platform/support permission keys | Phase 4C | Deferred | Admin shell/internal tooling | Add `internal.support.*` / `internal.platform.manage` only when an internal admin shell or support tool needs them. |
| DEF-015 | Active request feature limit | Phase 4D | Deferred | Keep limits | Add `keep.active_request_limit` key and plan-default row when a concrete product rule needs it. |
| DEF-016 | Branded customer request URLs with public slug | Phase 8 discovery | Deferred | Account public identity / Keep setup | Future route can be `/keep/r/{publicSlug}/{pageToken}`; token remains the security primitive, slug is branding only, and slug mismatch returns 404. |
| DEF-017 | Keep request-list SSE/realtime refresh and count invalidation | Phase 8 discovery / Session 4 decisions / V1 lock | Deferred by V1 decision | Post-v1 realtime UX only if proven necessary | V1 explicitly chooses "fresh, not realtime": refetch after writes, focus/resume sync, pull-to-refresh, 30-60 second active-list polling, server-derived counts/badges, and push/deep links for urgent off-screen work. Do not implement `/keep/requests/stream`, SSE, or WebSockets for go-live. Session 4 `viewCounts` remain request-time snapshots. Reconsider realtime only after pilot usage proves polling+push is insufficient; future work must address auth/session renewal, reconnect jitter, missed-event replay, event ordering, account/user-scoped invalidation, mobile background behavior, debounce/throttle, and battery/network tradeoffs. |
| DEF-018 | External-contact capture workflows | Phase 8 discovery / B2-gamma / B5 decisions | Implemented — Sessions 2A-2B | 8-B5 | Implemented: `POST /keep/requests/{requestId}/external-contact`, domain methods `LogOutboundExternalContact` / `LogInboundExternalContact`, full effect matrix (first response, attention clearing, activity timestamps), internal-only timeline event, 5-column migration, detail DTO external-contact fields, OffSeason blocked (ADR-221). Remaining: customer-visible recap (separate explicit action), undo/revert (DEF-041), SMS delivery (DEF-022), mobile recent-activity previews (DEF-040), source/provenance (DEF-044). |
| DEF-019 | Customer notification preferences and email opt-out routes | Phase 8 discovery; amended by S22p3 | Deferred | Phase 9 notifications | S22p3 implements a lightweight public-intake `ContactPreference` for human follow-up and S22p4 surfaces it to operators. Still deferred: customer-managed notification preferences, quiet hours, email opt-out routes, and automated platform notification preference handling. |
| DEF-020 | Full Keep signal/projection attention engine | Phase 8 discovery | Deferred | Later intelligence/automation phase | Reference has a larger signal/attention projection engine. Phase 8 pilot uses direct `KeepRequest` attention fields plus events; revisit richer signal engine after pilot behavior is validated. |
| DEF-021 | Native mobile push notifications and badge counts | Phase 8 discovery / V1 lock / Session 6 prerequisites P6e / Session 8 / Session 9 | V1 foundation implemented; real APNs/FCM still deferred | Real-adapter or explicit no-op-pilot posture slice | V1 foundation complete: `AccountUserDevice` table with token registration/revocation, `GET /me/badge` personal badge count, `IPushAdapter`/no-op adapter with payload mapping, candidate/routing/suppression engine, explicit post-commit push hooks (Session 8, build-log/063, ADR-355/356/357/358/359/360/361/362), and account-classification delivery eligibility (Session 9, ADR-366). `Production` and `Pilot` are delivery-eligible; `Demo` and `InternalTest` return no delivery devices. Remaining before real push delivery: real APNs/FCM adapter replaces no-op and is tested end-to-end, or pilot launch explicitly documents no-op/test posture and trains users that push is not live. Full preferences, quiet hours, outbox/retries, and delivery analytics remain DEF-012. |
| DEF-022 | SMS notification sending | Phase 8 discovery / V1 lock / Session 6 prerequisites P6e; amended by ADR-432 | Deferred / excluded for v1 platform delivery | Post-pilot only if business case changes | V1 may launch the operator's native phone, email, or Messages app from contact actions (`tel:`, `mailto:`, `sms:`), optionally with a draft and tracker-link prefill where supported. Customer-facing platform notifications are not automatic in P6e; Owner/Admin/Operator decides when to contact the customer. Keep does not send platform SMS in v1 and does not ingest customer SMS replies. ADR-432 separately allows narrow public-intake tracker-link email when the customer supplies email. Opening a native app is not durable evidence; only explicit customer-visible updates, status messages, or external-contact logging create audit/state effects. V1 UI may optionally prompt on app resume to log the attempted contact, but no history event is created unless staff confirms. Avoid Twilio-style outbound SMS in Keep/OpHalo pilot because automated customer-facing SMS would require A2P 10DLC registration/brand vetting, TCPA consent and STOP/opt-out management, gateway cost, and delivery/retry policy. Native launchers intentionally pass execution to the operator's device/client so the pilot stays in ordinary peer-to-peer contact behavior. |
| DEF-023 | Identity-bound customer page access and link management | Phase 8-B3 decisions / ADR-118 | Deferred | Post-pilot customer access hardening | Customer page links are bearer-access links for pilot. Defer customer login/passcodes, per-recipient access, link revocation/rotation UI, and customer account identity until sharing risk or product requirements justify the friction. |
| DEF-024 | Keep response policy onboarding/account settings UI | Phase 8-B3 decisions / ADR-124; amended S22r4 | Implemented — S22r4 / S22 | — | `KeepResponsePolicy` is the durable policy model. B3 uses stored policy when present and pilot defaults otherwise. S22r4 implemented the dedicated Response Policy Settings tab per ADR-428 (`settings/PolicySection.tsx`), surfacing first-response, standard, priority, and status-check targets with compact plain-language guidance. Business-hours-aware SLA math remains DEF-025; industry-driven presets remain DEF-081. |
| DEF-025 | Business-hours and quiet-hours SLA calculation | Phase 8-B3 decisions / ADR-124 | Deferred | Response policy / scheduling phase | `BusinessHoursOnly` remains stored, but B3 does not implement business-hours-aware attention math. Later work must handle account time zone, weekends, holidays, emergency/off-hours service, DST, and received-near-closing edge cases. |
| DEF-027 | Advanced feedback/review analytics | Phase 8-B3 decisions / ADR-134 | Deferred | Later insights / growth / packaging phase | CSAT/NPS, public review prompts, review generation, feedback dashboards, and automated follow-up campaigns are not part of core B3 or the first feedback slice; these may be packaged separately later. |
| DEF-028 | Closed-request closeout review and archive workflow | Phase 8-B4/B5 decisions / ADR-148 / ADR-188 / ADR-193 / ADR-245 | Deferred | Closeout/admin workflow phase | Model internal closeout review and archive separately from `ParticipationType`. Closed means customer/request work is done; closeout reviewed means internal business/admin follow-up is done; archived means no longer shown in normal active/history surfaces but still retained. B5 and Session 4 add no archive/unarchive/auto-archive action. Future archive decisions include who can archive/unarchive, manual vs auto-archive, whether archived requests appear in search, whether unresolved-feedback requests can be archived, note/timeline requirements, undo behavior, and analytics/reporting effects. |
| DEF-029 | Mark negative feedback reviewed | Phase 8-B4 decisions / ADR-154 / ADR-244 / ADR-261..286 | Implemented — Sessions 5A-5C / G7 / build-log/058 | — | Owner/Admin-only `POST /keep/requests/{requestId}/feedback-review`; reviewer/timestamp/note persistence; internal-only history event; `UnresolvedFeedback` attention clearing; OffSeason blocking; no undo/reopen; no customer notification; closed-history preservation. |
| DEF-030 | Customer page visit tracking as operator confidence signal | Phase 8-B4 discovery / reference lessons; Session 6 prerequisites ADR-341 | Implemented — P6c-2 / build-log/060 | — | Implemented as `CustomerPageLastViewedAtUtc` on `KeepRequest`, debounced writes from non-expired customer-page GETs, best-effort no-version-rotation persistence, and staff-facing detail metadata for last viewed / viewed after latest business update. This remains a confidence/adoption signal only: not presence, not a read receipt, does not raise business attention, and does not suppress stale/status-check without a later decision. |
| DEF-031 | Post-call outcome logging | Phase 8-B5 decisions / ADR-168 / ADR-169 | Implemented — Sessions 2A-2B | 8-B5 | Implemented in `LogOutboundExternalContact`: outcomes spoke/voicemail/no-answer/wrong-number; spoke/voicemail set first response and may clear attention; no-answer/wrong-number log only; outcome required for phone, rejected for SMS/email/inbound. |
| DEF-032 | Inbound external customer contact logging | Phase 8-B5 decisions / ADR-171 | Implemented — Sessions 2A-2B | 8-B5 | Implemented in `LogInboundExternalContact`: all non-InApp channels; always requires summary; does not count first response; raises/preserves business-waiting attention when `requiresBusinessFollowUp=true`; updates `LastCustomerActivityAt`; internal-only visibility. |
| DEF-033 | Request assignment/watch writes | Phase 8-B5 decisions / ADR-176 / ADR-185 / ADR-222..235 | Implemented — Sessions 3A-3D | 8-B5 | Implemented: non-terminal assign/transfer/clear/watch/unwatch/mute/unmute; Owner/Admin dispatch; Operator self-watch/mute; explicit idempotent endpoints returning detail; compact participant candidates; `ParticipationChanged` internal event metadata; 4-flag participation availability metadata (`CanWatch`/`CanUnwatch`/`CanMute`/`CanUnmute`) on detail; responsible name/stale indicator on list; customer-page excludes participation events; OffSeason blocks all participation writes; no notification delivery. |
| DEF-034 | Request-list filters, search, pagination, and closed history | Phase 8-B5 decisions / ADR-173 / ADR-187 / ADR-237..260 | Implemented — Sessions 4A-4C | 8-B5 Session 4 | Implemented: `GET /keep/requests` extended with named views (`default`, `assigned_to_me`, `watching`, `unassigned`, `needs_attention`, `feedback_review`, `closed_history`, `cancelled_history`, `all_history`), server-side filters/search (`status`, `attentionReason`, `q`, `createdFrom/To`, `closedFrom/To`), HMAC-signed cursor pagination, role-aware `viewCounts`, `listContext`, `rowContext`, `CanSelfAssignFromList`, and explicit query validation. `KeepRequestListQueryBinding` handles HTTP structural parsing; `HmacKeepRequestListCursorProtector` signs/verifies cursors; in-memory B5 ranking + DB keyset history sorting. Build-log/045. |
| DEF-035 | Feedback review queue and batch navigation | Phase 8-B5 decisions / ADR-186 / ADR-187 / ADR-242 / ADR-244 / ADR-261..286 | Ready for implementation | 8-B5 Session 5 | Session 4 adds Owner/Admin `feedback_review` view and focused row context/action metadata. Session 5 decisions add detail review completion, context-based next/previous navigation, optional client "mark reviewed and next" using the same endpoint, aging metadata, and Owner/Admin action context. Bulk review remains excluded. |
| DEF-036 | Ready-to-close queue and batch closeout | Phase 8-B5 decisions / ADR-188 / ADR-193 / V1 lock; Session 6 prerequisites ADR-343..344; P6f-2..P6f-5 | Implemented for V1 — batch endpoint deferred | 8-B5 Session 6 proper | Implemented: Operator-facing Mark completed maps to Resolved; Owner/Admin Close request maps to Closed; default close permission remains Owner/Admin and attention-gated (ADR-343); `view=ready_to_close` Owner/Admin-only queue (Resolved + AttentionLevel==None); `ReadyToClose` view count; row warning signal `KeepRequestReadyToCloseInfo.HasCustomerActivityAfterResolution`; detail `navView=ready_to_close`; close-and-next via PATCH status `navView=ready_to_close`; Closed and Cancelled customer pages expire 30 days after terminal transition. No batch close endpoint; Cancel remains careful detail-only. |
| DEF-037 | Needs status check for stale active requests | Phase 8-B5 decisions / ADR-193 / V1 lock; Session 6 prerequisites ADR-339 | Decision locked; implementation pending | 8-B5 Session 6 prerequisites | B5 does not auto-complete or auto-close stale requests. Needs-status-check uses an account-policy threshold with a 5-calendar-day pilot default when unset, excludes active attention, future Follow Up On, future Planned For, `Resolved`, and terminal rows, and relies on centralized latest-meaningful-activity signals. It is a human review queue only; no automatic customer update/status change. |
| DEF-038 | Rank history / movement deltas | Phase 8-B5 decisions / ADR-179 | Deferred | Later UX polish | B5 explains current ranking using reason, severity, elapsed/due timing, post-close context, and waiting direction. Do not add up/down arrows, numeric position deltas, rank history, or new-since-last-view indicators until the product needs and storage semantics are explicit. |
| DEF-039 | Event-derived request-list message previews | Phase 8-B5 implementation handoff / ADR-183 / ADR-184 | Deferred | Post-B5 list UX/history slice | B5 Session 1 may expose preview contract fields with `null` text/source and `previewTruncated=false` to avoid event loading in the default-list upgrade. A later slice should batch-load relevant `KeepRequestEvent` rows and compute mobile-first preview text/source/truncation without N+1 queries. |
| DEF-040 | Compact mobile recent-activity previews | Phase 8-B5 Session 2 discovery / ADR-201 | Deferred | Post-B5 mobile list UX/history slice | Pilot operators may need row expansion or compact last 1-2 activity previews showing who did what when, including external contact actor/channel/outcome, to avoid duplicate work without opening full detail. Do not build in Session 2; external-contact event data must be structured so this can be added later without changing the contact-log model. |
| DEF-041 | Mark external contact log as mistake/ignored | Phase 8-B5 Session 2 discovery / ADR-206 | Deferred | Later audit/timeline polish | Session 2 has append-only external contact logs and no undo/revert. A future mistake/ignore marker may visually flag a contact log as mistaken without reversing first-response, attention, activity, ranking, or projection effects. True revert remains a separate compensating-event workflow. |
| DEF-042 | OffSeason Keep read-only/freeze implementation | Phase 8-B5 Session 2 discovery / ADR-208 | Implemented — Session 2C (partial) | 8-B5 | Implemented: all 5 operator write services set `RequestImplementsAllowedInOffSeason: false` + `decision.IsReadOnly` check → 403; `GetKeepRequestDetailService` uses `canWrite = canOperate && !isOffSeason` for all write-action flags; customer-page writes return 409 `OffSeasonUnavailable`; public intake returns 422 unavailable. Still open: Owner/Admin narrow closeout in OffSeason (non-terminal requests with internal audit only). |
| DEF-043 | OffSeason public intake fallback settings and preview | Phase 8-B5 Session 2 discovery / ADR-208 | Deferred | Account settings / OffSeason polish | Public intake links remain reachable in OffSeason but show an unavailable/read-only response and do not create requests. Later Owner/Admin settings should configure custom unavailable message, public phone/email, website/social links, optional reopen date, emergency note, and preview. Do not expose auth/account-user emails automatically. |
| DEF-044 | External contact source, occurred-time, and analytics provenance | Phase 8-B5 Session 2 discovery / ADR-210 / ADR-212 | Deferred | Native app / analytics provenance slice | Session 2 uses `OccurredAtUtc` as contact log time and does not distinguish Keep-initiated from manually logged contact. Future native-app/analytics work may add `ExternalContactOccurredAtUtc` and `ExternalContactSource=keep_initiated/manual_log` together with validation and analytics inclusion rules. |
| DEF-045 | Operator Unassigned/Available queue and Operator self-assign | Phase 8-B5 Session 3 decisions / ADR-227 / ADR-240 | Implemented — Session 4C | 8-B5 Session 4 | Implemented: `view=unassigned` accessible to Operators in Session 4B/4C; Operator self-assign re-enabled in `ManageResponsibleService.SetAsync` (only for target==self, active Responsible from another user → 409 `ParticipationRequestAlreadyAssigned`, already self-assigned → no-op 200 per ADR-230); `CanSelfAssignFromList` flag on list rows; `GetViewCountsAsync` now returns real unassigned count for all roles. UX watch-out remains: native/mobile should expose a prominent top-level `Available` / unassigned toggle with count so operators notice unclaimed work — frontend UX decision deferred. |
| DEF-046 | Owner/Admin delegated mute/preference controls | Phase 8-B5 Session 3 decisions / ADR-225 | Deferred | Notification-admin/preferences slice | Session 3 exposes mute/unmute only for the current user. Internals should target notification-enabled changes by AccountUserId so future Owner/Admin delegated mute/preference controls can be added without a model rewrite. |
| DEF-047 | Stale participant cleanup after member role/status changes | Phase 8-B5 Session 3 decisions / ADR-226 | Deferred | Member-management cleanup / routing-maintenance slice | Session 3 validates participation eligibility on write and treats stale Responsible users as effectively unassigned for routing/notifications, with Owner/Admin indicators. It does not automatically detach participants when users are suspended, removed, invited, or demoted to Viewer. Future cleanup may explicitly detach/archive invalid participants. |
| DEF-048 | Operator closed-history access | Phase 8-B5 Session 4 decisions / ADR-248 | Deferred | Post-go-live history/permissions review | Session 4 keeps broad closed/cancelled/all history Owner/Admin-only. Future decisions should revisit whether Operators can browse closed requests they completed, were assigned to, or watched; whether Operators can search terminal history by customer/reference; whether feedback comments remain hidden; whether cancelled access differs from closed access; and whether access is account-configurable. |
| DEF-049 | Exact totals for history/search/filter views | Phase 8-B5 Session 4 decisions / ADR-251 | Deferred | Post-go-live analytics/admin review | Session 4 exposes operational `viewCounts` and `pageInfo.hasMore`, not exact totals for arbitrary q/status/date/history combinations. Reconsider after go-live if businesses need reporting-like counts or heavier admin review workflows. |
| DEF-050 | Contextual customer/address history summaries | Phase 8-B5 Session 4 discovery | Promoted to GAP-050 | V1 Request Detail repeat-customer context; fuller address-history UX later | GAP-050 delivers a bounded account-scoped related-work indicator for the same canonical customer. Broad customer/address history, fuzzy matching, and expanded summaries remain deferred pending pilot evidence. |
| DEF-051 | Internal/team-memory search | Phase 8-B5 Session 4 decisions / ADR-255 | Deferred | Internal search/privacy/indexing slice | Session 4 excludes internal notes, participation notes, external-contact summaries, and full internal timeline search. Future work should decide which roles can search internal memory, whether Operators can search only requests they can access, whether Owner/Admin can search all internal notes, feedback/external-contact inclusion, matched-content display, separate internal-search mode, indexing/performance, and retention/privacy rules. |
| DEF-052 | Tiny-team routing UI simplification | Product discussion / Foundation build plan | Needs discussion and decisions | After Sessions 4/5+ web/mobile product-surface planning | Keep the backend participation/routing model, but decide how much assignment/watch/mute UI should be visible for 1-2 person businesses. Candidate direction: 1 active user hides team-routing controls entirely; 2 active users shows a simplified Mine/All, Assigned-to, or "send to field" flow; 3+ active users exposes fuller assignment/watch/mute/unassigned surfaces. Tricky foundation boundary: this must not silently expand Operator visibility or bypass assigned/watched/unassigned routing rules without an ADR, because Operator request visibility, notification routing, counts, self-assign eligibility, and future mobile push all depend on participation semantics. Open decisions: automatic by active-user count vs account setting, Simple Mode vs Team Mode opt-in/out, role-specific behavior, whether tiny teams need auto-assign/auto-watch rules, how view counts/actions collapse, and how this affects native operator UI. |
| DEF-053 | Formal callback/rework linked-request workflow | Phase 8-B5 Session 5 decisions / ADR-284 | Deferred | Post-go-live feedback/rework learning slice | Session 5 does not add Rework/Callback status, reopen, linked requests, or automatic follow-up jobs. If unresolved feedback requires another visit or job, Owner/Admin may manually create a new request and record the relationship in internal notes. Future work should decide callback vs rework language, linked request data model, customer visibility, assignment/routing, warranty/billing/reporting effects, and whether it should reopen or supersede the original request. |
| DEF-054 | Customer-page expired/no-feedback check-in reminder | Phase 8-B5 Session 5 product discussion | Deferred | Customer-success / account-configurable follow-up slice | V1 page expiry remains an access/read boundary only. It does not create events, attention, notifications, or check-in tasks. Future account-configurable workflow may create an Owner/Admin reminder when a closed customer page expires without feedback, but must decide noise thresholds, no-feedback vs negative-feedback behavior, routing target, notification preferences, and whether the reminder belongs in queues or notifications. |
| DEF-055 | OffSeason readiness checklist | Phase 8-B5 Session 5 decisions / ADR-278 | Deferred | Account operating-mode UI/settings slice | Session 5 records that Owner/Admin should be warned before entering OffSeason if unresolved feedback or other open Keep work exists. Implementation of OffSeason activation UI, checklist content, warning copy, hard-block vs soft-warning behavior, and counts across active/ready-to-close/stale/unreviewed-feedback work remains deferred. |
| DEF-056 | Keep impact measurement and reporting architecture | Product positioning discussion / build plan insights posture | Promoted to GAP-037 | V1 founder/internal weekly report; fuller reporting post-pilot | GAP-037 delivers the first bounded report: founder/internal-only, copy-pasteable weekly Markdown/text for an account and reporting period. Owner dashboard, automated delivery, projections, and broader analytics remain deferred. |
| DEF-057 | Internal admin onboarding and usage observability | Reference app lesson / product operations discussion | Partially promoted to GAP-039 | V1 redacted reliability/product-ops minimum; admin tooling post-pilot | GAP-039 delivers redacted error capture, health/release visibility, alerting, and only the product-ops signals needed for onboarding, inactivity, and GAP-037. A read-only admin dashboard, broad usage analytics, internal mobile alerts, and event-subscription architecture remain deferred. |
| DEF-058 | Keep pricing strategy and packaging | Product positioning discussion | Needs review after pilot data | Pricing / packaging / billing readiness | Revisit pricing after pilot usage data. Current hypothesis: charge mainly by business/team size, use request volume as plan guardrails or fair-use limits, avoid strict per-request pricing early, keep extra-user pricing low enough to discourage shared logins, include push notifications, and separate/pass through SMS only if SMS is added. Track active users/operators, request volume, customer-created requests, response speed, unresolved feedback reviewed, stale follow-ups surfaced, repeat-customer signals, notification usage, and perceived value at $49-$79/$99/$199/$299+ price points. |
| DEF-059 | Customer SMS/text consent posture | Pilot readiness / compliance review | Closed — not applicable | — | Keep does not send platform SMS directly from the application. DEF-022 already locks this out of V1, and native `sms:` launch hands execution to the operator's device rather than Keep acting as an automated messaging platform. No A2P 10DLC/TCPA/STOP-loop consent posture is needed until/unless a future decision explicitly introduces platform SMS delivery. |
| DEF-060 | Request-level service location and address history | Pilot readiness / duplicate-identity discussion | Needs discussion and decisions | Intake/detail/history UX slice | Repeat customers may request work at a different home, rental, or job site. V1 customer reuse by phone should not erase historical service-location context. Decide request-level service address/location fields, whether customer-level address exists at all, address history display, and how later customer/address matching affects contextual history summaries. |
| DEF-061 | Public-intake garbage/spam/test request handling and metrics exclusion | Pilot readiness / public intake operations / V1 lock; Gap Decision reviews 2026-06-19 and 2026-06-20; Session 7 ADR-349..350 | V1 pre-go-live behavior locked; adaptive/source blocking deferred pending evidence | Intake/admin workflow / impact metrics slice | Public intake can create nonsense, accidental, spam, or test requests that clutter the command center and skew response metrics. Before pilot, Owner/Admin can classify a request explicitly as terminal `Spam` or terminal `Test`; these remain distinct statuses because `Test` supports onboarding, employee training, demos, and QA while `Spam` represents junk/abuse. Classification removes the request from operational queues without deleting its auditable history, excludes it from response-time, intake, stale-work, and impact metrics, and prevents further customer-page activity. Require confirmation and allow an optional short internal reason. Operators cannot classify in V1 because the action removes work and changes reporting. Pre-pilot abuse posture also requires bounded validation, trusted-IP/rate-limit proof, token-safe logs, and a founder/internal emergency runbook. Do not add ordinary intake pause/resume or expose raw customer IPs to businesses. Expiring keyed-hash “block source” controls, honeypot/minimum-completion-time checks, duplicate/pattern detection, per-link/platform thresholds, adaptive Turnstile, anomaly alerts, and optional phone verification remain post-pilot/V1.1 candidates; pull them before broad public launch only if pilot evidence shows repeated abuse that the pre-pilot controls cannot contain. |
| DEF-062 | Field-operator app-switching and orientation | Pilot readiness / co-existence strategy | Needs pilot playbook | Pilot onboarding / mobile UX planning | Keep intentionally coexists with field-service tools. Operators may switch between Keep for customer communication and another tool for job execution details. Pilot training and mobile UX should frame Keep as the customer-contact fast lane: call, update, log contact, mark completed. Watch for duplicate-entry friction before adding integrations. |
| DEF-063 | Ready-to-close customer-activity warning | Pilot readiness / Session 6 closeout planning; P6f-2 | Partially implemented — row signal done | Web/native closeout UX | **P6f-2 implemented:** `HasCustomerActivityAfterResolution = LastCustomerActivityAt > LastBusinessActivityAt` on Resolved rows, populated on every list row. Backend close eligibility already excludes active attention. Still open: detail-level warning display and final UI copy for customer activity after resolution; unresolved-feedback warning treatment belongs with feedback/closeout UX, not another backend queue. |
| DEF-064 | Post-close external contact for unresolved feedback | External review / ADR-200 watch-out | Implemented — G7b / build-log/058 | — | Owner/Admin outbound external-contact logging is now permitted on `Closed` requests when active `UnresolvedFeedback` review attention is present. It is internal-only, does not count first response, reopen, change status, notify the customer, or automatically mark feedback reviewed. Cancelled requests remain blocked. |
| DEF-065 | Operator available-work UI prominence | External review / ADR-227 / ADR-240 / ADR-321 / ADR-324 | Backend/security behavior locked; exact frontend placement deferred | Native operator app / web operator UI planning | Operator Default and Needs Attention remain participating `My Work`; active eligible unassigned work uses dedicated `GET /keep/requests/available` with a privacy-limited summary. Customer name is included for field recognition, while contact fields and private history remain withheld until an explicit audited self-assign/watch; opening a read never changes ownership. The route defaults to 20 rows, caps requests at 50, uses DB-level cursor pagination and a narrow bounded projection, and returns no exact total. Clients may request fewer rows on mobile or more in a wider Operator workspace, but the backend does not infer device type; restrained refresh/poll frequency is the larger cost control. **Known PWA correction:** `AvailableRequestRow` currently sends `priorityBand` (`priority`/`standard`) into an attention-reason formatter, so priority never receives its intended visual escalation; correct this with the next bounded operator-work presentation slice. Frontend still must decide exact placement—likely a top-level `My Work` / `Available` segmented control or tab with count on mobile and visible secondary navigation/count on web—and final visual treatment. |
| DEF-066 | Request-list query binder maintenance runway | External review / ADR-287 | Watch | Post-Session-4 technical debt review | `KeepRequestListQueryBinding` is appropriate for Session 4's explicit operational-list contract, duplicate param checks, and thin `Program.cs`. Watch for growth: if reporting, saved views, complex filters, or many dimensions appear, split operational list binding from reporting contracts and consider standard framework binding/validation where the safety requirements are lower. Do not let the custom binder become the place every future search/reporting concern accumulates. |
| DEF-067 | Request snooze / waiting-until follow-up | Late-night product review / stale-work discussion; Session 6 prerequisites ADR-337 | **Implemented — P6b-1 through P6b-3** (domain/schema, versioned API mutations, detail exposure, list scan metadata + stale-suppression inputs). Stale-suppression queue itself remains DEF-037 / P6d. | 8-B5 Session 6 prerequisites | Some active Keep requests are legitimately delayed because parts are ordered, weather blocks work, the customer is unavailable, operator availability shifts, or a third party is pending. V1 direction: staff-owned **Follow Up On** date for active requests only, not general callbacks or scheduling. Owner/Admin and operating Operators can set it; reason required from weather/parts/customer delay/business-operator availability/third party/other, note required for other. It suppresses stale/status-check until due, shows scan-friendly markers, resurfaces as follow-up attention on/after the date, and does not automatically notify the customer, though explicit customer update/contact should be easy. |
| DEF-068 | Quick replies and promise-safe reminders | Late-night product review / field-operator UX | Needs discussion and decisions | Native/web message UX slice | Quick replies can reduce typing for field operators, but promise-style replies can create broken promises if they clear attention without follow-up. Candidate direction: start with a small default set and defer business-custom reply libraries until real usage; classify templates as informational vs promise-bearing. Promise-bearing templates like "we will get back to you shortly" should require or strongly prompt a follow-up reminder / `NextAttentionAtUtc` so a canned response does not make the request disappear. |
| DEF-069 | Attention action clarity and clear-effect UX | Late-night product review / ADR-178 / ADR-191 / V1 lock | V1 UI requirement | Web/native attention UX | Every attention row/card should answer quickly: what happened, why it matters, what action to take, and what will clear attention. Backend action metadata remains the source of truth, but UI must render a clear primary action and effect summary: customer-visible vs internal, clears attention vs does not, counts first response vs does not, changes status vs leaves status unchanged. Field users should not have to infer how to make the alert go away. This is required for the V1 "active accountability loop" to feel trustworthy. |
| DEF-070 | Expired customer page repeat-request loop | Late-night product review / ADR-099 / DEF-016 | Deferred | Customer page / safe intake URL slice | Expired terminal customer pages should eventually become a polished repeat-request entry point instead of a dead end: "This request loop is closed/archived; need help again?" This should use `NewRequestUrl` only after a safe customer-facing intake URL contract exists. Do not expose raw public-intake tokens or hashes. Not a go-live blocker. |
| DEF-071 | End-of-day open promises summary | Late-night product review / owner/admin habit | Candidate pilot polish | Owner/Admin dashboard / daily summary | A simple "Open Promises Tonight" summary could show requests currently waiting on the business before day end, helping Owner/Admin reach "0 customers waiting on us" or deliberately snooze/assign/update the remaining items. Candidate implementation can likely derive from `WaitingDirection.Business`, attention state, and list counts. Decide after core list/detail flows are stable; avoid building a reporting platform before pilot learning. |
| DEF-072 | In-app pilot Report Friction hook | Pilot support discussion / ADR-293 | Promoted to GAP-038 | V1 pilot support feedback slice | GAP-038 carries the authenticated, API-mediated, rate-limited feedback hook and its sensitive-data boundary. |
| DEF-073 | In-app Pilot Updates page | Pilot support discussion / ADR-294 | Promoted to GAP-038 | V1 pilot support communication slice | GAP-038 carries the maintained Help & Updates content and feedback entry point; CMS, public status page, roadmap portal, feature voting, and admin editor remain deferred. |
| DEF-074 | Keep request write concurrency control | Phase 8-B5 Session 5 service review / ADR-275 / ADR-276 | Implemented — G5 / build-log/056 | — | G5 delivered entity-wide optimistic concurrency: application-managed `Guid ConcurrencyVersion` on `KeepRequest`, `X-Keep-Request-Version` header contract, version exposure on all reads/mutations, atomic rotation across request/event/participant/customer writes, stable 409 `KeepRequest.RequestChanged` on stale/save-race, and two-DbContext race proofs. ADR-330–335. |
| DEF-075 | API contract evolution, compatibility window, and versioning strategy | G5 concurrency-version discussion | Deferred architecture decision | Before mobile beta distribution or external API access | G5's opaque request `version` is optimistic-concurrency state, not API versioning. Before independently deployed mobile clients depend on the API, define supported client/backend compatibility windows, additive-versus-breaking change rules, deprecation and minimum-client behavior, contract testing, and rollout/rollback expectations. Then decide whether explicit API versions are needed and, if so, whether they use URL paths, headers, or media types. Do not introduce `/v1` mechanically before the compatibility policy and a real breaking-change boundary exist; the PWA and internal API alone do not justify premature parallel contracts. |
| DEF-076 | Business-first request capture UI | Pilot product concern / ADR-336 / build-log/051; pilot readiness discussion 2026-06-25; amended by ADR-432 | V1 pilot must-have UI | PWA/native request creation slice before pilot | Backend `POST /keep/requests` is implemented, but pilot UI must make business-created requests fast and prominent. Quick Capture is an immediately-after-contact tool, not live note-taking during a customer interaction. Owner/Admin/Operator must be able to create a normal authenticated business-created `KeepRequest` in `Received` status without waiting for public intake. Required fields: customer name, phone number, short request summary, and source/channel. Phone is required because pilot service-business workflows depend on call/text; email is optional secondary contact info. After save, keep the user in the capture flow with confirmation, share/copy actions, `Capture Another`, and `View Request` rather than forcing request-detail navigation. Staff-created requests start with tracker sharing marked needed; lists/detail must show `Needs Share` until an explicit in-app share-intent action occurs (native SMS/share handoff, copy from request share controls, or manual `Mark Shared`). Clearing means staff initiated or recorded sharing, not proof of delivery/read. Use a predefined source/channel enum: Phone, Voicemail, Text, Email, WalkIn, Referral, PublicIntake, Other; public intake sets PublicIntake automatically, while staff Quick Capture does not manually set it in normal V1 flow. Public intake remains valuable but is optional enrichment/collaboration, not the gate for work to enter Keep. Deferred: call-log/SMS reading, voice capture, automatic parsing, backend SMS, broad customer notification workflows, and proof-of-send. ADR-432 allows narrow public-intake tracker-link email when customer email exists. |
| DEF-077 | Temporary personal notification silence | Session 6 prerequisites P6e discussion | Deferred | Notification/device UX polish after V1 foundation | Future user-level control to silence Keep pushes temporarily while an operator is on a job site, driving, in a meeting, or otherwise unavailable. This is distinct from request-level mute: it suppresses push delivery for that user for a bounded/manual window without changing visibility, assignment, responsibility, badge counts, or list state. Duration presets, manual resume, urgent-bypass behavior, OS Do Not Disturb interaction, and fallback/escalation rules are undecided. Not part of P6e or the first notification/device implementation unless pilot feedback makes it necessary. |
| DEF-078 | Account timezone and future branch/location timezones | P6f-3 closed-history shortcut discussion / pilot account with Memphis and Little Rock offices | Deferred | Account onboarding/settings before polished date shortcuts, reporting, business-hours, or notification schedules | V1 can use UTC-backed shortcut windows where necessary, but business-facing date language such as "yesterday", "this week", Follow Up On labels, Planned For labels, end-of-day summaries, business-hours response policy, reporting, and quiet-hours/notification schedules should eventually use the business operating timezone. Model this first as an account/business setting using an IANA timezone such as `America/Chicago`, defaulted from browser/device during onboarding and editable by Owner/Admin. Do not make shared operational queues depend on each staff user's personal timezone preference. Multi-office businesses can use the account timezone when offices share a timezone, such as Memphis and Little Rock in Central Time. Future branch/location support may add per-location timezones and, if needed, request-level service-location timezone behavior. |
| DEF-079 | Demo/internal-test account classification and resettable scenario packs | Session 7 decision discussion / sales-demo planning; pilot readiness discussion 2026-06-25; Session 9 | Split: classification/delivery gate implemented; scenario packs remain deferred | Sales demo / internal QA tooling later | Sales demos and regression testing need resettable fake business data that can show Keep signals such as needs attention, stale/follow-up, ready-to-close, unresolved feedback, assignment/watch state, and history. Do not infer demo data from account names, email domains, or fake customer details. Session 9 implemented the durable classification foundation: `Production`/`Pilot`/`Demo`/`InternalTest`, separate from commercial lifecycle, replacing `AccountEntitlements.IsPilot`; `SignupDefaultsSettings` sets classification; Demo/InternalTest suppress production notification delivery by default. Still deferred: explicit admin/internal creation flows for Demo/InternalTest accounts, reset/seed scripts, and rich reset UI. Real users may belong to demo accounts, but future reset/seed scripts must preserve users/memberships, clear notification device tokens, replace only demo business/request data, require an explicit target account id, and refuse Production/Pilot accounts. Broad demo seeds are not part of normal migration/local setup. |
| DEF-080 | Demo-only role switching / run-as support | Session 7 decision discussion / sales-demo planning | Deferred | Demo/admin tooling after account classification | Demonstrating Owner/Admin/Operator/Viewer behavior may require role-specific views. Prefer separate real demo users per role at first. If role switching or "run as" is added later, gate it to Demo/InternalTest accounts by default, make the active role/person obvious in the UI, preserve auditability, and avoid broad production impersonation. Any production support impersonation would require a separate security decision with explicit permissioning, auditing, customer-content minimization, and access review. |
| DEF-081 | Business type / industry setting and response-policy presets | Pilot readiness discussion 2026-06-25 | Needs more discussion | Account onboarding/settings before preset-driven defaults | V1 response policy is account-level and configurable, and new public-intake first-response targets should be measured in minutes rather than hours. Business type/industry may be useful for suggested defaults, onboarding examples, customer-page language, report interpretation, future templates, and pilot segmentation, but it is not locked yet. Discuss whether onboarding should require a lightweight industry value such as lawn care/landscaping, plumbing, HVAC, electrical, handyman/general contractor, cleaning, pest control, roofing, restoration/emergency repair, or Other; whether this setting should drive presets only or any domain behavior; and how to keep owners able to adjust policy without hard-coded industry assumptions. |

| DEF-082 | Business logo upload | S22 Settings/public-intake scope discussion; ADR-428; build-log/076 | Deferred | V1.1 personalization | Public intake and customer pages display the business display name and customer-facing contact; no logo or image upload is included in V1. Business logo upload is deferred to V1.1 as a personalization feature. Requires decisions on storage, CDN delivery, image validation, size/format constraints, and customer-page display placement. |
| DEF-083 | Brand color customization | S22 Settings/public-intake scope discussion; ADR-428; build-log/076 | Deferred | Beyond V1.1 unless accessibility-safe design is ready | Custom brand colors on public intake and customer pages are deferred beyond V1.1. Any future color-customization feature must pass WCAG contrast requirements and cannot allow owners to choose combinations that fail accessibility standards. Requires an accessibility-safe palette approach and design/UX review before implementation. |

## Operational Watch-outs

These are not product backlog items, but should remain visible before deployment or infrastructure work.

| ID | Topic | Origin | Notes |
|---|---|---|
| OPS-001 | Resend production secrets | 5D; S23 follow-up | `Resend:ApiKey` and `Resend:FromAddress` must be set via user secrets or deployment environment. Track production configuration and tracker-link email readiness in `docs/build-log/078-customer-tracker-link-email-and-resend-configuration.md`. |
| OPS-002 | Public/app base URL cleanup | 5B/5D; ADR-378 | `App:PublicBaseUrl` must point at the public auth site. Existing `App:OperatorBaseUrl` invite-link usage must be reviewed during S13 and either mapped to the public invite-accept surface or replaced by a clearer `PublicBaseUrl` / `AppBaseUrl` split, with post-auth redirect to the authenticated workbench. |
| OPS-003 | Migration generation command shape | Session log | Use `--startup-project src/OpHalo.Keep.Infrastructure`; design-time factory needs `ConnectionStrings__DefaultConnection`. |
| OPS-004 | Test database reset pattern | Session log | Integration factory uses `DROP SCHEMA public CASCADE`, recreate schema, then `MigrateAsync`. |
| OPS-005 | Testing rate limiter behavior | Session log | `UseRateLimiter` is skipped in `Testing` intentionally. |
| OPS-006 | GitHub remote configured | Session log | Branch is `main` tracking `origin/main`; push local commits daily when green. |
