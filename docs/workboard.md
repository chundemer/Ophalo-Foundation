# OpHalo Workboard

**Canonical forward-looking board.** Update it in the same commit as the work it records. ADRs own decisions and contracts; build logs are append-only delivery history. The legacy [pilot readiness tracker](pilot-readiness-bug-tracker.md) is frozen as an archive.

## Operating rules

- An item enters **Now** only with a written decision, acceptance criteria, owner, and no unresolved prerequisite. Move its status in the delivery commit.
- **Next** is ordered but unstarted. **Decision queue** is unschedulable. **Deferred** is deliberately outside the current pilot sequence. **Done** needs evidence.
- Keep `session-log.md` to a short handoff; it does not duplicate this board or build-log history.
- Keep this board short and forward-looking. Unresolved ideas and deliberately parked topics live in [deferred-topics.md](deferred-topics.md); the frozen [pilot readiness tracker](pilot-readiness-bug-tracker.md) is the authoritative index of completed pilot-readiness work, each item carrying commit evidence.

## Now

GAP-073, GAP-094, GAP-099, GAP-063, and GAP-048 reviewed/verified and closed (Christian; 2026-09-15/16).

## Next

The foundation-first closed-loop / pilot-risk order is:

1. **GAP-100 — business-hours and closure-aware attention.** Promoted from DEF-025 (2026-09-17): the current pilot requires office-hours-aware signals. Complete after GAP-092 and before staff rely on after-hours, paused-clock, or business-hours-aware overdue/Needs Attention claims.
   - **Locked decision / validation gate:** [ADR-505](decisions/ADR-505-business-hours-closure-aware-attention.md) fixes one shared account calendar, per-target continuous/staffed timing, UTC deadline snapshots, and DST semantics. Closures are account-local `DateOnly` values; relational storage is fixed; staffed calculations/preflights have a five-local-calendar-year bound and return a controlled configuration error rather than falling back. Code review confirmed no persisted 15-minute first-response policy in live or development; the first 15→60 reconciliation is therefore limited to the unsaved-policy path. `BusinessHoursOnly` has no consumers and is retired rather than interpreted.
   - **Implementation boundary:** Server-owned timing/attention calculations must produce the authoritative due/overdue outcomes used by queues, badges, push timing, and all clients. The PWA only renders those outcomes in the account business timezone; it must not calculate business hours, holidays, or deadline state locally. No automated customer notification system is included.
   - **Delivery slices:** (1) 60/240/60 fallback reconciliation; (2) reusable non-API IANA-timezone validation; (3) policy/calendar schema; (4) audit-backed atomic settings persistence; (5) settings backend contract; (6) settings UI; (7) pure business-clock calculator; (8) public-intake writer; then customer-message, feedback, and inbound-external-contact writers separately. The calculator receives one coherent policy/calendar snapshot loaded by its writer; it performs no EF read. Public intake is a dedicated fork-worthy slice: its aggregate factory has 37 positional test call sites across 12 files. Preserve the existing SQL queue shape throughout.
   - **Explicit exclusion:** ADR-451 voicemail follow-up retains its current Mon–Fri local-midnight rule in this gap and may conflict with a staffed-hours calendar or closure. Its calendar-aware successor is [DEF-097](deferred-topics.md).
   - **Next: GAP-100 acceptance closure, locked order (Christian, 2026-09-21): (A) viewer-independence test — done (`32933aa1`); (C1a) remove the unused `IKeepRequestOperatePersistence.GetResponsePolicyAsync` (interface, EF impl, 6 `FakeOperatePersistence` stubs) — done (`6c968f44`); (C1b) remove the unused `IKeepCustomerWritePersistence.GetResponsePolicyAsync` (interface, EF impl, the single `FakeCustomerPersistence` in `KeepPushCustomerIntentHookTests`) — done (`c1b05317`); after C1a and C1b no persistence-seam interface exposes the response-policy read, and the shared timing snapshot port is the only reader; (G) architecture guard: production code outside Core must not call the four duration overloads (`CreateFromCustomerIntake`, `AddCustomerMessage`, `SubmitFeedback`, `LogInboundExternalContact`) — done (`9fd07c7d`) (IL-scan `KeepDeadlineOverloadGuardTests`: exactly-four-overloads assertion, positive control, no reflection/expression-tree coverage; ADR-505 Consequences updated; 3 new architecture tests, 17 passing); then the manual live check of Settings → Business Hours & Closures and an explained full integration run before GAP-100 closes.** The duration overloads are deliberately kept as test-seed/compatibility overloads (about 280 call sites across ~55 files); production writers must use the explicit/delegate deadline overloads, and the G guard enforces it. Slice A: the acceptance sentence "two viewers in different timezones" is true by construction, because no viewer timezone reaches the read path (the account timezone only feeds the write-side calculation). It is proven instead as viewer-independence: one Postgres test stamps an inbound Standard deadline under a Sydney staffed-hours account, then reads the request as two roles through both list and detail and asserts one identical UTC instant and one overdue value across all eight reads, equal to the stored deadline. Slice 11 (inbound-external-contact writer) is done (`b085f237`): `KeepRequest.LogInboundExternalContact` gains a lazy `Func<DateTime?>` Standard-deadline overload (called at most once, only when a customer-to-business flip or fresh attention is actually raised; never for an already-business-waiting request, a no-follow-up contact, or a validation failure); it stamps the result literally (null = attention raised with no deadline, never a duration fallback) and the legacy minutes overload delegates. `LogExternalContactService` reads the 9a snapshot only for an inbound contact with `RequiresBusinessFollowUp = true`, resolves the Standard target, never blocks the staff contact log, and after commit logs an error with account id, request id, target, and reason only (never the summary). Outbound, including the ADR-451 voicemail rule, is untouched. `IKeepRequestOperatePersistence.GetResponsePolicyAsync` now has no callers and joins the queued mechanical cleanup (with `IKeepCustomerWritePersistence.GetResponsePolicyAsync` and the intake, message, and feedback duration-parameter removals). With slice 11, all four GAP-100 writers (8-11) stamp business-clock deadlines. 6 new domain tests and 2 new Postgres integration tests (staffed-hours stamp; fail-closed no-deadline on a clock failure); 2,091 unit, 14 architecture, and 112 targeted external-contact/attention/related-work/row-auth integration tests passing; the full integration suite was not run (the flaky-test investigation is separate). Prior slice: Slice 10 (feedback writer) is done (`494d5ff0`): `KeepRequest.SubmitFeedback` gains a lazy `Func<DateTime?>` Priority-deadline overload (called at most once, only for negative feedback that passed validation; positive feedback never calls it); closing clears attention (`ClearAllAttentionForTerminal`), so negative feedback is always a new obligation and stamps the result literally (null = attention raised with no deadline, never a duration fallback); the legacy minutes overload delegates unchanged. `SubmitFeedbackService` reads the timing snapshot only for negative feedback, resolves the Priority target, never blocks the one-time feedback, and after commit and before push logs an error with account id, request id, target, and reason only (never the comment). `IKeepCustomerWritePersistence.GetResponsePolicyAsync` now has no callers and stays for the queued mechanical cleanup (about 6 test fakes), alongside the intake and message duration-parameter removals. 6 new domain tests, 4 new service tests; 2,085 unit, 14 architecture, and 190 targeted feedback/attention integration tests passing; the full integration suite was not run this slice (the unidentified flaky failure noted under 9b is being investigated separately). Prior slice: Slice 9b (customer-message writer) is done (`8ea427f9`): `KeepRequest.AddCustomerMessage` gains a `Func<PriorityBand, DateTime?>` deadline overload (no `firstResponseTargetMinutes`; the delegate is called at most once and never on a same-priority repeat); fresh/flipped obligations stamp its result (null = no deadline), and a Standard-to-Priority escalation stores the earlier of the new and existing deadlines (a failed calculation keeps the existing one, a null existing one adopts the new one; band and reason still upgrade). The legacy minutes overload delegates to it, so the guard is universal. **Behavior change:** escalating an already-overdue Standard request no longer resets its clock; the earlier existing deadline stands. `AddCustomerMessageService` reads the 9a snapshot, resolves through `KeepResponseDeadlineResolver`, never blocks the customer, and after commit and before push logs an error with account id, request id, band, and reason only (no customer content). `IKeepCustomerWritePersistence.GetResponsePolicyAsync` is now unused by this service and is retained for the queued mechanical cleanup, alongside the intake and message duration-parameter removals. 19 new domain tests, 6 new service test cases; 2,075 unit, 14 architecture, and 170 targeted customer-message/attention integration tests passing; a full integration run had 1 unidentified failure of 1,755 (previously observed flaky tests exist), not reproduced in the affected areas. Prior slice: 9a (shared response-timing snapshot and resolver; no behavior change) is done (`879764c7`): standalone `IKeepResponseTimingSnapshotPersistence` (Application/ResponseTiming; all three targets and bases plus the shared calendar; Continuous-only loads the policy only, any StaffedHours target reads policy, timezone, intervals, and closures in one read-only RepeatableRead transaction; one DI registration) and the pure `KeepResponseDeadlineResolver.Resolve(snapshot, First|Standard|Priority, nowUtc)`; slice 8 intake now uses both and its own snapshot/records are removed. 11 new resolver unit tests; 1,609 Keep unit, 170 intake/timing/rate-limit/snapshot integration, 14 architecture passing. 9b scope (locked): `AddCustomerMessage` gets a deadline-based overload (omits the unused `firstResponseTargetMinutes`), the legacy minutes overload delegates to it so the earlier-of-existing/new guard is universal; fresh/flip obligations get a null deadline plus a structured error log on clock failure, a failed escalation preserves the existing deadline, and a null existing deadline adopts the newly calculated one; the customer is never blocked and never falls back to continuous. Slices 10 (feedback) and 11 (inbound external contact) reuse the 9a port and resolver. Prior slice: Public-intake writer (slice 8) is done (`18a38872`): `CreateKeepPublicIntakeService` stamps `FirstResponseDueAtUtc` from `BusinessClock` via a new `IKeepIntakePersistence.GetFirstResponseSnapshotAsync` (Continuous loads the policy only; StaffedHours reads policy, timezone, intervals, and closures in one read-only RepeatableRead transaction). `KeepRequest.CreateFromCustomerIntake` gains an explicit-deadline overload (nullable, UTC, strictly after `nowUtc`; null means intentionally no deadline); the duration overload is unchanged for existing callers and its mechanical removal is queued. A clock failure (or unresolvable stored timezone) never blocks the customer and never falls back to continuous: the request is accepted with no deadline and a structured error-level log carries account id, request id, and the failure reason (no PII). The intake response is unchanged and exposes no deadline. 16 new unit and 4 new Postgres integration tests; 1,598 Keep unit, 168 intake/timing/rate-limit integration, 14 architecture passing. Prior slice: Pure business clock (slice 7): Pure business clock (slice 7) is done (`a7c79cda`): `BusinessClock.TryCalculate` (Core/Domain; UTC start + target + `ResponseTimingBasis` + one weekly-interval/closure snapshot + `TimeZoneInfo` → UTC deadline, typed `BusinessClockFailure` for invalid target/basis/start, no intervals, invalid calendar, and the five-local-year bound; never a continuous fallback; a start inside an open interval consumes immediately, a closed arrival waits for opening). `StaffedHoursReachability` now shares one regime-based DST helper (`StaffedIntervalMath`) with the clock, which also corrects intervals that open or close inside a transition hour. The "earlier of existing/new deadline" escalation guard is deliberately not in the clock; it belongs to slice 9's customer-message writer, not public intake (a new intake request has no existing deadline). 30 new clock tests; 1,582 Keep unit, 47 policy/calendar integration, 14 architecture passing. Prior slice: 6c (policy timing-basis controls) is done (`017dd6c6`): `KeepSetupPolicyResult` gains optional `firstResponseTimingBasis`/`standardResponseTimingBasis`/`priorityResponseTimingBasis` (optional in TypeScript only, tightened with `settingsVersion` in the later mechanical fixture slice; `PolicySection` blocks the save and never defaults to Continuous when one is missing), `api.updatePolicy` requires all three, three independent selectors, full-snapshot save with version, an advisory helper (no client block) when staffed hours is selected with no weekly interval, priority copy that disclaims on-call/emergency coverage, code→copy map using the real `KeepResponsePolicy.*` codes (the raw `ApiError.message` is no longer shown), 409 preserves the draft; 12 new tests. ADR-507 closure labels are complete (7a-1 `3ef69461`, 7a-2 `90b40dbb`, 7b `2431f46b`, 7c `57b6cf3b`); 7b and 7c deploy together, and only after a manual live check of Settings → Business Hours & Closures (add, edit, clear, remove, stale-save 409) — 6b and 7c have not been checked against a live API. The `CalendarSection` error-code follow-up (three `ERROR_COPY` keys lacked the backend's `KeepResponsePolicy.` prefix, so those 422s showed the generic copy) is done (`924f282a`): prefixed keys, tests now use the real codes, plus a regression that the bare names fall back to the generic copy.
   - **Complete (2026-09-22):** Focused business-clock and writer tests, the viewer-independence proof, and initial live Business Hours & Closures testing satisfy the GAP-100 acceptance gate. The Sentry capture-test isolation fix passed the full integration suite, 1,755/1,755 (2026-09-21). The calendar governs server-side deadline calculation; future calendar-consuming features need their own acceptance tests and do not reopen this gate.
   - **Acceptance target:** Two viewers in different timezones receive the same server-authored attention/deadline state; configured closures and office hours correctly pause or advance time only according to the locked policy; boundary, DST, weekend, holiday, and near-closing cases have focused server tests.
2. **Maintainability/refactor review — validated order (2026-09-22).** Sourced from an external (ChatGPT) code-quality pass, validated against the actual code (grep-verified, not taken at face value) and cross-checked by a second external (Codex) review; address one slice at a time, each independently compiling and gated per the batch-size rule. Do not fold into feature work.
   1. **Account-timezone display fix — highest priority; correctness, not cosmetic.** Five confirmed sites render a UTC instant via raw `new Date(...).toLocaleString()` in the *viewer's* browser timezone instead of the *account* timezone, violating ADR-505's rendering boundary (server computes, PWA only renders in account time). Two independently-sized groups: **Group A** (`timeZone` already in scope, same-file fix) — `RequestRow.tsx`'s `shortDate()` fallback branch, `TimelineEvent.tsx`'s `formatAuditDateTime` (the component already threads `timeZone` correctly into `formatEventTime` in the same file). **Group B** (no `timeZone` in scope; needs prop-threading) — `ActualWorkHistoryCard.tsx`, `ActualWorkComposer.tsx`, `ActualWorkWorkspacePage.tsx`'s inline visit-timestamp formatter (the page already has `businessTimeZone` via `useBusinessTimeZone()` and passes it to two other children, just not this one). Two safe, no-op sites confirmed by the same sweep: `helpers.ts`'s `formatDateOnly` and `RequestRow.tsx`'s date-only branch split y/m/d before formatting, so no instant→local conversion occurs — leave as-is. `accountLocalDate` (`businessTime.ts`) is already committed (DEF-037 frontend slice, `61e47430`); this is new work on top of merged code, not a pending diff. **Group A: done (`d62c92cf`)** — `RequestRow.tsx` and `TimelineEvent.tsx` now format via `accountLocalDate`/`formatInstant`; 54 targeted tests passing. **Group B: done (`3d28bc64`)** — new `formatInstantShort(isoUtc, timeZone)` helper added to `businessTime.ts` (month/day/hour/minute, no year, same UTC-fallback-label convention as `formatInstant`), chosen deliberately over reusing `formatInstant` so the fix stays strictly behavioral: `ActualWorkHistoryCard.tsx` and `ActualWorkComposer.tsx`'s `SubmittedVisits` keep their exact existing no-year visual format; `ActualWorkWorkspacePage.tsx`'s `ReadOnlyVisit` had no deliberate format (bare `toLocaleString()`) and is deliberately unified onto the same helper for one consistent submitted-visit timestamp format across all three surfaces. `timeZone` threaded as a new optional prop through `ActualWorkHistoryCard`, `ActualWorkComposer`/`SubmittedVisits`, and `ReadOnlyVisit`, sourced from each caller's already-in-scope value (`RequestDetailContent`'s `props.timeZone`, `RequestDetailActualWorkSection`'s `timeZone`, `ActualWorkWorkspacePage`'s `businessTimeZone`) — no new plumbing above those callers. 3 new regression tests (each a UTC instant crossing a calendar/hour boundary in `America/Los_Angeles`); 142 files / 1,326 tests passing, `tsc --noEmit` clean. **Item 2.1 (both groups) is complete.**
   2. **Shared enum-to-wire-string mappers.** `MapStatus`, `MapAttentionLevel`, `MapWaitingDirection`, `MapAttentionReason`, `MapFollowUpReason`, `MapIntakeUrgency`, `MapContactPreference` are independently reimplemented in 3–5 places (`KeepRequestDetailMapper`, `GetAvailableKeepRequestsService`, `LookupKeepRequestByPhoneService`, `KeepCustomerPageMapper`, `GetKeepRequestListService`). Extract into one Application-layer mapping module (not Core — these are transport/read-model wire strings, not domain concepts). Preflight the actual mapping tables and every consumer first; preserve any consumer that intentionally uses different public/customer-facing vocabulary than internal staff views. **Done (`b78d73be`).** Preflight found two things beyond the original claim: (1) live drift — `GetAvailableKeepRequestsService.MapStatus` was already missing `Spam`/`Test` (unreachable only because `ApplyAvailable`'s SQL filter excludes both upstream, not a live bug, but exactly the risk duplication invites); (2) a confirmed intentional divergence — `KeepCustomerPageMapper.MapIntakeUrgency` hides `Routine` from customers and returns null instead of throwing on an unmapped value, unlike the two identical staff-facing copies. New internal `KeepRequestWireMappers` (Application/Requests) holds the six byte-identical mappers plus the staff-facing `MapIntakeUrgency`; `KeepCustomerPageMapper` keeps its customer-only `MapIntakeUrgency` local with an explicit comment on why it stays separate. All 5 original consumers plus 2 external callers of the now-removed `KeepRequestDetailMapper.MapStatus` (`ActualWorkFinancialReadApiService`, `GetKeepRequestRelatedWorkService`) now call the shared module; `GetAvailableKeepRequestsService` picks up the full switch, closing the drift with no observable behavior change. One new exhaustive test file (every enum member per mapper, plus an unmapped-value throw case per mapper). 8 production files (at the batch cap) + 1 test file; 2,152 unit, 17 architecture tests passing, full solution build clean.
   3. **Staffed-hours reachability validation scaffolding.** **Done (`4cd69930`).** New `KeepSettingsPersistenceSupport.ValidateStaffedHoursReachability` consolidates the target-collection/timezone-resolution/loop scaffolding duplicated across `EfKeepResponsePolicyPersistence`'s policy-save and calendar-save paths and `StageTimeZoneChangeAsync`'s own reachability preflight (the underlying `StaffedHoursReachability.IsReachable` call was already shared). The caller's no-weekly-interval error is a parameter, so policy save keeps `StaffedTimingRequiresWeeklyInterval` and calendar save keeps `LastWeeklyIntervalRequired` — consolidation does not collapse the two distinct user-visible error codes; the always-`StaffedHoursTargetUnreachable` five-year-unreachable case is shared, since every path already agreed on that one. `StageTimeZoneChangeAsync` previously had no explicit no-weekly-interval fast-fail (fell through to the generic unreachable error); it now surfaces `StaffedTimingRequiresWeeklyInterval`, matching the policy-save path. 3 new regression tests, one per path, each locking in that path's exact error code. 2 production files + 1 test file; 21/21 targeted `EfKeepResponsePolicyPersistenceTests` passing.
   4. **`GetKeepRequestListService`.** **Done (`065fe82f`).** New internal static `KeepRequestRankingAndActionsBuilder` (Application/Requests, same shape as item 2.2's `KeepRequestWireMappers` — no DI/constructor changes) holds `ComputeRankingGroup`, `ComputeSeverity`, `BuildQuickActions`, and `BuildContactActions`, moved verbatim along with their private-only dependencies (`IsEffectivelyUrgent`, `QuickActionDefs`). `ComputeRowContext` deliberately stays on `GetKeepRequestListService` — not named in this item's scope, and a distinct UI-row-grouping concern, not ranking/severity/actions. 34 new direct unit tests (every ranking-group branch/order, every severity branch, quick-action/contact-action gating) on top of the existing 200 indirect tests in `KeepRequestListServiceTests.cs` (unchanged, still passing through `ExecuteAsync`). The status-check predicate was already unified (`302840c8`); no longer a duplication task. 2 production files + 1 test file; 234 targeted unit, 17 architecture tests passing.
   5. **`KeepEndpoints.cs` route-family split.** **Baseline done (`73138f6e`); split itself deferred to its own session.** New `KeepEndpointsRouteInventoryTests` boots the real app (`KeepApiWebFactory`) and asserts all 86 routes this file registers — path pattern, HTTP verb, whether authentication is required (this file has no named auth policies, only the bare default `.RequireAuthorization()` vs. none, so "auth-policy" collapses to that boolean), and rate-limit policy where attached (14 routes carry `.RequireRateLimiting(...)`; included beyond the literal "route+verb+auth-policy" wording since a silent drop during the split would be a real rate-limit-bypass behavior change, not just a code-shape change) — against a hard-coded baseline captured from the live endpoint registrations. Scoped to handlers declared on `KeepEndpoints` itself (`MethodInfo.DeclaringType`), since the file shares the `/keep` prefix with a dozen sibling files already split by family (`PriceBookEndpoints`, `OfferingAssemblyEndpoints`, etc.). Verified the test actually catches drift (deliberately flipped one expected value, confirmed failure, reverted). The family-file split (deciding where ~15+ shared private handler helpers land across new files) is separate follow-on work against this now-locked baseline.
   6. **`RequestDetail.tsx` decomposition** — contact logging, SMS/QR, service location, and page orchestration are independently extractable.
   7. **`ActualWorkComposer.tsx`** — its own later, intentional decomposition slice (already decided); not a drive-by change.
   8. **API response-DTO question** — endpoints returning Application-layer service results directly (`Results.Ok(result.Value)`) can expose newly-added fields without a deliberate contract change. This changes public API contract policy mid-pilot: route to the Decision Queue as an ADR, not a cleanup slice. Interim guard: serialization-contract tests over representative list/detail responses, without restructuring anything.
   - **Confirmed clean, no action:** backend sweep for sync-over-async (`.Result`/`.Wait()`/`GetAwaiter().GetResult()`) and N+1 query patterns (awaited DB calls inside loops) found none — all loops operate on in-memory aggregates with a single post-loop `SaveChangesAsync`, and list-persistence batch reads are correctly single-query-for-all-ids. `KeepRequestListPersistence.GetPendingFinancialReviewCountsAsync`'s comment claims it "mirrors exactly" a query in `EfActualWorkFinancialReviewPersistence`; unverified, flagged as a drift-watch item only, not a dedup candidate (different bounded contexts). Large files not otherwise listed above (`PriceBook.tsx`, `DetailPanels.tsx`, `ActualWorkDraftApiService.cs`, etc.) produced no finding — line count alone is not evidence per this file's own rule; not queued.
   - **Performance:** the `needs_status_check` count/row-filter evaluating its candidate set in memory per list request remains the existing accepted pilot-scale watch item (see DEF-037 below) — instrument only once real volume makes it material; no new performance issue found.
3. **DEF-037 — needs-status-check resurfacing.** Next after GAP-100: complete the already-locked human-review queue for stale active requests. Reconcile its existing five-calendar-day pilot default with GAP-100's account timezone before coding; it remains a server-authored review signal, never an automatic customer update, status change, or close.
   - **PWA placement (locked, Christian, 2026-09-22):** Secondary Views (alongside Watching), visible to Owner, Admin, and Operator — not Office Review, which stays owner/admin operational-decision queues (Ready to Close, Feedback Review, Actual Work Review) with no membership-visibility amendment. An authoritative, role-scoped tab count is required at launch, reusing the server's own calculation (no client approximation). Row treatment is dedicated-queue-only (no bleed into other views/tabs), restrained, server-authored status text ("No meaningful activity since [account-local date]", no "needs a status check" suffix — the tab already establishes that context); no urgency styling (not an overdue/SLA badge) and no client-side date calculation — the PWA renders the server's account-local `StatusCheck` values as-is (GAP-100 implementation boundary).
   - **Server-correctness slice: done (`b7d5ef16`).** `GetKeepRequestListService`'s threshold/due calculation moves from hard-coded UTC days to account-local calendar days: new narrow `IKeepRequestListPersistence.GetStatusCheckPolicyAsync` (`StatusCheckPolicySnapshot(TimeZoneId, ThresholdDays)`, EF impl reads `Account.TimeZone` + optional `KeepResponsePolicy.StatusCheckThresholdDays`, defaults to 5 with no policy row — ADR-339) is a list-only dependency, deliberately not added to the broader `AccountAccessSnapshot` or reused from the GAP-100 response-timing snapshot (wrong bounded context: no calendar/staffed-hours data belongs in this quiet review queue); the read runs after the permission/account-access/feature gates so a forbidden request never pays for it. The needs_status_check eligibility filter and every row's `StatusCheck` metadata (`IsDue`, `DueAtUtc`, `AgeDays`) now convert `nowUtc` and latest-meaningful-activity to the account timezone via `TimeZoneId.TryResolve` before comparing `DateOnly` values. `DueAtUtc` uses a dedicated `LocalMidnightToUtcSafe` helper rather than a raw `ConvertTimeToUtc`, because some IANA zones (e.g. America/Havana) transition exactly at local midnight and this path runs for every row on every list read: a normal midnight converts directly, an invalid spring-forward midnight advances to the first valid local instant, and an ambiguous fall-back midnight resolves deterministically to its earliest UTC instant — informational for a passive queue, not a stamped deadline, so no exception and no locked policy beyond "don't crash, don't move backward in time." An unresolvable stored timezone falls back to UTC-as-local (`TryResolve`'s default) with no exclusion and no per-read logging — a passive review queue must stay visible, and invalid timezones are prevented at the settings-write boundary, not here. 6 new unit tests (timezone-crossing-UTC-boundary regression, configured non-default threshold, unresolvable-timezone fallback, `DueAtUtc` local-midnight conversion, invalid-DST-midnight and ambiguous-DST-midnight regressions against America/Havana) plus 2 new Postgres integration tests for the EF read (default-5, stored policy/timezone); 2,097 unit, 17 architecture, 128 targeted list-API/persistence integration tests passing. 6 files: `StatusCheckPolicySnapshot.cs` (new), `IKeepRequestListPersistence.cs`, `KeepRequestListPersistence.cs`, `GetKeepRequestListService.cs`, `KeepRequestListServiceTests.cs`, `KeepPersistenceProofTests.cs`. PWA resurfacing (no current web consumer of `needs_status_check`) is a separate later slice.
   - **Backend count slice: done (`302840c8`).** `KeepRequestViewCounts` gains `int NeedsStatusCheck = 0` (trailing optional positional param — every existing 7-arg construction site keeps compiling unchanged). `GetKeepRequestListService` extracts the row-eligibility check into a private static `IsNeedsStatusCheckDue(KeepRequest, DateOnly localToday, TimeZoneInfo, int thresholdDays)`, used by both the needs_status_check row filter and the new count — one predicate, both call sites, cannot drift. `statusCheckLocalToday` is computed once and threaded through `ToSummary` into `BuildStatusCheckInfo` too (no longer recomputed per row from `nowUtc`), so the row filter, the count, and every serialized row's `StatusCheck` metadata all share the identical account-local "today." The count is computed unconditionally (every view, not just when active — matches readyToClose/feedbackReview), reusing the exact same `GetActiveViewRequestsAsync(..., ActiveViewKind.NeedsStatusCheck, ...)` SQL-narrowed candidate fetch the queue itself uses, with an all-default filter set (so an active search/status filter on another tab never narrows the badge) and the same role-derived `scope` (so an Operator's MyWork count cannot include another operator's work — the row-exclusion guarantee is enforced at the persistence/SQL layer, same as every other view). No new persistence interface method. Computed after the permission/account-access/feature gates (before query validation), and — to avoid shadowing the real active-view fetch's persistence-fake call tracking in tests — fetched earlier in the method than the page-building logic, so the real list fetch is always the chronologically-last `GetActiveViewRequestsAsync` call. 8 new unit tests (count matches actual queue-row count; account-local timezone, not UTC; unresolvable-timezone fallback; configured threshold; MyWork scope plumbing; not computed on 3 different forbidden/gate paths) plus 3 new Postgres integration tests (count included in response; count equals the queue's own row count in the *same* HTTP response — the strongest anti-drift proof, since both are computed from one `nowUtc`; cross-operator isolation with a second seeded operator). One integration-test-writing lesson: `OpHaloDbContext.SaveChangesAsync` stamps `CreatedAtUtc` from the real clock on insert (`EntityState.Added`), ignoring the domain factory's `nowUtc` param — backdating an "old" seeded row for `GetNeedsStatusCheckInputs`'s `Max(CreatedAtUtc, ...)` requires a second, `Modified`-state save (which only re-stamps `UpdatedAtUtc`), same pattern as this suite's existing `Account.TimeZone` override. 2,105 unit, 17 architecture, 56 targeted list-API integration tests passing (one unrelated pre-existing flaky cursor-HMAC test observed only under parallel run, passes standalone — not touched by this slice). 4 files: `GetKeepRequestListResult.cs`, `GetKeepRequestListService.cs`, `KeepRequestListServiceTests.cs`, `KeepRequestListQueryApiTests.cs`.
   - **Frontend badge-rendering slice: done (`61e47430`).** `needs_status_check` joins Watching in Secondary Views (all three roles); `countForTab` sources the server's authoritative count. `RequestRow` renders the locked copy ("No meaningful activity since [account-local date]", no urgency suffix) gated on both the active-tab cue prop and `statusCheck.isDue === true` (not just `sinceUtc` presence, so a wrong/excluded row reaching the component still renders nothing). New `businessTime.ts` `accountLocalDate` helper (zone-aware, date-only sibling of `formatInstant`) replaces a duplicate local copy, with 4 new unit tests incl. a UTC-day-boundary-crossing case. 9 files; `tsc --noEmit` clean; full suite 142 files / 1,321 tests passing. **DEF-037 is now complete** — both the server-correctness and count calculations, and their PWA surfacing, are done.
4. **DEF-063 — ready-to-close customer-activity warning.** Promote into the active sequence before staff routinely close resolved requests: complete the Request Detail warning and final copy using the existing server/list-row `HasCustomerActivityAfterResolution` signal. Do not add another queue or change close eligibility in this slice.

GAP-040 remains deliberately deferred until the application feature set stabilizes; reopen its existing
marketing-copy findings before promoting the public intake link. GAP-072 and GAP-047 are non-gating
future work. GAP-093 and GAP-095 are complete; GAP-054 and GAP-038 are recorded in the evidence index.

**Native work is deliberately not current.** GAP-091 is already code-complete but its review must
not displace this foundation-first sequence or make Quick Capture a relied-on workflow. Native S18
(push/device-token lifecycle) and S19 (internal testing, signing, universal links, store materials,
and App Store/Play submission) remain deferred until the foundation and closed loops are proven.

## Release readiness

| Item | Trigger / acceptance |
| --- | --- |
| GAP-069 — durable Data Protection and narrow proxy trust | Start roughly two weeks before Keep becomes the pilot's authoritative record for live requests, completed work, or customer follow-up—or before any released customer-approval flow relies on protected links. Founder upgrades Railway to Pro, enables daily backups and PITR, and waits for the first PITR recovery window. Then implement the PostgreSQL-backed, certificate-encrypted key ring with a stable production application discriminator and narrowly process `X-Forwarded-Proto` from the measured Railway ingress boundary; preserve `ClientIpResolver` as the client-IP authority. Prove a ciphertext from API instance A unprotects after a redeploy to instance B, verify forged forwarded headers fail safely, and run a safe restore drill. See [authoritative-pilot-release-readiness.md](runbook/authoritative-pilot-release-readiness.md). |

**Planned maintenance — `ophalo-web` Next 16.3.** [BL151](build-log/151-ophalo-web-next-16-3-maintenance-preflight.md)
locks the isolated routine upgrade from Next `16.2.9` to `16.3.4`, with a Node `22.x` engine pin and
regenerated lockfile/`next-env.d.ts`. React 19.3 and .NET SDK/container pinning are explicitly
separate follow-ups. This is not a pilot gate and must not be batched with GAP-073; Vercel preview
acceptance remains the deployment gate.

## Decision queue

| Item | Decision required before scheduling |
| --- | --- |
| GAP-064 | **Pilot operating decision:** minimum accountable staff-alert policy — recipient/fallback, channel, failure/escalation, quiet-hours, and privacy posture. Confirm whether the interim founder-watches-the-queue posture is sufficient for this pilot; otherwise lock and deliver the smallest accountable alert path before staff rely on unattended attention signals. |
| GAP-025 | Customer phone-number lifecycle. ADR-492 is only the narrow request-phone continuity guardrail; it does not solve a customer permanently changing numbers. Decide editable current unique phone, verified historical aliases, audit/authority, shared-or-recycled-number safeguards, and request-entered-phone snapshots. No inferred identity matching. Includes [DEF-060](deferred-topics.md) (request-level service location and address history): recognizing a returning customer safely depends on location/address context, not phone alone. |
| GAP-043 | V1 request-list scale model and verification threshold. |
| GAP-070 / GAP-071 | Optional-module commercial workflow and truthful discovery/handoff; no self-service entitlement write or guided setup. |
| Proposed Work & Commercial Quotes | Agree the pilot finish-line and sequencing for the existing field-scope foundation, Owner/Admin Proposed Work Review ([ADR-488](decisions/ADR-488-owner-admin-proposed-work-review-workbench.md), [BL127](build-log/127-owner-admin-proposed-work-review-preflight.md)), and office commercial documents ([BL130](build-log/130-office-commercial-estimate-preflight.md)). Decide which postures—Estimate, Fixed-Price Quote, T&M Authorization—the pilot actually needs. Public customer acceptance is a later separately sequenced capability with its product contract locked in [ADR-498](decisions/ADR-498-public-quote-acceptance-change-orders-and-accounting-boundary.md). |
| Commercial value & packaging | Decide the sellable OpHalo product around the market-analysis proposal. Every capability must close a business-critical loop with an authoritative fact, owner, next action, and observable recurring outcome; convenience alone is insufficient (see [Subscription Test](keep-product-positioning.md#subscription-test)). Then: (1) validate pricing and competitor claims with dated primary sources; (2) decide whether a dollar-valued revenue-control queue is a core commercial product or a later reporting feature ([DEF-094](deferred-topics.md)); (3) define the accounting-handoff path before considering sync ([DEF-086](deferred-topics.md)); (4) decide photo evidence separately from generic attachments ([DEF-013](deferred-topics.md)); and (5) sequence customer quote acceptance and automated delivery only after their consent, audit, and operational contracts are ready ([DEF-088](deferred-topics.md), [DEF-022](deferred-topics.md), [DEF-095](deferred-topics.md)). Packaging belongs in [DEF-058](deferred-topics.md). Pilot learning may validate assumptions, but is not the definition of the product. |

## Proposed Work & Commercial Quotes — current state

| Layer | State |
| --- | --- |
| Field Proposed Work capture | Merged: price-blind scope composer, catalog/assembly/custom lines, undo, nudges, submission, request signal, entitlement, and server release gate. Confirm the production release setting before treating it as pilot-available. |
| Owner/Admin Proposed Work Review | Not built. ADR-488 and BL127 lock the queue, history, review note, and explicit `Mark reviewed` transition. |
| Office commercial estimate / quote | Not built. There is no `OfficeQuote`, `QuoteRevision`, or `QuoteLine` domain/UI/API. BL130 locks the product boundary but requires a mechanical preflight. |
| Customer-facing quote | Later, separately sequenced capability. ADR-498 locks opaque revision viewing, typed-name electronic consent, all-or-nothing decision, audit snapshot, durable receipt, and change-order boundaries. Payment, invoicing, and accounting sync remain out of scope. |

**Price Book requirement:** use **Serial #** rather than SKU for the relevant item identifier. When
Price Book item-identity work is scheduled, define its scope, uniqueness, entry/edit authority, and
whether it identifies a catalog item, a specific installed unit, or both; do not silently introduce
SKU terminology or behavior.

## Deferred / pilot learning

- **PWA bundle performance — post-pilot measurement slice.** Vite reports that the current
  authenticated PWA entry chunk exceeds its default 500 kB raw warning threshold (observed bundle:
  approximately 959 kB minified / 238 kB gzip). Do not raise `build.chunkSizeWarningLimit` merely
  to hide the warning. First capture real pilot navigation/performance evidence, then use
  route-level lazy loading for heavy workspaces and re-measure the resulting chunks. Not a pilot
  gate and do not batch with current release or safety work.
- **GAP-037:** founder weekly value report. (GAP-038 promoted to **Next**, immediately after GAP-054.)
- **GAP-041, GAP-046, GAP-026, GAP-053:** request-list selection, filter, search, and action-order refinements.
- **GAP-044:** completed/cancelled-work discoverability.
- **Native mobile app — parked, outside this pilot.** Field-execution app is Session 14+; store submission is S19. Stack and posture are locked: [ADR-236](decisions/ADR-236-mobile-native-app-technology-stack.md) (promoted by ADR-385). Open submission work is tracked in the [mobile store submission checklist](mobile-store-submission-checklist.md). GAP-051 native phone-formatting parity rides this track; public-web delivery is done.
- **GAP-066:** Catalog Item financial/operational-impact workspace.
- **GAP-067:** request-workspace presentation pass; blocks GAP-042 until screenshot acceptance.
- **GAP-070, GAP-071:** P2 optional-module work, pending their Decision Queue entry.
- **4g close advisory, Price Book direct-cost visibility, workbench brand alignment:** carry-forward items; not pilot blockers.
- **Acceptance passes:** Request UI Upgrade 1.1 ([BL139](build-log/139-request-ui-upgrade-1.1-implementation.md)) and Settings & Getting Started V2 §5 ([BL144](build-log/144-settings-and-getting-started-v2-upgrade.md)).
- **Minimum Office Closeout:** resumes after controlled-pilot and rehearsal gates ([BL135](build-log/135-minimum-office-closeout-mechanical-preflight.md)).
- **DEF-096 — Actual Work history read efficiency and request-abort telemetry:** a 2026-09-17 Sentry `OperationCanceledException` was an aborted read request, not a write/data incident. Later add narrow request-abort-only Sentry suppression, share the duplicate Request Detail history fetch, and fold history bounding/batched performer-name resolution into the existing GAP-082 query-hygiene work. Reprioritize only on recurrence, latency evidence, or materially large histories. See [deferred topic](deferred-topics.md).
- **AUDIT-V1-A — line-entity input bounds (pilot-risk).** Add length caps + `*TooLong` errors and an upper/precision bound on quantity to `ProposedScopeLine` (`Note`, `OffCatalogDescription`, `quantity`) and `ActualWorkLine` (`Note`, `quantity`), with rejection tests both directions. Currently unbounded free-text from a field user reaches persistence. Bounded single-slice. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 1, F1.6 + F1.7.
- **AUDIT-V1-B — tenant-scoping & internal-route defense-in-depth (hardening).** One slice: add `AccountId` to the `GetAllEventsAsync` / `GetParticipantsAsync` predicates (F1.1) and to the call/SMS/intake-SMS handoff lookups (F1.3); hash `KeepRequest.PageToken` at rest (F1.2); attach a route-level policy to `/internal/*` entitlement endpoints and an allowlist check on `featureKey` (F1.8, F1.9); add `Guid.Empty` / invite-email guards (F1.10). None exploitable today. Also folds in the Vector 2 hardening set (F2.6–F2.13): feedback persist/notify atomicity + raw-body window, missing version headers on `share-intent`/handoff, concurrent `POST /accounts/me/invite` → 500 (add unique-violation catch), `FeedbackSubmission`/`PriceBookVersionLine` cascade tightening, internal-entitlements version-in-body → 400. And the Vector 3 hardening set: actor-display-name batch seam to kill the N+1 in the actual-work reads (F3.7), defensive `Take` caps on the naturally-small unbounded lists (F3.8), feedback metrics/retention-sweep indexes (F3.12), cleanup-service per-run timeout (F3.14). And the Vector 4 frontend hardening set: global React Query `QueryCache`/`MutationCache` `onError` (F4.11), fail-loud on missing `NEXT_PUBLIC_APP_BASE_URL` instead of the `localhost:5173` fallback (F4.12), narrow the over-broad `onError → onConflict()` attribution (F4.14), fix the `#/pricebook` not-entitled copy for non-owner roles (F4.15), in-app workspace switcher (F4.17). Source: [production-readiness audit](audits/production-readiness-audit.md) Vectors 1–4.
- **AUDIT-V2-A — request-creation double-submit protection (pilot-risk).** No `Idempotency-Key` infrastructure exists. `POST /keep/public-intake/*` (F2.4) and `POST /keep/requests` staff create (F2.5) have no server-side dedup — a double-tap creates duplicate requests + events + queue items. Add a dedup window (account + canonical phone + description hash) or accept a client idempotency token. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 2.
- **AUDIT-V2-B — auth continuation atomicity (pilot-risk).** `/auth/exchange` multi-workspace / name-required branches consume the magic-link code in a standalone autocommit before a separate `PostAuthContinuation` insert; a failed insert burns the link with no in-band recovery. Wrap consume + insert in one `BeginTransactionAsync` (as `CommitNewAccountExchangeAsync` already does). Small slice. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 2, F2.1.
- **AUDIT-V2-C — concurrency-conflict recovery contract (pilot-risk).** The 409 body is opaque — return the current server `ConcurrencyVersion` + entity state so a client can merge instead of losing an in-progress edit (F2.2); resolve the intentional `publish-price` non-rotation of `CatalogItem.ConcurrencyVersion` so a concurrent header edit can't save on stale context (F2.3). Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 2.
- **AUDIT-V3-A — server-side pagination for the unbounded read paths (pilot-risk; GA-blocker).** Active-view request list (F3.1), request event timeline / `GetAllEventsAsync` (F3.4), and actual-work review-queue (F3.2) all load full entity sets with no SQL `LIMIT` and paginate/sort in memory; the list cursor is a linear re-scan. Port the DB-side keyset + `Take(limit+1)` pattern already used by the history path. Fold in the 7-sequential-`CountAsync` view-count collapse (F3.6). Holds at pilot scale, must land before broad scale-out. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 3.
- **AUDIT-V3-B — index tune (hardening; one migration).** Add `keep_request_events (request_id, occurred_at_utc)` (F3.3 — 3× per list render), the review-queue partial index (F3.2), and history / catalog-browse sort compounds (F3.10); drop the 3 redundant single-column indexes and note the degenerate `ix_keep_requests_account_attention` 3rd column (F3.9); add `deleted_at_utc IS NULL` to the two partial unique indexes (F3.11). Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 3.
- **AUDIT-V3-C — `FeedbackDeliveryWorker` multi-instance safety (pilot-risk; GA-blocker before API scales past one replica).** Row claim uses a plain `SELECT … LIMIT 100` — add `FOR UPDATE SKIP LOCKED` / a lease, a per-pass timeout on the founder-channel calls, and wrap `NotifyAsync` so a throwing notifier can't starve the retention sweep (F3.5). Alternatively resolve structurally by deleting the dead `src/OpHalo.Worker` scaffold or adopting it as the real single-instance background host (F3.13). Correct on one replica (current pilot posture — see Next-list note / mobile + deploy posture). Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 3.
- **AUDIT-V4-B — error-boundary resilience (pilot-risk).** One app-wide `ErrorBoundary` whose `hasError` never resets, with a Reload button that reloads the same hash route → a deterministic render crash on `#/request/<id>` is an inescapable loop (F4.3). Reset on route change + wrap the workbench / memory rail / account menu / modals in independent boundaries with a "back to list" affordance. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 4.
- **AUDIT-V4-C — identity-load error handling (pilot-risk).** `["me"]` has no error branch → a 500 on `/auth/me` hangs the shell on a spinner forever (F4.4); `AuthGuard` treats any transient error as a 401 → an outage looks like a mass logout (F4.5); `main.tsx` bootstrap is unguarded except the one config branch (F4.6). Retry on transient, distinguish 500 from 401, guard `bootstrap()`. Also fold in F4.13 (consolidate the three inconsistent `["me"]` observers into one `useMe()` hook). Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 4.
- **AUDIT-V4-D — capability-probe + entitlement-signal consistency (pilot-risk).** A non-403 error on the per-request capability probe silently removes the "Record completed work" entry point with no message (F4.7); the nav `["capabilityPackages"]` cache (`staleTime: 5min`, never invalidated) diverges from the live per-request probe, so a mid-session enrollment or role change lags on the nav for minutes with no in-app affordance (F4.8). Show a retry strip instead of silently hiding; invalidate `["capabilityPackages"]` alongside `["me"]`. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 4.
- **AUDIT-V4-E — 409 recovery outside the two good composers (pilot-risk).** `conflictDisabled` / `noteConflictDisabled` / `priorityConflictDisabled` are set in ~10 places and reset in zero — a 409 permanently deadens the control while `refetchOnWindowFocus` silently rebases the data underneath it (F4.9); write-failure errors are not announced to assistive tech — no `role` or a conditionally-mounted `aria-live` that never fires (F4.10, F4 G2); no in-app "refresh this view" button on 409 (F4.16). Reset the flag on version change, route errors through `announcePolite()`, pass `refetch` to conflict handlers. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 4.
- **AUDIT-V5-A — Quick Capture draft persistence (pilot blocker; field-work loss).** `mobile/ophalo-mobile/app/modal.tsx` holds the whole capture (phone, name, email, description, service address) in component state; offline disables Save with no local save / send-when-online queue and no dismiss guard, so a capture in a no-signal basement or a backgrounded app loses everything (F5.1). AsyncStorage-backed autosave + restore-on-reopen + `beforeRemove` discard confirm; build into the S17f real Quick Capture form, not the current modal. **Pilot gate** before field techs rely on capture. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 5.
- **AUDIT-V5-B — business-timezone display (pilot-risk).** The IANA-validated `Account` timezone (ADR-073) is returned by the API but no render path reads it — every `toLocale*` / `Intl.DateTimeFormat` call formats in the viewer's device timezone (F5.4), and `helpers.ts` `isDateOnlyToday` / `isDateOnlyPast` compute follow-up "today / overdue" from device-local `now` against ADR-451's business-TZ promise (F5.5). Add `useBusinessTimeZone()` + a shared `formatInBusinessTz` / `businessToday` helper and route every formatter and date-only comparison through it. Also folds F5.7 (mobile `useNetworkState` treats unknown connectivity as online) into GAP-075. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 5.
- **AUDIT-V6-A / F6.1 — customer-page rate limiting (pilot-risk).** `GET /keep/r/{pageToken}` — the anonymous customer request page — had no `.RequireRateLimiting(...)`, unlike every other anonymous route in the API, and isn't read-only: it debounce-writes `CustomerPageLastViewedAtUtc` on every non-expired hit. **Implemented, awaiting Christian's diff review** — attached the existing composite IP+pageToken `"customer-write"` policy (ADR-129), matching every other `{pageToken}` route (confirmed the public tracker at `web/ophalo-web/src/app/keep/r/[pageToken]/page.tsx` does a single server-side no-store fetch per view, no polling — 10/min is generous, and the composite key is required since every customer's read arrives from the Next.js server's one egress IP). `src/OpHalo.Api/Keep/KeepEndpoints.cs` (1 line) + 3 new proof tests in `tests/OpHalo.IntegrationTests/Api/RateLimitIntegrationTests.cs` (429-at-11th-request, per-token bucket isolation, and the debounced write proven to still hold under the limiter). 25/25 focused tests pass. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 6, F6.1.
- **AUDIT-V6-A / F6.2 — drop unused `RequestId` from public-intake response (hardening, deferred).** `POST /keep/public-intake/*` returns an unused raw `RequestId` alongside `pageToken` / `referenceCode` — the frontend only reads the latter two. A dormant, low-impact internal-ID exposure, not bundled into F6.1: `tests/OpHalo.IntegrationTests/Api/KeepPublicIntakeSlugApiTests.cs:198,214,278` and `KeepIntakeApiTests.cs:127,643` assert on / query by `body.RequestId`, so this needs those repointed first — a tiny separate DTO-contract cleanup. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 6, F6.2.
- **`pg_trgm` full-text search — pilot-exit, not a pilot fix.** `filters.Q` (5× `LIKE '%q%'` incl. `description` varchar 4000) and catalog substring search are unindexable without a `pg_trgm` GIN index; every filtered list render is a multi-column full-text scan. Fine at pilot volume, a hard cliff at a few thousand rows. Add GIN indexes on `keep_requests (customer_name, reference_code, description)` + catalog equivalents before GA. Source: [production-readiness audit](audits/production-readiness-audit.md) Vector 3.

## Audit and operational gap registry

The temporary `AUDIT-V*` labels below resolve to these permanent GAP identifiers. Each remains a
separately gated implementation slice; the identifier groups related findings, not a license to
batch unrelated code.

| GAP | Scope | Findings / status |
| --- | --- | --- |
| GAP-073 | Request-detail composer draft safety | F4.1–F4.2, F5.2–F5.3; pilot blocker. [ADR-502](decisions/ADR-502-request-detail-composer-draft-safety.md) / [BL150](build-log/150-gap-073-request-detail-composer-draft-safety.md). **073-1 landed `3599ee3c`** — `useComposerDraft` shared sessionStorage store + `readOnly`-on-409 (both composer paths) + version-advance reset + `beforeunload`-only dirty registry; browser verification done (Christian). **073-2 landed 2026-09-10** — dead-prop-chain deletion (4 prod + 6 test). `ophalo-app` 1184/1184. **GAP-073 reviewed and merged 2026-09-15 (Christian; no findings).** Non-composer 409 recovery → GAP-085. |
| GAP-074 | Line-entity input bounds | F1.6–F1.7; pilot-risk. |
| GAP-075 | Tenant, transaction, and lifecycle defense-in-depth | F1.1–F1.5, F1.8–F1.10, F2.6–F2.13, F5.7; hardening. |
| GAP-076 | Request-creation idempotency | F2.4–F2.5; pilot-risk. |
| GAP-077 | Auth-continuation atomicity | F2.1; pilot-risk. |
| GAP-078 | Concurrency-conflict recovery contract | F2.2–F2.3; pilot-risk. |
| GAP-079 | Bounded read-path pagination | F3.1–F3.2, F3.4, F3.6; pilot-risk / GA blocker. |
| GAP-080 | Database index hygiene | F3.2–F3.3, F3.9–F3.11; hardening. |
| GAP-081 | Feedback-worker multi-instance safety | F3.5, F3.13; before multi-replica API. |
| GAP-082 | Backend query and retention hygiene | F3.7–F3.8, F3.12, F3.14; hardening. |
| GAP-083 | Route-scoped error-boundary recovery | F4.3; pilot-risk. |
| GAP-084 | Identity/bootstrap resilience | F4.4–F4.6, F4.13; pilot-risk. |
| GAP-085 | Entitlement and accessible-409 recovery | F4.7–F4.10, F4.16; pilot-risk. |
| GAP-086 | Frontend resilience cleanup | F4.11–F4.12, F4.14–F4.15, F4.17; hardening. |
| GAP-087 | Validated Help & Updates publisher | Interactive founder preview/validate/upload flow; removes manual JSON/R2 editing. Also cover content lifecycle: no automatic pruning of stale `known_issue`/`whats_new` entries exists today against the 100-entry / 4 MB feed cap — flagged during 038-R3 verification 2026-09-11. |
| GAP-088 | Public incident fallback | Independently reachable outage/maintenance page; no client-side-PWA redirect dependency. |
| GAP-089 | Incident communications runbook | Publish/resolve process, `highlight` discipline, templates, and external-channel fallback. |
| GAP-090 | Scalable text search | `pg_trgm` GIN indexes for request/catalog substring search; pilot-exit. |
| GAP-091 | Quick Capture draft persistence | F5.1; pilot blocker (field-work loss). Code-complete, awaiting review. [BL152](build-log/152-gap-091-quick-capture-draft-persistence.md). |
| GAP-092 | Business-timezone display | F5.4–F5.5; pilot-risk. Device-TZ formatting + device-local follow-up day boundaries vs ADR-073 / ADR-451. |
| GAP-100 | Business-hours and closure-aware attention | ADR-505 is locked; validated, re-sliced implementation begins with unsaved 60/240/60 fallback reconciliation, then timezone validation, schema/settings, pure business clock, and isolated writer slices. Server-owned SLA/attention policy, not a PWA-only display change; preserve stored UTC queue deadlines. |
| GAP-093 | Session-layer account-lifecycle gate | F8.3; supervised-pilot gate. **Reviewed, committed as `b7c815e4`, and pushed to `main` 2026-09-11** — [BL153](build-log/153-gap-093-session-account-lifecycle-gate.md): account lifecycle fails closed in session auth, independently of opt-in service policy. |
| GAP-094 | Auth-code / invite issuance rate limiting | F8.4–F8.5, F8.11; supervised-pilot gate. [BL154](build-log/154-gap-094-auth-issuance-rate-limiting.md) locks a PostgreSQL-authoritative per-email/per-account throttle alongside the existing per-IP policy, split into public magic-link issuance (094-1) then authenticated invite issuance (094-2). **Both slices reviewed and merged 2026-09-15 (Christian; no findings)** — 094-1 `197457b8`, 094-2 `f1de909b`. |
| GAP-095 | Auth-response enumeration hardening | F8.9–F8.10; pilot-risk. [BL155](build-log/155-gap-095-auth-response-enumeration-hardening.md): **095-2 (drop `entryContext` from stale/used-code `/auth/exchange` failures) reviewed and committed as `d1d2d0dc` 2026-09-11. 095-1 (move magic-link email dispatch off the request path via a bounded `Channel<T>` background dispatcher with drop-on-full admission and a bounded graceful-shutdown drain, closing the start/signin timing oracle) reviewed and committed as `67648891` 2026-09-11. GAP-095 complete.** |
| GAP-096 | Vector 8 hardening batch | F8.1–F8.2, F8.6–F8.8, F8.12–F8.13; hardening, pre-GA, no pilot urgency. |
| GAP-097 | Guide discoverability | Flat `guides[]` list has no search, categorization, or in-app deep link (e.g. from "New Request" to the matching guide); flagged during 038-R3 verification 2026-09-11 when discussing "how do I add a request" help lookup. Client-side only: search box over already-fetched guides, optional `category` field, `#/help?guide=<id>` deep link. No new endpoint or migration. |
| GAP-098 | Feedback follow-up identity and honest closure | **Complete.** Slice A (backend identity resolver, alert enrichment, DI) and Slice B (frontend confirmation copy) both landed 2026-09-13 — [BL156](build-log/156-gap-098-feedback-founder-identity-preflight.md). Resolve before 038-R3, which remains next. The private, access-controlled founder Google Chat alert must include server-resolved business name, submitter display name, membership role, email, and stable account/account-user IDs. No internal deep link is in scope: there is no cross-workspace support/admin route or workspace-switching authorization model, so do not invent one. Never trust client-supplied identity, include phone by default, or persist a redundant contact snapshot in `feedback_submissions`; retry delivery resolves the current account-user record and still delivers if it is unavailable. The restricted Google Workspace feedback register is the pilot accountability record: manually log correlation ID, safe follow-up metadata, response outcome, and any Help & Updates link; no duplicate full body, phone, in-app ticket-status UI, SLA promise, or bidirectional Google Chat integration. Change successful-submission copy to: “Thanks — we review every submission. We may contact the account owner if we need more detail. Updates that affect multiple businesses will appear in Help & Updates.” Deliver as two commits: (a) backend identity resolver, alert enrichment, DI, and backend tests; (b) frontend confirmation copy and its tests. Verify identity resolution and unavailable-user fallback, the frontend copy, authenticated end-to-end delivery to the private channel, and the register workflow. |

## Done / evidence index

The frozen [pilot readiness tracker](pilot-readiness-bug-tracker.md) is the authoritative record of all completed pilot-readiness work, each item carrying commit evidence. This table lists only items that need a pointer here because they were not tracked there.

| Item | Evidence |
| --- | --- |
| GAP-099 | Team-member access clarity, closed 2026-09-16. Slice 1 — cancel a pending invite: `198423ca` (via `65d9d82e`), [BL157](build-log/157-gap-099-invited-member-cancel-invite.md). Slice 2 — removed-member action contract fix + invite-action clarity: `198423ca`, [BL158](build-log/158-gap-099-removed-member-action-contract-fix.md). Slice 3 — invite-accept `Invite.AlreadyActive` message honesty (retained `InviteTokenHash`/`InviteExpiresAtUtc` through the original expiry, no migration): unit 19/19, integration 31/31. [ADR-504](decisions/decision-index.md) — no delete/purge for Removed rows. Disable/Enable access-clarity matrix (items 1-4) verified live against the deployed Team list with Owner and Admin accounts. |
| GAP-050 | Account-scoped related-work indicator for the same canonical customer: `924b808e` (backend read path), `bdf90c33` (panel + navigation); [BL096](build-log/096-phase-4-request-detail-preflight-handoff.md). Promoted from DEF-050; never appeared in the legacy tracker. |
| GAP-033 | Public-intake trust/event allowlist: `11c19d3d`, `89a776d8`; [BL145](build-log/145-gap-033-public-intake-trust-and-event-feed-allowlist.md). |
| GAP-038 | Feedback / Help & Updates production release: implementation ([BL149](build-log/149-gap-038-in-product-feedback-help-updates-implementation.md)) plus founder activation/verification **038-R0–R3, done 2026-09-14** — banner dismissal and a harmless feedback submission confirmed reaching the founder channel; founder accepted the single-webhook outage posture. GAP-098 (feedback follow-up identity) resolved first. A founder publisher/preview tool replacing manual JSON/R2 editing is deferred as **GAP-087** (publishing strategy to be revisited later). |
| GAP-039 implementation | API/PWA Sentry code/runbooks: `d7d0ee22`, `fd34af34`, `baf07265`, `a69a8edf`, `70e75a3f`. |
| GAP-039 Batch 4 | Operational production-candidate verification **done 2026-09-14**: console config, PWA and API controlled-error tests, the browser-scrubber fix (`0c3c8b3e`), the invalid-`VITE_PUBLIC_BASE_URL` fail-safe test (verified via existing automated tests), and named incident roles — see [sentry-configuration.md](runbook/sentry-configuration.md). [ADR-495](decisions/ADR-495-gap-039-redacted-error-capture-and-release-safety.md), [BL140](build-log/140-gap-039-sentry-implementation-handoff.md). |
| GAP-065 / GAP-065A | Financial-review discovery: `faf7b64`, `e27c48c`, `6ab880b`, `baaeff1`, `f231126`, `606203d`; [BL138](build-log/138-gap-065-owner-admin-financial-review-discovery-and-delivery-plan.md). |
| GAP-068 | Multi-workspace sign-in/invited name: `755c7eaf`, verification `2cda916d`; [BL143](build-log/143-multi-workspace-signin-and-invited-name-handoff.md). |
| GAP-016 / GAP-021 | ADR-444 phone path; native parity separately deferred. |
| GAP-051 public web | Configured business-phone display; [BL147](build-log/147-gap-051-public-web-business-phone-display.md). |
| GAP-063 | Spam/Test terminal classification from Request Detail — quiet Admin actions menu, accessible confirm dialog, optional ≤500-char reason, version-snapshotted submit, 409/403 preserves reason and refetches detail: `5872f135`. [ADR-296](decisions/decision-index.md), [ADR-435](decisions/ADR-435-request-list-action-cockpit-boundary.md). Manually verified live by Christian, 2026-09-16. |
| GAP-048 | Tracker-bearing email in the Request Detail Contact customer drawer now requires an explicit post-launch confirmation before it posts `{ method: "email" }`; `mailto:` launch alone creates no event or state change; plain email (no `canRecordShareIntent`, or no tracker) carries no token and no confirmation; success invalidates both request-detail and request-list caches so `NeedsShare` reflects server truth: `115bb862`. [ADR-372](decisions/decision-index.md), [ADR-381](decisions/ADR-381-tracker-link-sharing-footprint.md). Manually verified live by Christian, 2026-09-16. |
| GAP-049 | Closed-request follow-up prefill (`buildFollowUpDescription` in `request-detail/helpers.ts`) reserves the provenance-prefix length and truncates only the copied source text at the last whitespace boundary within the remaining room, appending an ellipsis and a `wasTruncated` flag; falls back to a hard cut when no whitespace boundary exists; never mutates the original closed-request record. Landed in `21baaab5` (bundled with GAP-047/048), never marked done on this board. 4/4 unit tests passing (`helpers.followUpDescription.test.ts`), confirmed 2026-09-16. |
| GAP-092 | Business-timezone display, complete 2026-09-17. `useBusinessTimeZone()` (role-agnostic `['me']`, five-minute freshness, no new endpoint) and pure `businessTime.ts` day-boundary/formatting helpers landed across Request Row/Detail Hero (1a/1b), Request Detail/communications timestamp consumers (2a–2d), and Actual Work/history consumers (3), each an independently shippable slice within the batch-size gate. Slice 3-cleanup removed `helpers.ts`'s `undefined` compatibility branch — `formatDate`/`formatEventTime` now require `timeZone: string | null` — and folded in `buildAttentionGuidance` (`DetailPanels.tsx`'s `HeroAttentionBanner`, threaded through `RequestDetailWorkCanvas.tsx`), the one remaining one-argument call site found by repo-wide grep. [ADR-073](decisions/decision-index.md), [ADR-451](decisions/decision-index.md). 540/540 request-detail unit tests passing. |
| GAP-100 Batches 1-4 complete; GAP-100 in progress | [ADR-505](decisions/decision-index.md)'s re-sliced sequence. Batch 1 — canonical unsaved response-policy defaults reconciled to 60/240/60: new `KeepResponsePolicyDefaults` (Core) as the single source; `KeepSetupService`'s stray 15-minute first-response default fixed; `CreateKeepPublicIntakeService`, `AddCustomerMessageService`, `SubmitFeedbackService`, `LogExternalContactService` all point at the shared constants: `e1a8987b`. Batch 2 — reusable non-API IANA timezone resolver: `SharedKernel.Time.TimeZoneId.TryResolve` replaces `AuthEndpoints`'s private validator (rejection use) and `LogExternalContactService`'s inline lookup (ADR-451 UTC-fallback use, behavior preserved); no product/persistence/HTTP coupling: `d8e50df9`. 9 focused unit tests (resolver + external-contact), 24 `AuthStartTests` integration tests, 14 architecture tests passing. Batch 3 — policy/calendar schema only, no writers/settings surface yet: `KeepResponsePolicy`'s stale `BusinessHoursOnly` replaced with three per-target `ResponseTimingBasis` (Continuous/StaffedHours) columns defaulting to Continuous; new `KeepCalendarWeeklyInterval` (half-open `[OpensAt, ClosesAt)`, unique per account+weekday) and `KeepCalendarClosure` (unique per account+local date) tables. Migration `20260918215047_Gap100PolicyCalendarSchema` applied to local dev; scaffolded `defaultValue: ""` on the three new non-null columns was corrected to `"Continuous"` before apply (existing per-account policy rows would otherwise get an unparseable enum value). Schema verified live via `psql`. Batch 4 — audit-backed atomic settings persistence, re-sliced into 4a/4b/4c to stay under the batch-size gate. 4a: `KeepSettingsAuditEvent` (append-only, account-scoped, mirrors `KeepRequestEvent`'s actor-triple pattern, five event categories incl. `TimeZoneChanged`), migration `20260918225208_Gap100SettingsAuditEvent`: `e6b8e47a`. 4b: `KeepResponsePolicyService` with three narrow authorized operations (`UpdatePolicyTargets`, `UpdateCalendar`, `UpdateTimeZone`), each delegating to `EfKeepResponsePolicyPersistence`, which owns a `Serializable`-isolation transaction per operation (cross-aggregate validation, write, audit, commit — matching `EfCatalogItemCreateAndActivatePersistence`); `StaffedHoursReachability` is a pure, bounded (~1826-iteration) five-year preflight computing real DST-aware elapsed minutes (handles both spanning and wholly-inside-transition intervals); a concurrent-write race test proves the staffed-hours/weekly-interval invariant survives real transaction overlap, not just sequential calls: `4a465c26`. 4c — closed the one remaining ungoverned bypass: `KeepSetupService.UpdateProfileAsync`'s direct `Account.TimeZone` write now routes through the same governed path (shared `KeepSettingsPersistenceSupport.StageTimeZoneChangeAsync`), with `IKeepSetupPersistence.SaveProfileWithTimeZoneAsync` committing the business-name/profile fields and the governed timezone change atomically — a rejected timezone change discards the whole save, proven by both a fake-persistence unit test and a real Postgres integration test: `6ea887bb`. GAP-100 batch 4 is complete; Railway production table-presence verification confirmed all four settings tables on 2026-09-20. Remaining slices at that point (settings backend/UI, pure business clock, isolated writer slices) were not started; see the Next bullet above for current status. Settings backend (ADR-506 batch 5) is sequenced as 5a (version + read; **done** — pure `KeepSettingsVersion` hash, `GET /keep/setup` extended with string timing bases, calendar, and `settingsVersion`, `IKeepSetupPersistence.GetCalendarAsync`; the profile/policy PUT responses share the extended result; no writes or error mapping changed: `3316c997`; 43 focused unit, 70 setup/onboarding integration, 14 architecture tests passing), 5b-1 (governed policy persistence; **done** — `KeepResponsePolicyErrors.SettingsVersionMismatch`, version recomputed inside the serializable transaction before validation, omitted bases preserve persisted values, changed-fields-only `field: before -> after` audit with first policy as `unset -> value` and no-op silent, once-per-account `PolicySaved` ops event preserved for onboarding, route unchanged: `7bfc9d27`; 14 policy persistence/race tests, 31 onboarding/setup integration, 1,526 Keep unit, 14 architecture tests passing), 5b-2 (route rewire, string-basis body, 409/422 mapping — **deferred until batch 6** because the current PWA policy save sends no `settingsVersion`; the `SavePolicyAsync` cleanup is a separate later slice), 5c, re-sliced (approved 2026-09-20) into 5c-1a/5c-1b/5c-2 to stay under the batch-size gate (full 5c-1 was 13 files): **5c-1a** (governed calendar persistence, no route/mapper/setup-service dependency; **done: `2fad6272`** — `expectedSettingsVersion` on `IKeepResponsePolicyPersistence`/`KeepResponsePolicyService.UpdateCalendarAsync`, recomputed inside the serializable transaction before validation, missing/stale returns `SettingsVersionMismatch` with nothing written; closure audit text is now `YYYY-MM-DD: absent -> closed` / `YYYY-MM-DD: closed -> absent`; 4 direct callers updated (3 persistence-test, 1 race-test); 17 policy persistence/race tests, 1,526 Keep unit passing), **5c-1b** (**done: `1cbc1c4a`** — `PUT /keep/setup/calendar` — `UpdateCalendarBody`, `KeepSetupService` gains `KeepResponsePolicyService` and a full-snapshot-to-add/remove diff returning the refreshed snapshot, `KeepEndpoints`, and the `ErrorHttpMapper` entries locked by ADR-506: 409 `SettingsVersionMismatch` + `ConcurrentSettingsChange`; 422 duplicate weekday/closure date, overlapping closure change, staffed-hours unreachable, last-interval-required, staffed-timing-requires-interval; binding/parse stays 400; only `KeepSetupServiceTests` constructs `KeepSetupService`; `settingsVersion` passed through unchanged incl. missing/empty → governed 409; parse/binding failures return `KeepSetup.CalendarValidation` → 400; 8 new unit cases, 12 new `KeepSetupCalendarApiTests` integration cases, 79 setup/onboarding integration, 1,527 Keep unit, 14 architecture tests passing; `ConcurrentSettingsChange`/`OverlappingClosureChange` mappings are explicit but not reachable through the HTTP fixture), **5c-2** (**deferred entirely until batch 6**, decided 2026-09-20 — backend enforcement would 409 every actual timezone change from the current PWA, which sends no `settingsVersion`; same compatibility constraint as 5b-2; batch 6 must include the client's canonical setup read and `settingsVersion` pass-through; preflight found no drift; the locked rule is unchanged — profile timezone versioning: `settingsVersion` required only when the requested timezone differs from the stored one; `IKeepSetupPersistence`/`EfKeepSetupPersistence`/`UpdateProfileBody`/`KeepSetupService`, three fake persistences, `EfKeepSetupPersistenceTests`); settings UI is batch 6, approved 2026-09-20 as 6a-1 (**done**: backend `3b58e50c`, frontend `60c10cfc`; policy route rewire + policy-client `settingsVersion` pass-through; 12 policy API + 9 service unit tests, 1,270 web tests passing; manual live Settings-page check passed 2026-09-20 — save, stale-save 409 with Refresh recovery and draft kept, 400 on a zero duration, onboarding policy step — so the deployment hold on the backend commit is **lifted**; adds a `KeepSetup.PolicyValidation` 400 pre-check in `KeepSetupService.UpdatePolicyAsync` for any numeric field <= 0, because the governed path otherwise lets the domain `ArgumentException` escape as a 500; the domain stays authoritative), 6a-2, split 2026-09-20 (batch-size gate: ~15 files) frontend-first — **6a-2a** (frontend, **done: `8021ab4c`**: `updateProfile` sends `settingsVersion`; `CompanySection` blocks a timezone change only when no version is available, never a profile-only save; 409 shows conflict copy, keeps the draft, explicit Refresh settings, no auto-retry; refresh handler intentionally duplicated from `PolicySection`, no shared hook yet; `Settings.tsx` passes `settingsVersion` + stored `timeZone`, and now resyncs `profileDraft` from a new `setup` only while the draft is unedited, with `CompanySection` `onSaved` re-syncing to the canonical result after a successful save, so a 409 → Refresh keeps the typed draft; 5 new `CompanySection.settingsVersion` tests plus a parent-level 409 → Refresh preservation regression in `Settings.branding.test.tsx` (verified to fail against the old always-reset sync), 55 settings tests passing, typecheck clean; backend ignores the extra field, so no deploy hold) then a mechanical extraction slice (**6a-2-pre**, done `10e9faf0`: `KeepSettingsPersistenceSupport.ComputeVersion` replaces the two inline snapshot-mapping + `KeepSettingsVersion.Compute` copies in `EfKeepResponsePolicyPersistence`; no behavior change; 41 policy/calendar/setup persistence + API integration and 14 architecture tests passing; loading stays with each writer because they reuse the rows for validation) then **6a-2b** (backend, i.e. 5c-2, calls the helper instead of inlining a third copy: `IKeepSetupPersistence`/`EfKeepSetupPersistence`/`KeepSetupService`/`UpdateProfileBody`/`KeepEndpoints` + 4 test files; version checked inside the serializable transaction only when the timezone differs, before IANA validation; missing/stale → 409; **done: `88eb993b`**: the stored timezone is read fresh inside the serializable transaction and is the only state used to decide whether the timezone differs and to compute the version, because the caller's pre-transaction `Account` may be stale; the tracked entity's `TimeZone` is aligned (original + current) to the stored value so the staged business-name change survives and no phantom change is audited; `UpdateProfileBody.SettingsVersion` optional; 6 new persistence cases incl. two stale-account regressions verified to fail against the stale-entity behavior; 1,979 unit, 45 focused setup/onboarding integration, 14 architecture passing; full integration 1,736/1,738 in ~6.5 min — the two failures (`SentryUnhandledCaptureTests` passes alone; catalog `Create_TwoConcurrentCreatesInSameAccount_ExactlyOneWins` passed 4/4 and on clean HEAD) are unrelated and load/race-flaky), 6b (**done** — `a8c9d03b`: calendar + closures section, its own Settings section "Business Hours & Closures" (tab + Account Menu entry), per-day switch with Open/Closed badge, full-snapshot save with `settingsVersion`, 409 keeps the draft with explicit refresh, code→copy map with generic fallback for `KeepSetup.CalendarValidation`/`OverlappingClosureChange`; 12 files; full web suite 141 files / 1291 tests; not yet checked against a live API. Deferred, not in V1: closure reasons (ADR-505 has no labels), customer-facing preview, "apply 9–5 to weekdays" shortcut, two-column layout), and 6c (policy timing-basis controls). **Type transition (6a-1 frontend, shipped):** `KeepSetupResult.settingsVersion?: string` is temporarily optional in TypeScript only, to keep the slice under the file cap (nine test fixtures build `KeepSetupResult` literals); `PolicySection` enforces a runtime nonempty-version submit guard and shows minimal 409 recovery ("settings changed" copy, explicit Refresh settings action, no automatic retry, draft preserved); a dedicated mechanical slice later tightens the type to required and updates the fixtures. |

## Pilot gate checklist

The current evaluative pilot may continue while its records are recoverable from the founder's local
backup and Keep is not the authoritative operating record. Before authoritative live use: GAP-069's
Railway Pro backup/PITR and recovery proof, GAP-099 before staff access is managed, GAP-073 before
staff send customer updates, GAP-091 before field techs rely on Quick Capture, GAP-063 (done
2026-09-16) before staff rely on the active queue, GAP-048 (done 2026-09-16) before sharing private
pages, and GAP-049 (done 2026-09-16) before relying on closed-request follow-ups. GAP-092 (done
2026-09-17) before multi-timezone use. GAP-100 is required before staff rely on after-hours,
paused-clock, or business-hours-aware overdue/Needs Attention signals. GAP-040 is separately
required before marketing/public-intake promotion, not before the present evaluative pilot. GAP-047
was dropped as a gate on 2026-09-07 (staff do not triage by Internal priority). GAP-064 needs a
written alert-policy decision; until then the founder deliberately watches the queue.
