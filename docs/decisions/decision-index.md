# Decision Index — OpHalo Foundation

The living ledger of architecture/product decisions for **this** build. One line per
decision. This file is authoritative for the new project.

> The legacy reference app has its own `decision-index` and `decisions/` — those
> contain many decisions that no longer apply and are **not** used here. Consult
> them only as historical reference (`_reference/docs/`), and only after validation.

## Conventions

- Add a one-line entry the moment a decision is made. Expand to a full ADR under
  `docs/decisions/ADR-NNN-*.md` only when the rationale needs more than a line.
- IDs are sequential and never reused. A reversed decision is marked **Superseded**
  (link the successor), not deleted.

**Status legend:** `Implemented` = decided and built/verified in this repo ·
`Locked` = decided (build plan §4), not yet built · `Proposed` = under discussion ·
`Superseded` = replaced.

---

| ID | Decision | Status | Source |
|----|----------|--------|--------|
| ADR-001 | Greenfield boundaries, brownfield behavior — clean structure, port proven behavior; legacy app is reference/fallback until parity | Locked | plan §1, §12 |
| ADR-002 | GitHub repo `ophalo-foundation`; internal solution/namespaces stay `OpHalo.*` (do **not** rename to `OpHalo.Foundation.*` — collides with the Foundation layer) | Implemented | this session |
| ADR-003 | One API host `OpHalo.Api` — collapse the legacy two-host (API + Continuity.API) setup | Locked | plan §4.4 |
| ADR-004 | One Foundation work model, no Hangfire; flexible hosting (in-proc pilot worker / separate `OpHalo.Worker` at scale) | Locked | plan §4.5 |
| ADR-005 | One PostgreSQL DB, one `OpHaloDbContext`, clean migrations (no prod data to preserve) | Locked | plan §4.6 |
| ADR-006 | Magic links for entry/recovery + trusted server-side opaque sessions; **no JWT** | Locked | plan §4.7 |
| ADR-007 | Authorization model: Account entitled · User permitted · Session trusted · Action allowed. Roles Owner/Admin/Operator/Viewer; statuses Invited/Active/Suspended/Removed; **permission keys** over scattered role checks | Locked | plan §4.8 |
| ADR-008 | Product-specific user settings attach to `AccountUser` (e.g. `KeepUserSettings`) — no separate product identity | Locked | plan §4.9 |
| ADR-009 | Entitlements via **feature keys**, not plan-name checks; no generic feature-flag/billing engine in v1 | Locked | plan §4.11 |
| ADR-010 | Admin shell lives in `OpHalo.Api` + `ophalo-app/app/admin`; internal-only, read-only first; writes audited; no impersonation in v1 | Locked | plan §4.18 |
| ADR-011 | Public token posture standardized in Foundation (high-entropy, hashed, expiry, rotation, revocation, public guard, rate limits, no client-trusted AccountId) | Locked | plan §4.19 |
| ADR-012 | Solution skeleton + **real** architecture tests (NetArchTest) before any behavior movement | Implemented | commit `00227b0` |
| ADR-013 | Exclude legacy families from the target: `Signal.*`, `Continuity.*`, `Platform.*`, separate `Auth` project, second API host | Implemented | build-log/001 |
| ADR-014 | Disciplined SharedKernel: `Result`/`Error`/`IClock` only. Canonical `ICurrentUser` + `IEmailSender` live in `Foundation.Application` (forbidden in SharedKernel); duplicate abstractions collapsed | Implemented | build-log/002 |
| ADR-015 | _(Phase4-001)_ `AccountUserRole` = Owner/Admin/Operator/Viewer. Legacy maps Owner→Owner, Admin→Admin, Technician→Operator, Member→Viewer. Roles are product-neutral; "Technician"/"Member" dropped as trade-specific/vague | Implemented | build-log/003 |
| ADR-016 | _(Phase4-002)_ `MembershipStatus` = Invited/Active/Suspended/Removed. PendingInvite→Invited; **add** membership-level Suspended (per-member access block, distinct from account-lifecycle Suspended) | Implemented | build-log/003 |
| ADR-017 | _(Phase4-003)_ Foundation `AccountUser` is **membership-only** (AccountId, UserId, Role, MembershipStatus, invite lifecycle, ownership ref, timestamps). Push subscription, `HasSeenMemberWelcome`, `HasDismissedInstallPrompt` excluded → move to KeepUserSettings / AccountUserDevice / NotificationDestination later | Implemented | build-log/003 |
| ADR-018 | _(Phase4-004)_ Foundation `Account` trimmed to identity + lifecycle + purpose + core profile. Quotas/credits→entitlements/usage (§4.11), commercial/plan/billing→AccountEntitlements/commercial profile, public slug + intake token→Keep public-intake (§4.19), notification policy→AccountNotificationPolicy | Implemented | build-log/003 |
| ADR-019 | Primary ownership modeled as `Account.PrimaryOwnerAccountUserId` (FK), **not** an `AccountUser.IsPrimaryOwner` flag — guarantees exactly-one primary owner; greenfield (ADR-005) means no migration cost. Ownership is separate from role | Implemented | build-log/003 |
| ADR-020 | Foundation `Account` keeps `TimeZone` (account-wide, product-neutral: quiet hours, business-hours, reporting boundaries, SLA, scheduling). `ServiceCategory` is deferred — vertical/business-domain context belongs to a KeepBusinessProfile / later AccountBusinessProfile satellite, not Foundation identity | Implemented | build-log/003 |
| ADR-021 | Foundation `Account` carries **no** `Email` — account-level identity is the business (`BusinessName`); inbox identity lives on `User`/`AccountUser`. The legacy `Account.Email` is dropped, not ported | Implemented | build-log/003 |
| ADR-022 | `Account.CreateVerified(businessName, purpose, timeZone)` — `BusinessName` + `TimeZone` are **required & non-nullable** at creation; `Purpose` is an explicit argument (no hidden default). The legacy create-then-name onboarding path (nullable name, `ApplyBusinessNameSnapshot`) is **not** ported in 4a. IANA time-zone format validation stays upstream (request validation), not in the domain | Implemented | build-log/003 |
| ADR-023 | `AccountUser.IsActive` is a **computed** projection of `MembershipStatus == Active`, not a stored field — removes the dual-write hazard introduced by the new per-member `Suspended` status | Implemented | build-log/003 |
| ADR-024 | `Account.AssignPrimaryOwner(AccountUser)` is a **self-validating** aggregate method (same account / `Owner` role / `Active` status) returning `Result`, not a bare id setter — the aggregate guards the ADR-019 invariant so an unrelated/non-owner/inactive member can never be assigned | Implemented | build-log/003 |
| ADR-025 | Pending invites accept **any non-owner role** (Admin/Operator/Viewer); `Owner` is reserved to `CreateOwner`. Reverses the legacy "Member-only" invite constraint under the role rename (ADR-015) | Implemented | build-log/003 |
| ADR-026 | All Foundation account enums carry **explicit numeric values** (1-based) to prevent reorder drift before persistence exists. The obsolete `AccountCommercialState.Pilot` is dropped (greenfield); pilots model as Trial + a pilot flag on entitlements | Implemented | build-log/003 |
| ADR-027 | _(Phase4b-001)_ `AccountEntitlements` slice scope = **commercial/operating posture + the entitlement state machine + `MaxUserSeats` + `IsPilot`** only. It is the producer the 4a access policy lacked (sources `CommercialState`/`OperatingMode`/`TrialEndsAtUtc`/`PastDueGraceEndsAtUtc`). Methods renamed/trimmed from legacy: `Cancel` (was `CancelCommercial`), `ResolvePastDue` (was `ResolvePastDueBilling`); the legacy `IsDelinquent`/`DelinquentSinceUtc` flags are dropped — `CommercialState == PastDue` + a non-null grace window is the single source of truth | Implemented | build-log/004 |
| ADR-028 | _(Phase4b-002)_ Legacy halo booleans (`HasQuoteRecoveryHalo`/`HasRepeatServiceHalo`/`HasContinuityHalo`) are **dropped entirely**, not ported and **not** replaced with a `keep.enabled` placeholder. Avoids a second gating model before real feature keys exist: commercial state answers "is this account commercially allowed?"; feature keys (later permissions slice) answer "which features may it use?". Halos belong to ADR-013-excluded Signal/Continuity families | Implemented | build-log/004 |
| ADR-029 | _(Phase4b-003)_ `AccountPlan` = `Trial/Starter/Professional/Business/Enterprise/Internal` (explicit 1-based values). **Pilot is not a plan** — it is the separate singular `IsPilot` cohort flag (no generic flag-bag). Plan is a label, not a gate (ADR-009); the tier matrix/pricing stay unlocked (plan §4.11) | Implemented | build-log/004 |
| ADR-030 | _(Phase4b-004)_ The `AccountEntitlements → AccountAccessContext` bridge (`ToAccessContext`) lives in **Foundation.Application** as an extension method, **not** on the Core entity: `AccountAccessContext` is an Application type and Core must not depend on Application (§8). The entity owns only 4 of the 8 context inputs (lifecycle/purpose come from `Account`, the off-season exemption from the request, the clock from the caller) | Implemented | build-log/004 |
| ADR-031 | _(Phase4c-001)_ Slice scope = **§4.8 permission-key layer only**; split from §4.11 feature keys/limits (→ Phase 4d). **Permission key** (can this *user* do this *action*?) and **feature key** (is this *account* allowed this *capability*?) are deliberately distinct catalogs that compose later, never collapse. `keep.*` admitted into 4c only when it is a user action (`keep.requests.respond` ✓, `keep.enabled` ✗) | Implemented | build-log/005 |
| ADR-032 | _(Phase4c-002)_ Permission keys use a hierarchical `domain.resource.action` convention in a Foundation-owned string catalog (`PermissionKeys`) — adopted over the plan's flat-verb examples. Strings only (no Keep type refs) so the catalog lives in Foundation without breaching the no-Keep boundary (§8). `internal.` prefix marks purpose-gated keys | Implemented | build-log/005 |
| ADR-033 | _(Phase4c-003)_ Role hierarchy Owner ⊇ Admin ⊇ Operator ⊇ Viewer is expressed via **explicit set composition**, never enum numeric comparison — `AccountUserRole` values are identity, not authorization arithmetic. Two maps: `Base` (`account.*`/`keep.*`, every account) + `Internal` (`internal.*`, Internal-purpose accounts only, same hierarchy) | Implemented | build-log/005 |
| ADR-034 | _(Phase4c-004)_ `UserAccessPolicy.IsPermitted(role, membershipStatus, accountPurpose, key)` composes three gates: (1) only `MembershipStatus.Active` is permitted anything; (2) `internal.*` keys additionally require `AccountPurpose.Internal` — preserving the legacy `AdminGuard` semantic that internal authority comes from the account's purpose boundary, not a role name; (3) explicit role-map check. **Fail-closed**: empty/whitespace/unknown/non-canonical keys denied, no trimming. Matrix locks: `account.billing.manage` Owner-only; Operator gets `keep.insights.view`; internal support/platform keys deferred | Implemented | build-log/005 |
| ADR-035 | _(Phase4d-001)_ Slice scope = **§4.11 feature/limit layer**. Capabilities are **derived from the plan** via a static `PlanEntitlements` map ("plans produce entitlements"), **not** stored as per-account feature booleans on `AccountEntitlements` — that would recreate the legacy halo-boolean model dropped in ADR-028. The account's durable entitlement state stays compact (Plan/CommercialState/OperatingMode/MaxUserSeats/IsPilot); runtime authorizes against feature keys, never plan names (ADR-009) | Implemented | build-log/006 |
| ADR-036 | _(Phase4d-002)_ Two **separate** entitlement catalogs: `FeatureKeys` (boolean capability — "is it enabled?", `domain.capability`) and `FeatureLimitKeys` (numeric allowance — "what is the allowance?", `domain.x_limit`). Both distinct from `PermissionKeys` (4c) — feature/limit = account is entitled, permission = user is permitted; they compose, never collapse. `keep.active_request_limit` (a §4.11 example) is **deferred** — only `account.user_limit` + `keep.monthly_request_limit` are in v1 locked scope | Implemented | build-log/006 |
| ADR-037 | _(Phase4d-003)_ Usage limits resolve as **plan default with per-account override**; in v1 only `account.user_limit` is overridable, via the existing stored `AccountEntitlements.MaxUserSeats` (`> 0` wins, else plan default). No new per-account limit storage is added. "Unlimited" is the `int.MaxValue` sentinel (`FeatureLimitKeys.Unlimited`) so the natural `count < limit` call-site comparison needs no special-casing | Implemented | build-log/006 |
| ADR-038 | _(Phase4d-004)_ `FeatureAccessPolicy` is **fail-closed** (blank/unknown feature key → `false`; blank/unknown key or unmapped plan → limit `0`; exact match, no trimming) and **standalone** — no combined permitted+entitled facade until a real caller exists (composition at the call site: account allowed **and** user permitted **and** account entitled). The provisional tier matrix and limit values are intentionally **NOT locked** (§4.11): Trial is full-featured for evaluation, Starter omits mobile push + insights, limits constrain usage | Implemented | build-log/006 |
| ADR-039 | _(Provisioning-001)_ Account-creation orchestration is a **pure domain provisioning service** (`AccountProvisioningService` in Foundation.Application) — composes the four Phase 4 factories into the canonical `User + Account + owner AccountUser + AccountEntitlements` graph and returns it as `Result<AccountProvisioningResult>`. **No repositories / `IUnitOfWork` / MediatR / command-handler / DI** are introduced: the slice answers "what graph should exist?", and the persistence seam (save contract, transaction, unique constraints, aggregate boundary) is deferred to the persistence phase to avoid early-abstraction churn at that exact seam. Uses `DateTime` (UTC) to match the entity factory contracts, not the sketch's `DateTimeOffset` | Implemented | build-log/007 |
| ADR-040 | _(Provisioning-002)_ Cross-aggregate provisioning rules are **fail-closed**: the internal-account triad (purpose `Internal` ⇒ plan `Internal`, no pilot, no trial window) and the non-internal trial-window requirement return `Result.Failure`; seat allowance is **plan-derived** (`PlanEntitlements.LimitDefaults[plan][account.user_limit]`, ADR-037) — the method takes **no** seat parameter, so account↔entitlements seat drift is unrepresentable. Per-field validity (non-empty name, UTC timestamps, IANA TZ) is trusted to the entity factories (which throw); `Result.Failure` is reserved for composition rules. Primary owner is assigned in-memory pre-persistence (IDs exist at construction, ADR-019/024) and its failure is propagated. Provisioning errors live in Core `AccountErrors` (its single-source-of-truth directive) | Implemented | build-log/007 |
| ADR-041 | _(Persistence-001)_ `OpHaloDbContext` lives in **Foundation.Infrastructure** (DbSets for `Account`/`AccountUser`/`User`/`AccountEntitlements`; `ApplyConfigurationsFromAssembly`; ctor takes SharedKernel `IClock`). Two legacy behaviors ported **verbatim**: `SaveChangesAsync` timestamp interception (Added ⇒ `CreatedAtUtc`+`UpdatedAtUtc`; Modified ⇒ `UpdatedAtUtc`, via `IClock`) and the soft-delete **global query filters** over every `BaseEntity` — **except `AccountEntitlements`** (foundational satellite, ADR-025, always visible). The legacy `SystemRecord` / non-`BaseEntity` SaveChanges branches are dropped (not present here). `BaseEntityConfiguration` ports verbatim: key `ValueGeneratedNever` (domain mints v7 GUIDs), enums `HasConversion<string>().HasMaxLength(50)`. Design-time `OpHaloDbContextFactory` reads its own config (user-secrets→env→appsettings); snake_case; migrations history `__OpHaloMigrationsHistory`; `SystemClock` ported into Infrastructure on the single SharedKernel `IClock`. DI/host wiring + `appsettings` connection string **deferred to Phase 5** (no caller yet) | Implemented | build-log/008 |
| ADR-042 | _(Persistence-002)_ Three deliberate divergences from the legacy config: **(1)** `AccountUser.IsActive` is `builder.Ignore`d, not a column — it is a computed projection of `MembershipStatus == Active` (ADR-023); **(2)** primary ownership is a **nullable FK** `Account.PrimaryOwnerAccountUserId → AccountUser` (`OnDelete.Restrict`, no navigation), replacing the legacy `is_primary_owner` flag + `ix_account_users_one_primary_owner` filtered unique index (both dropped, ADR-019) — nullable owner FK + `Restrict` resolves the **circular FK** (`Account↔AccountUser`) so the migration creates `account_users`, then `accounts` with its owner FK inline, then adds `account_users.account_id → accounts` as a separate `AddForeignKey`; **(3)** migrations history table `__OpHaloMigrationsHistory`. Trimmed legacy columns (quotas/credits, halo booleans, Stripe/billing/delinquency, slug + intake token, `Account.Email`, push/welcome/install-prompt, `ProvisioningKey`, pilot dates) are absent because the entities were already trimmed in Phase 4a/4b — not reintroduced. Migration `InitialFoundationSchema` generated + inspected; `database update` against a live DB is the operator step | Implemented | build-log/008 |
| ADR-043 | _(Persistence-003)_ Phase 6B persistence proof tests use **Testcontainers.PostgreSql** against real PostgreSQL, not EF InMemory or SQLite. The proof target is provider/database behavior: migration application, partial/filtered unique indexes, FK enforcement around the Account↔AccountUser cycle, Npgsql duplicate-key errors, snake_case DDL, and the `AccountEntitlements` soft-delete filter exemption. Tests construct `OpHaloDbContext` directly with Npgsql + snake_case + `__OpHaloMigrationsHistory`; no repositories, `IUnitOfWork`, runtime DI, appsettings wiring, or host registration are introduced in this slice. Use the existing xUnit v2 lifecycle manually (`IAsyncLifetime`) rather than adding a Testcontainers xUnit integration package | Implemented | build-log/009 |
| ADR-044 | _(Persistence-004)_ Persisting the provisioning graph **requires two-phase save** due to the circular FK (Account↔AccountUser, ADR-019/042): **(1)** insert `User`, `Account` (with `primary_owner_account_user_id = NULL`), `AccountUser`, `AccountEntitlements` — EF cannot topologically order mutually-referencing inserts; **(2)** update `Account.primary_owner_account_user_id` to the owner's id. This is the **canonical persistence contract** for provisioning, not a test workaround. The real persistence boundary (Phase 6 repository/unit-of-work) must encapsulate this two-phase save; callers must never manage it manually. No domain or schema change required. Discovered and proven in the Phase 6B integration tests | Implemented | build-log/010 |
| ADR-045 | _(Keep-001)_ Phase 7 is split under the Session Size Rule. **Phase 7A** builds the Keep domain/persistence foundation for intake-to-list (`KeepCustomer`, `KeepRequest`, `KeepRequestEvent`, Keep-owned public intake link, token/reference services, EF mappings, migration, persistence proof tests). **Phase 7B** builds the Application/API vertical (public intake endpoint, create-request service, operator request list endpoint, entitlement/permission/public guard/rate-limit checks, HTTP tests). The full legacy Continuity path spans Core/Application/Infrastructure/API/auth/rate limiting/notifications/rich list read models/SSE; splitting prevents dragging the whole surface forward before the schema and domain contracts are proven | Implemented | build-log/011 |
| ADR-046 | _(Keep-002)_ Public intake identity is a **Keep-owned hashed token satellite**, not columns resurrected on `Account`. Add a Keep public intake link keyed by `AccountId` with `PublicSlug`, `TokenHash`, revocation/audit/soft-delete state, and active unique indexes for slug/hash. Raw public intake tokens are high-entropy opaque values hashed at rest (SHA-256 lowercase hex); public slug is routing/display context, not authority. This adapts the legacy raw `Account.PublicIntakeToken` lookup to ADR-018/021's trimmed Account model and build-plan §4.19's public-token posture: high entropy, hashing where appropriate, rotation/revocation, public guard, rate limits, and no client-trusted `AccountId` | Implemented | build-log/011 |
| ADR-047 | _(Keep-003)_ `BaseEntityConfiguration<T>` changed from `internal` to `public` so Keep.Infrastructure can extend the Foundation base mapping (key `ValueGeneratedNever`, enum-as-string) instead of duplicating it. Visibility only — no behavior change | Implemented | build-log/012 |
| ADR-048 | _(Keep-004)_ `OpHaloDbContext` takes an optional `IEnumerable<Assembly>? additionalModelAssemblies`; `OnModelCreating` applies Foundation's own assembly first, then each additional assembly. This lets bounded contexts (Keep) register EF configs **without Foundation.Infrastructure taking a compile-time dependency on Keep** (§8 boundary). Enforced: no `DbSet<KeepT>` named properties on the context; Keep callers use `context.Set<T>()`. One `OpHaloDbContext`, one schema (ADR-005) | Implemented | build-log/012 |
| ADR-049 | _(Keep-005)_ Migrations for the whole schema stay in **one operational assembly, `OpHalo.Foundation.Infrastructure`** (single context = single migration history `__OpHaloMigrationsHistory`). `KeepDesignTimeDbContextFactory` in Keep.Infrastructure is the **migration entrypoint** when Keep tables are involved (`--project Foundation.Infrastructure --startup-project Keep.Infrastructure`): it constructs `OpHaloDbContext` with the Keep assembly so configs are discovered, while migration files land in Foundation.Infrastructure. Foundation's own factory remains for Foundation-only generation but is not used for full-schema migrations | Implemented | build-log/012 |
| ADR-050 | _(Keep-006)_ `KeepRequestStatus` carries an explicit `Cancelled = 6` alongside `Closed = 5`. Terminal differentiation: `Closed` = work resolved, `Cancelled` = stopped without resolution. The cancellation reason lives on a `KeepRequestEvent` content field, not on the status enum — the enum stays a small lifecycle vocabulary, not a reason carrier | Implemented | build-log/012 |
| ADR-051 | _(Keep-007)_ Phase 7B public intake uses `/keep/public-intake/token/{publicIntakeToken}` as the canonical route and maps legacy `/continuity/public-intake/token/{publicIntakeToken}` as a cutover alias only. Both hit the same Keep handler; no `Continuity` namespaces/projects/tables/application types are reintroduced. Operator list is Keep-only: `GET /keep/requests` | Superseded by ADR-298 | build-log/013; build-log/047 |
| ADR-052 | _(Keep-008)_ Phase 7B ships a token-only backend route first. `publicSlug` stays stored/unique for future friendly URLs, but token hash resolution remains the only authority. Future slug routes must treat slug as context/display, not authorization | Implemented | build-log/013 |
| ADR-053 | _(Keep-009)_ Customer email delivery on intake stays deferred to the notifications phase. Phase 7B accepts/stores `CustomerEmail` and `EmailNotificationsEnabled` for contract continuity but does not send email, create notification audit rows, or wire notification transports | Deferred — notifications phase | build-log/013; ADR-092; ADR-289 |
| ADR-054 | _(Keep-010)_ Real Phase 5 auth is not a prerequisite for Phase 7B. Operator list uses `ICurrentUser` plus Foundation access/permission/feature policies and may use test fakes in integration tests; production behavior must fail closed when unauthenticated or unresolved | Implemented | build-log/013; build-log/016 (Phase 5 auth delivered) |
| ADR-055 | _(Keep-011)_ Keep.Application uses **intent-revealing persistence abstractions** (`IKeepIntakePersistence`, `IKeepRequestListPersistence`) rather than an injected DbContext or DbSet-exposing interface. Account state consumed as `AccountAccessSnapshot`/`AccountUserSnapshot` read models in `Keep.Application.Abstractions`. EF Core stays entirely out of `Keep.Application`. Bounded retry (MaxAttempts=5) in the service covers both pre-check and commit-time unique constraint collisions; `PublicIntakeCommitResult` enum makes the retry contract explicit; Infrastructure owns EF entity state cleanup on collision | Implemented | build-log/013 |
| ADR-056 | _(Keep-012)_ All public intake gate failures (token blank, link not found/revoked, account snapshot missing, account blocked/readonly, feature off) return the **same generic `keep.public_intake.unavailable` error** — no gate-specific error codes on the public surface. Prevents callers from probing which gate fired (information hiding). Extends the public-guard posture of ADR-011 to the Keep intake service layer | Implemented | 7B Session A |
| ADR-057 | _(Keep-013)_ `KeepRequestStatus` serializes to lowercase snake_case string slugs in all API responses: `Received→"received"`, `InProgress→"in_progress"`, `PendingCustomer→"pending_customer"`, `Resolved→"resolved"`, `Closed→"closed"`, `Cancelled→"cancelled"`. These slugs are an **API contract** — changing them is a breaking change for API consumers. Mapping lives in `GetKeepRequestListService.MapStatus` with an exhaustive switch and a `default: throw` to catch future enum additions | Implemented | 7B Session A |
| ADR-058 | _(Keep-014)_ Phase 7B Session B uses interim **service-level auth** only: production registers `ICurrentUser` as `AnonymousCurrentUser` (unauthenticated, empty IDs), and `GET /keep/requests` calls `GetKeepRequestListService` without ASP.NET `RequireAuthorization()` until real Phase 5 auth exists. HTTP tests override `ICurrentUser` through `WebApplicationFactory`; no fake header auth or framework auth scheme becomes a temporary contract | Superseded | build-log/014; superseded by ADR-061..064 |
| ADR-059 | _(Keep-015)_ Phase 7B HTTP contract: public intake accepts legacy-compatible body fields including `emailNotificationsEnabled` but ignores that opt-in until notifications; success returns `201` with `{ requestId, referenceCode, pageToken }`; public validation errors map to `400`, public gate failures map to `422 keep.public_intake.unavailable`, operator unauthorized maps to `401`, operator forbidden maps to `403`. Public alias `/continuity/public-intake/token/{publicIntakeToken}` remains; no operator Continuity alias | Superseded in part by ADR-298 | build-log/014; build-log/047 |
| ADR-060 | _(Keep-016)_ Rate limiting on public intake uses a **per-IP fixed-window policy** (policy name `"public-intake"`): 10 requests per 1-minute window, 0 queue, 429 rejection. Fixed window chosen over sliding window — boundary-burst risk (20 req in ~2 s at window edge) is acceptable for a form endpoint where ADR-056 already blocks information leakage. IP resolved from `CF-Connecting-IP` (Cloudflare; cannot be forged when Railway ingress is Cloudflare-only — **deploy-time constraint, not code-enforced**) → `X-Forwarded-For` first segment → `RemoteIpAddress` (local dev fallback). Applied to `POST /keep/public-intake/token/{token}` and the `/continuity/` alias. Operator endpoints are authenticated; no per-IP limiter on `GET /keep/requests`. Ported from reference `Continuity.API` `"public-intake"` policy | Implemented | reference Continuity.API/Program.cs; build-log/058 (G8a: trusted-proxy CIDR config, `ClientIpResolver`, spoofed-header rejection, 429/partition/spoof-resistance integration proofs) |

| ADR-061 | _(Phase5A-001)_ `AccountSession` is a **standalone entity** (does not extend `BaseEntity`): no soft-delete, no generic audit timestamps. Carries `ClientType` (`SessionClientType`: Browser/MobileApp/Admin), optional `DeviceName`, `CreatedAtUtc`, `ExpiresAtUtc`, `LastActivityAtUtc`, `LastSeenAtUtc` (both updated together on activity renewal), `RevokedAtUtc` (nullable, idempotent revoke). Not mapped via `BaseEntityConfiguration`; unique index on `SessionTokenHash`; `IsRevoked` is a derived property ignored by EF | Implemented | build-log/016 |
| ADR-062 | _(Phase5A-002)_ One opaque session token model for browser **and** mobile. `Authorization: Bearer <token>` resolved **first** (mobile priority); `ophalo.sid` cookie fallback (browser). One `SessionAuthenticationHandler` scheme, no second scheme. Session creation issues a cryptographically random 32-byte Base64 token; the raw token is **never stored** — only the SHA-256 lowercase hex hash (`SessionHasher`) is persisted | Implemented | build-log/016 |
| ADR-063 | _(Phase5A-003)_ Session handler **fails closed** unless `AccountUserMembershipStatus == Active`. `SessionStore.FindByTokenHash` verifies the AccountUser exists AND `AccountUser.AccountId == session.AccountId` (integrity cross-check against corrupt rows); any mismatch returns null. Handler also returns `NoResult` (unauthenticated) for: missing session, revoked, expired, inactivity window exceeded, or non-Active membership. `Invited`, `Suspended`, and `Removed` all fail the Active gate. Original 7-day inactivity pilot value superseded by ADR-378 | Implemented | build-log/016; ADR-378 |
| ADR-064 | _(Phase5A-004)_ `ErrorHttpMapper` (in `OpHalo.Api.Helpers`) replaces the inline `ToProblem`/`Problem` helper functions. Pattern-based switch: explicit Foundation/Keep error codes first (`auth.unauthorized`, `auth.forbidden`, `keep.public_intake.unavailable`), then `.EndsWith`/`.Contains` patterns for 400/401/402/403/404/409/422/503 families; unknown codes fall to 400. Returns RFC 7807 ProblemDetails with `extensions["code"]`. Ported and adapted from reference `ErrorHttpMapper` | Implemented | build-log/016 |

---

| ADR-065 | _(Phase5C-001)_ `EntryContext.NewAccount = 1` added (value matches reference to prevent gap confusion). `AccountAuthCode.Create` rejects `NewAccount` at call site — use `CreateForNewAccount` which requires `businessName`, `name?`, and `timeZone` snapshots. Snapshot properties are nullable on the entity so EF can map ExistingMember codes without dummy values | Implemented | build-log/020 |
| ADR-066 | _(Phase5C-002)_ Config-backed `SignupDefaults` (`IsPilot`, `TrialDurationDays`, `MaxPilotAccounts?`) drives all new-account provisioning parameters. No plan/pilot values hard-coded in any service. Later public signup switches to `IsPilot=false`, `TrialDurationDays=14` by config only | Implemented | build-log/020 |
| ADR-067 | _(Phase5C-003)_ `ClassifyStartRequestAsync` implements three-query classification: (1) active AccountUsers Take(2) — exactly one active → ExistingMember, two+ → Neutral; (2) any AccountUser (any status) → Neutral; (3) any User row → Neutral; else → NewAccount. No enumeration leak through the API | Implemented | build-log/020 |
| ADR-068 | _(Phase5C-004)_ `CommitStartCodeAsync` invalidates prior codes context-specifically: ExistingMember by `TargetAccountUserId`; NewAccount by `DeliveryEmailSnapshot + EntryContext`. Composite index `(delivery_email_snapshot, entry_context)` supports the NewAccount invalidation query | Implemented | build-log/020 |
| ADR-069 | _(Phase5C-005)_ `CommitNewAccountExchangeAsync` is **fully atomic**: ExecuteUpdateAsync consume (race guard) + two-phase graph save (ADR-044 pattern) in one transaction. Failed graph save rolls back the consume — code remains unconsumed. Unique constraint violation `23505` → `AccountErrors.EmailAlreadyInUse` (not a 500) | Implemented | build-log/020 |
| ADR-070 | _(Phase5C-006)_ Pilot cap checked at both `/auth/start` (before code issuance) and `/auth/exchange` (before consuming code). A pilot-full rejection at exchange leaves the code unconsumed. Count is `AccountEntitlements.IsPilot = true` (conservative: cancelled accounts not excluded in v1) | Implemented | build-log/020 |
| ADR-071 | _(Phase5C-007)_ `StartAuthService` reuses `MagicLinkEmailTemplate.BuildHtmlBody` for both sign-in and registration links. `NewAccountSubject` is the only email distinction between existing-member and new-account flows — keeps the template simple until branded registration email is designed | Implemented | build-log/020 |
| ADR-072 | _(Phase5C-008)_ `Account.PilotFull` is mapped explicitly to 409 Conflict in `ErrorHttpMapper` (same style as `Account.Expired` → 402). `Account.EmailAlreadyInUse` already mapped to 409 via the `EndsWith(".EmailAlreadyInUse")` pattern — no explicit entry needed | Implemented | build-log/020 |
| ADR-073 | _(Phase5C-009)_ IANA time zone validation at the endpoint via `TimeZoneInfo.TryFindSystemTimeZoneById` (.NET 10, cross-platform). Rejects blank and unrecognised values with `Validation.TimeZoneInvalid` before the service is called. `Account.CreateVerified` trusts the upstream validation (ADR-022) | Implemented | build-log/020 |

| ADR-074 | _(Phase5D-001)_ `EntryContext.InvitedUser = 3` added for reference parity. Not wired to `/auth/exchange` — invite acceptance uses a dedicated direct-token flow (`POST /accounts/invite/accept`), not the magic-link exchange path | Implemented | build-log/021 |
| ADR-075 | _(Phase5D-002)_ Invite send: any non-Owner role (Admin, Operator, Viewer) may be invited; Owner is reserved for `CreateOwner`. Caller gate: `UserAccessPolicy.IsPermitted(role, status, purpose, PermissionKeys.Account.MembersManage)` — only Active Owner/Admin pass. `OperatorBaseUrl` added to `MagicLinkSettings` (bound from `"App"` config section); invite link: `{OperatorBaseUrl}/invite/accept?token=...`. Resend of an existing Invited row rotates the token in place (old token immediately invalid) and skips the seat-limit check (seat already reserved) | Implemented | build-log/021 |
| ADR-076 | _(Phase5D-003)_ Invite accept: `POST /accounts/invite/accept` direct raw-token endpoint; `InviteTokenGenerator` (uppercase SHA-256 hex via `Convert.ToHexString`, mirroring reference). Activation is atomic via `ExecuteUpdateAsync` conditioned on `MembershipStatus == Invited` — concurrent accept race loser gets 0 rows updated → `InviteErrors.InvalidToken`. User find-or-create wrapped in a PostgreSQL savepoint to handle concurrent User creation race. Browser-only this phase (`clientType = Browser`, `deviceName = null`). Session creation outside the transaction — failure returns 503, `AccountUser` stays Active | Implemented | build-log/021 |
| ADR-077 | _(Phase5D-004)_ `IInvitePersistence` is a separate persistence seam from `IAuthCodePersistence` — invite storage (AccountUser invite columns) is unrelated to auth code storage. Seat limit checked via `IFeatureAccessPolicy.ResolveLimit(entitlements, FeatureLimitKeys.Account.UserLimit)` at send time; counts Active + Invited + Suspended, excludes Removed. `Invite.SeatLimitReached` mapped explicitly to 409 in `ErrorHttpMapper` (no suffix pattern covers it); remaining `Invite.*` errors fall through existing patterns (.Forbidden→403, .InvalidToken→422, .Expired→422, .AlreadyActive→409) | Implemented | build-log/021 |

| ADR-078 | _(Phase5E-001)_ Member-management API/domain language is **members** (`/accounts/me/members`), not team. Product UI may say "Team" later, but backend contracts use membership naming. All routes derive `AccountId` from the trusted session; callers never supply account id | Implemented | build-log/022 |
| ADR-079 | _(Phase5E-002)_ Member-management caller gate is `UserAccessPolicy.IsPermitted(role, status, purpose, PermissionKeys.Account.MembersManage)`: Active Owner/Admin pass. Owner can manage Admin/Operator/Viewer and the second Owner subject to owner safety; Admin can manage Admin/Operator/Viewer; Admin cannot manage Owners; Operator/Viewer cannot manage members | Implemented | build-log/022 |
| ADR-080 | _(Phase5E-003)_ Owner safety: maximum 2 Owner-role memberships per account; at least 1 Active Owner must remain; only Owners can promote/demote Owner role. `Account.PrimaryOwnerAccountUserId` is protected in Phase 5E — primary owner cannot be demoted, suspended, or removed. Primary-owner transfer is a later explicit flow | Implemented | build-log/022 |
| ADR-081 | _(Phase5E-004)_ Member transition semantics: Invited and Suspended members can have role changed; Removed members cannot. Removal uses `MembershipStatus.Removed`, not EF soft-delete. Invited removal cancels the invite by clearing token/expiry. Removed with `UserId` reactivates via `reactivate`; Removed with no `UserId` restores via `resend-invite`; new invite to Removed email returns a previously-removed conflict with suggested action | Implemented | build-log/022 |
| ADR-082 | _(Phase5E-005)_ Session revocation policy for member management: suspend/remove revoke all durable sessions for the target `AccountUser` immediately. Role change does not revoke sessions because session auth reloads role/status from persistence on subsequent requests. Reactivation does not create a session | Implemented | build-log/022 |
| ADR-083 | _(Phase5E-006)_ Off-season subscription posture is deferred and must be account/commercial-level, not `MembershipStatus`: owners-only read-only access, Admin/Operator/Viewer blocked, no new public submissions/outbound notifications. Do not implement off-season in Phase 5E | Locked | build-log/022 |

| ADR-084 | _(Phase8-001)_ Customer request pages use anonymous, request-scoped `KeepRequest.PageToken` access under `/keep/r/{pageToken}`. The token is the only access key; APIs never accept client-supplied `AccountId`. Customer page responses must include `businessName` as a trust signal. Public-slug branded URLs are deferred; if added later, slug is branding only and token remains the security primitive | Locked | build-log/024 |
| ADR-085 | _(Phase8-002)_ Phase 8 route shape uses Keep naming and explicit access boundaries: authenticated operator routes under `/keep/requests...`, anonymous customer routes under `/keep/r/{pageToken}...`. Operator routes derive account from session; customer routes derive account/request from token. SSE list streaming, external-contact/revert, and customer contact-preference/opt-out routes are deferred | Locked | build-log/024 |
| ADR-086 | _(Phase8-003)_ Keep timeline is append-only `KeepRequestEvent` records. Event type describes what happened; actor fields describe who did it (`ActorType`, optional `ActorAccountUserId`, actor display snapshot). `KeepRequest` stores current-state fields for fast reads; events store timeline/audit facts. Customer timeline shows `All` events; operator detail can include `All`, `Internal`, and useful `System` events | Locked | build-log/024 |
| ADR-087 | _(Phase8-004)_ Keep uses lightweight request participation for filtering and notification routing, not heavyweight ticket tracking. `KeepRequestParticipant` records Responsible/Watching users plus notification-enabled state. Owner/Admin can see all; Operators see requests they are responsible for or watching; unassigned public intake requests remain visible to Owner/Admin for routing. Deep notification preferences/quiet hours remain Phase 9 | Locked | build-log/024 |
| ADR-088 | _(Phase8-005)_ Customer/business communication persists as `KeepRequestEvent` with `EventType=MessageAdded` plus `MessageIntent`, actor fields, content, and `Visibility=All`. Pilot customer intents: GeneralMessage, Question, UpdateRequest, ScheduleChangeRequest, ChangeOrCancelRequest, Complaint; operator/business updates use BusinessUpdate. Customer actions do not automatically transition status in pilot, including change/cancel intent; business/operator decides status changes | Locked | build-log/024 |
| ADR-089 | _(Phase8-006)_ Keep request lifecycle uses existing statuses: Received, InProgress, PendingCustomer, Resolved, Closed, Cancelled. Only authenticated business users change status in pilot. `/status` handles non-terminal transitions; `/close` and `/cancel` are explicit terminal commands with optional final customer-visible message. Closed/cancelled pages remain readable until expiry, block normal customer actions, may accept one lightweight resolution feedback submission, and expired links return 410 with safe context plus optional new-request URL. Reopen is deferred | Locked | build-log/024 |
| ADR-090 | _(Phase8-007)_ First response models business communication reality: account-configurable response SLA, request origin matters (`Customer` vs `Business`), customer-created requests start waiting at creation, and first response is customer-visible business contact or explicitly logged external customer contact. Internal notes, assignments, participation changes, acknowledgement, non-visible status changes, system events, and customer-originated messages do not count. Event/model design must support later mobile capture of phone/SMS/email/in-person contact | Locked | build-log/024 |
| ADR-091 | _(Phase8-008)_ Keep attention state for pilot is stored directly on `KeepRequest`, with events for history/audit; full signal/projection engine is deferred. Attention tracks level, waiting direction, reason, priority band, since/next timestamps, and acknowledgement audit fields. Customer waiting keeps the oldest unresolved `AttentionSinceUtc`; higher-priority later intents can upgrade priority without resetting the clock. Queue sorting is deterministic: Overdue, NeedsAttention, Waiting, None; Priority before Standard; oldest unresolved first | Locked | build-log/024 |
| ADR-092 | _(Phase8-009)_ Phase 8 prepares notification-routing state but does not implement notification delivery. Store participants, notification-enabled intent, attention state, message intent, priority, and first-response fields; document routing/fallback/actor-exclusion rules. Superseded for notification timing: basic APNs/FCM push, device registration, deep links, and server-derived badges are V1 pre-go-live (ADR-289); SMS, quiet hours, preferences, retries/outbox/dead-letter, delivery analytics, and work-engine integration remain deferred | Superseded | build-log/024; superseded by ADR-289 |
| ADR-093 | _(Phase8-010)_ Phase 8 implementation is sliced. B1 is data model + read surfaces only: extend Keep request/event model, add response policy and participants, EF migration/configuration, operator detail `GET /keep/requests/{requestId}`, customer page `GET /keep/r/{pageToken}`, expired-link 410 context, and read/access integration tests. B2 operator writes, B3 customer writes, B4 participation filtering/routing surface. Do not hand off all of Phase 8 as one coding task | Locked | build-log/024 |

| ADR-094 | _(Phase8B1-001)_ `KeepRequestEventType` uses action-oriented vocabulary with explicit int values. `OperatorReplied` (3) and `CustomerReplied` (4) removed — actor is now `ActorType`, not event type. Final values: `RequestCreated=1`, `StatusChanged=2`, `MessageAdded=3`, `RequestClosed=5`, `RequestCancelled=6`, `InternalNoteAdded=7`, `AttentionAcknowledged=8`. Gap at 4 is intentional; removed values are never reused | Implemented | B1-α session |
| ADR-095 | _(Phase8B1-002)_ `KeepRequest.Create()` adds `origin` as an optional parameter defaulting to `KeepRequestOrigin.Customer` for B1-α. Public intake is always Customer; existing callers require no changes. Operator-created flow makes origin explicit in a later phase | Implemented | B1-α session |
| ADR-096 | _(Phase8B1-003)_ `KeepRequest.ClosedAtUtc` renamed to `TerminatedAtUtc`. Covers both terminal states (`Closed` and `Cancelled`). `Status` tells which terminal state; `TerminatedAtUtc` tells when it became terminal | Implemented | B1-α session |
| ADR-097 | _(Phase8B1-004)_ `KeepResponsePolicy` inherits `BaseEntity` (surrogate `Guid Id`) for consistency with all other Keep entities. Unique index on `AccountId` enforces one policy per account | Implemented | B1-α session |
| ADR-098 | _(Phase8B1-005)_ `KeepRequest.Create()` initializes attention to `AttentionLevel.None` / `WaitingDirection.None` for B1-α. B2 wires business-waiting + first-response-due behavior in the intake and operator write paths | Implemented | B1-α session |

| ADR-099 | _(Phase8B1-006)_ Expired customer page is modeled as a valid terminal read result (`IsExpired = true` on `KeepCustomerPageResult`), not an application error. The service returns `Result.Success` with only `BusinessName`, `ReferenceCode`, `IsExpired`, and `NewRequestUrl` populated; the endpoint maps this to HTTP 410. No `KeepRequestErrors.Expired` error needed; no `ErrorHttpMapper` change needed | Implemented | B1-β session |
| ADR-100 | _(Phase8B1-007)_ `IKeepRequestDetailPersistence` is a separate persistence contract from `IKeepRequestListPersistence`. Snapshot methods (`GetAccountUserSnapshotAsync`, `GetAccountAccessSnapshotAsync`) are intentionally duplicated — no shared base until a third service requires them. `GetParticipantsAsync` returns `KeepParticipantProjection` (joined with `AccountUser` for display name + role) rather than raw `KeepRequestParticipant` entities, keeping EF out of the application layer | Implemented | B1-β session |
| ADR-101 | _(Phase8B1-008)_ Customer page service applies a defensive `Visibility == All` filter in the service layer even though persistence already scopes to `Visibility = All`. A public endpoint must not rely solely on the persistence contract to prevent internal-event leakage; belt-and-suspenders is warranted here | Implemented | B1-β session |

| ADR-102 | _(Phase8B2-001)_ `KeepRequestStatus.Scheduled = 7` added. Explicit value prevents reorder drift. A scheduled request is a non-terminal active state; allowed transitions follow the same table as Received/InProgress/PendingCustomer | Implemented | B2-alpha session |
| ADR-103 | _(Phase8B2-002)_ `KeepRequestEvent.StatusAfter` (nullable `KeepRequestStatus`) is recorded on every `StatusChanged` event. The current `KeepRequest.Status` is the live value and cannot reconstruct the status at each past event; timeline labels and history rendering require the snapshot at event time | Implemented | B2-alpha session |
| ADR-104 | _(Phase8B2-003)_ `KeepStatusChangeOutcome` is a non-null wrapper for `KeepRequest.ChangeStatus` result. `Result<T>.Success` has an `ArgumentNullException.ThrowIfNull` guard that fires regardless of T nullability; returning null through `Result<T>` is not supported. The wrapper makes the no-op case explicit (`IsNoOp`, `StatusChangedEvent?`) and type-safe without changing `Result<T>` | Implemented | B2-alpha session |
| ADR-105 | _(Phase8B2-004)_ `PermissionKeys.Keep.RequestsOperate = "keep.requests.operate"` is the unified gate for all B2 operator write actions (status change, business updates, internal notes, attention acknowledge). Added to `OperatorBase` (propagates to Admin/Owner); Viewer does not receive it | Implemented | B2-alpha session |
| ADR-106 | _(Phase8B2-005)_ `ChangeKeepRequestStatusService` holds both `IKeepRequestOperatePersistence` (write-side: tracked load, commit, actor/snapshot queries) and `IKeepRequestDetailPersistence` (read-side: post-commit event/participant/business-name load). Avoids re-running the auth stack after commit; avoids duplicating read methods on the write interface. Snapshot methods are duplicated across the two persistence interfaces until a fourth service repeats the pattern | Implemented | B2-alpha session |
| ADR-107 | _(Phase8B2-006)_ `KeepRequest.CurrentStatusText` is preserved on silent status changes (no message supplied). Only overwritten when `trimmedMessage is not null`. Erasing the last customer-facing status text on an internal reclassification is a silent UX regression | Implemented | B2-alpha session |

| ADR-108 | _(Phase8B2-007)_ Separate error codes per character limit: `ChangeStatus` messages 2000 chars → `MessageTooLong`; business update messages 4000 chars → `BusinessUpdateMessageTooLong`; internal notes 4000 chars → `NoteTooLong`. `AddBusinessUpdateWithStatus` is a separate domain method (not routed through `ChangeStatus`) so the 4000-char limit can apply without overloading `ChangeStatus`'s 2000-char constraint | Implemented | build-log/029 |
| ADR-109 | _(Phase8B2-008)_ First response wired in both `ChangeStatus` (when message non-null) and `AddBusinessUpdate`/`AddBusinessUpdateWithStatus`. B2-alpha's `ChangeStatus` missed this; B2-beta corrects the gap. Internal notes do NOT set first response — they are not customer-visible | Implemented | build-log/029 |
| ADR-110 | _(Phase8B2-009)_ `KeepRequestDetailMapper` extracted as `internal static class` in `Keep.Application`. Eliminates full mapper duplication across `GetKeepRequestDetailService` and `ChangeKeepRequestStatusService` before a third write service (`AddBusinessUpdateService`) would require it again | Implemented | build-log/029 |

| ADR-111 | _(Phase8B2-010)_ `CanAcknowledgeAttention` is derived from operator write permission plus active request attention: `hasOperatePermission && request.AttentionLevel != AttentionLevel.None`. Viewer/read-only users can view request detail but cannot acknowledge attention, and requests with no active attention do not show the action. Terminal status is not part of the formula so legacy/bad terminal data with active attention can still be cleaned up | Implemented | build-log/030 |
| ADR-112 | _(Phase8B2-011)_ Acknowledge attention is an internal operator action. It requires a non-blank reason capped at 500 characters to prevent accidental dismissal and preserve audit intent. It clears request-level attention fields, records `AttentionClearedAtUtc`, `AttentionClearedByAccountUserId`, and `AttentionClearReason`, creates one internal `AttentionAcknowledged` event, does not notify the customer, does not appear on the customer timeline, does not update `LastBusinessActivityAt`, and does not count as first response | Implemented | build-log/030 |
| ADR-113 | _(Phase8B2-012)_ Customer-visible business responses clear business-waiting attention; silent status changes and internal notes do not. Clearing attention through a business response records who cleared it and when, but leaves `AttentionClearReason` null because the visible business message itself is the explanatory action. Explicitly logged external customer contact will also clear business-waiting attention when applicable, but the capture workflow is a separate pre-go-live slice — **gamma portion implemented**; external-contact clearing remains Locked | Implemented (gamma); Locked (external-contact) | build-log/030; ADR-115 |
| ADR-114 | _(Phase8B2-013)_ Gamma error contract: anonymous → 401, no operate permission → 403, unknown/cross-account request → 404, `KeepRequest.AttentionReasonRequired` → 400, `KeepRequest.AttentionReasonTooLong` → 400, and `KeepRequest.AttentionNotRaised` → 409. ProblemDetails extension values continue to be asserted at the JSON root, including `code` | Implemented | build-log/030 |
| ADR-115 | _(Phase8B2-014)_ External contact logs are internal-only by default. Logged phone/SMS/email/in-person/other customer contact may count as customer contact, clear business-waiting attention, and count as first response when applicable, but it must not automatically appear on the customer timeline, notify the customer, or populate `AttentionClearReason`. A future explicit `Show on customer timeline` receipt/recap option can be designed separately with customer-safe copy and audit | Locked / deferred implementation | Pre-go-live external-contact slice |
| ADR-116 | _(Phase8B2-015)_ Terminal transitions to `Closed` or `Cancelled` are lifecycle analytics boundaries. They must set `TerminatedAtUtc = now` so request duration can be derived from `CreatedAtUtc → TerminatedAtUtc`, and they must auto-clear active attention as terminal cleanup without requiring an acknowledgement reason, without creating `AttentionAcknowledged`, and with `AttentionClearReason = null`. The terminal status event is the audit anchor; deeper auto-clear audit can be added later via a distinct event/kind if needed | Implemented | build-log/031 |
| ADR-117 | _(Phase8B2-016)_ B2 terminal/attention work is split into two sessions: B2-gamma completes core attention acknowledgement and business-waiting attention clearing; B2-delta handles terminal lifecycle and analytics primitives (`TerminatedAtUtc`, terminal auto-clear, terminal fallback acknowledge coverage). This keeps the acknowledge endpoint focused while giving paid-insight reporting fields the care they need | Implemented | build-log/031 |

| ADR-118 | _(Phase8B3-001)_ Customer request page links under `/keep/r/{pageToken}` are bearer-access links for the pilot. Possession of a valid, unexpired page token grants access to the customer-visible request page and allowed customer actions. This intentionally supports real-world sharing with spouses, tenants, property managers, coworkers, and family members. Security relies on high-entropy tokens, token-only server-side resolution, minimal public data exposure, account/feature/expiry guards, terminal write blocking, and anonymous-write rate limiting. Identity-bound customer access, passcodes, per-recipient access, and link revocation/rotation UI are deferred | Locked | build-log/032 |
| ADR-119 | _(Phase8B3-002)_ Customer page reads and writes under `/keep/r/{pageToken}` must pass through a centralized Keep public customer access guard. The guard resolves request/account context exclusively from the page token, enforces account access and Keep feature gates, handles expiry consistently, and returns a safe projection rather than tracked EF entities. Write services may mutate only after guard success by re-fetching the tracked request using the server-resolved request id | Locked | build-log/032 |
| ADR-120 | _(Phase8B3-003)_ Customer page expiry is enforced only after terminal lifecycle. Active and resolved requests remain readable even if `ExpiresAtUtc` is populated. Closed/cancelled requests remain readable until `ExpiresAtUtc`; after that, customer page reads and writes return `410 Gone` with safe expired-page context. This avoids the reference-app bug where open requests could become inaccessible due to stale expiry timestamps | Locked | build-log/032 |
| ADR-121 | _(Phase8B3-004)_ Normal customer reply routes are intent-specific but share one implementation path: `/message`, `/question`, `/update-request`, `/schedule-change-request`, `/change-or-cancel-request`, and `/issue`. Each accepts `{ message }`, maps to a fixed `MessageIntent` (`/issue` stores `Complaint`), and returns the updated customer page result on success. Feedback, attachments, contact preferences/opt-out, appointment confirmation, quote/payment/approval flows, and direct customer status/cancel/close/reopen actions remain outside normal reply routes | Locked | build-log/032 |
| ADR-122 | _(Phase8B3-005)_ Customer reply validation uses one required plain-text `message`, trimmed before validation/storage, non-blank, max 4000 characters. Blank/missing messages return `400 KeepRequest.MessageRequired`; over-limit messages return `400 KeepRequest.CustomerMessageTooLong`. The reference app's 500-character limit is not carried forward because real customer explanations, access instructions, scheduling constraints, and issue reports need more room | Locked | build-log/032 |
| ADR-123 | _(Phase8B3-006)_ Customer replies append `KeepRequestEvent` records with `EventType=MessageAdded`, `Visibility=All`, `ActorType=Customer`, `ActorAccountUserId=null`, `ActorDisplayName=request.CustomerName`, `Content=trimmed message`, mapped `MessageIntent`, `CommunicationChannel=InApp`, and `StatusAfter=null`. Business quick-contact actions come from request-level denormalized `CustomerName`, `CustomerPhone`, and optional `CustomerEmail`; reply events do not duplicate phone/email. Operator/mobile clients can render one-click call/email actions from the request contact card. Contact preference editing, alternate contacts, opt-out, notification preference rules, and external contact logging remain deferred | Locked | build-log/032 |
| ADR-124 | _(Phase8B3-007)_ Keep response timing is account policy, not a universal constant. `KeepResponsePolicy` owns first-response, standard-response, priority-response, and future business-hours behavior. B3 customer-reply attention uses the account policy when present and falls back to pilot defaults: first response 60 minutes, standard reply 240 minutes, priority reply 60 minutes. `BusinessHoursOnly` remains stored but B3 does not implement full quiet-hours/business-hours calculation. Onboarding/account settings UI for these values is deferred unless explicitly pulled into scope | Locked | build-log/032 |
| ADR-125 | _(Phase8B3-008)_ Customer-submitted messages from the customer page raise request-level business-waiting attention. Normal customer routes (`/message`, `/question`, `/update-request`, `/schedule-change-request`, `/change-or-cancel-request`, `/issue`) set `AttentionLevel=Waiting`, `WaitingDirection=Business`, `LastCustomerActivityAt=now`, and `NextAttentionAtUtc` from account response policy or pilot defaults. Intent maps to `AttentionReason`/`PriorityBand`: message/question → CustomerMessage/Standard; update-request → UpdateRequest/Standard; schedule-change → ScheduleChangeRequest/Priority; change-or-cancel → ChangeOrCancelRequest/Priority; issue → Complaint/Priority. New business-waiting attention sets `AttentionSinceUtc=now`; additional customer messages while already business-waiting preserve the oldest unresolved `AttentionSinceUtc`, though higher-priority messages may upgrade reason/priority. A message while waiting on the customer flips direction to business and sets `AttentionSinceUtc=now`. Customer-submitted messages do not change status or count as first business response | Locked | build-log/032 |
| ADR-126 | _(Phase8B3-009)_ `KeepCustomerPageResult.AllowedActions` is server-computed by request state. `Received`, `Scheduled`, `InProgress`, `PendingCustomer`, and `Resolved` expose normal customer message actions (`message`, `question`, `update_request`, `schedule_change_request`, `change_or_cancel_request`, `issue`). `Closed` and `Cancelled` expose no B3 actions; expired terminal pages return safe tombstone context with `AllowedActions = null`. Server validation remains authoritative; `AllowedActions` is a UI affordance, not the security boundary. Closed-request feedback is deferred to the next customer-write follow-up slice and can add `feedback` later | Locked | build-log/032 |
| ADR-127 | _(Phase8B3-010)_ Terminal customer write behavior is explicit: `Resolved` accepts normal customer-submitted messages; `Closed` and `Cancelled` block normal customer-submitted messages with `409 KeepRequest.TerminalState`; expired terminal tokens return `410 Gone` with safe expired-page context; unknown tokens return `404`. Customer writes never reopen a request automatically, and `ChangeOrCancelRequest` records customer intent only — it does not transition status to `Cancelled`. Lifecycle control stays with the business and avoids the reference app's ambiguous customer terminal action path | Locked | build-log/032 |
| ADR-128 | _(Phase8B3-011)_ Closed-request feedback behavior is locked as core Keep follow-up direction but deferred out of B3 implementation. Future feedback route: `POST /keep/r/{pageToken}/feedback`, allowed only on unexpired `Closed` requests, one submission only, `wasResolved` required, optional plain-text `comment` capped at 2000 characters, storing `FeedbackWasResolved`, `FeedbackComment`, and `FeedbackSubmittedAtUtc`. Positive feedback creates no attention; negative feedback raises priority `UnresolvedFeedback` business attention without reopening automatically. No new feedback timeline event type is required for the first feedback slice unless the implementation decision changes | Locked / deferred implementation | build-log/032; deferred-topics |
| ADR-129 | _(Phase8B3-012)_ Anonymous customer writes use a dedicated `customer-write` rate limit policy applied to normal customer message routes (and future feedback). Partition by composite `client IP + pageToken`, limit to 10 writes per 1 minute, `QueueLimit = 0`, and keep the existing `Testing` environment rate-limiter bypass. This adds spam/brute-force friction for bearer-access links without over-penalizing shared networks or relying only on token partitioning | Implemented | build-log/032; build-log/058 (G8a: partition and spoof-resistance proofs cover the composite IP+token key) |
| ADR-130 | _(Phase8B3-013)_ B3 customer-write error contract: blank/unknown page token → `404 KeepRequest.NotFound`; account blocked or feature unavailable in the public guard → `404 KeepRequest.NotFound` to avoid public account-state leaks; expired terminal token → `410 Gone` safe expired-page context; missing/blank customer message → `400 KeepRequest.MessageRequired`; customer message over 4000 chars → `400 KeepRequest.CustomerMessageTooLong`; normal message on `Closed`/`Cancelled` → `409 KeepRequest.TerminalState`; rate limited anonymous writes → `429`. Feedback-specific errors are deferred with the feedback slice | Implemented | build-log/032, 034 |
| ADR-131 | _(Phase8B3-014)_ B3 completion requires focused integration coverage for customer-submitted messages: unknown token 404; blocked/unavailable account guard 404 with no mutation; expired terminal token 410 safe context; blank message 400; over-limit message 400; happy path message returns updated customer page with customer timeline event; `/issue` creates priority Complaint attention; repeated standard message preserves oldest `AttentionSinceUtc`; later priority message upgrades reason/priority without resetting since; message while waiting on customer flips direction and resets since to now; `Resolved` accepts messages; `Closed`/`Cancelled` reject messages; customer page response exposes no internal IDs/attention internals; `AllowedActions` matches request state; scheduled customer page maps `scheduled` correctly | Implemented | build-log/032, 034 |
| ADR-132 | _(Phase8B3-015)_ Phase 8-B3 implementation scope is customer-submitted messages only: public customer access guard, expiry semantics, `AllowedActions`, scheduled status mapper fix, customer message domain/service/persistence/API routes, attention update rules, error mappings, `customer-write` rate limit policy, and integration tests. Closed-request feedback moves to the next customer-write follow-up slice. This keeps the core customer communication loop focused before adding post-close behavior | Implemented | build-log/032, 034 |
| ADR-133 | _(Phase8B3-016)_ Final reference-app lessons for B3: adopt centralized public customer-token guard, guard-before-mutation/no-side-effects-on-denied-access, tracked re-fetch by server-resolved request id, customer message clears/flips waiting-on-customer, anonymous customer route rate limiting, terminal-only expiry, and no internal data exposure. Modify reference behavior by using intent-specific routes with one shared implementation, `/issue` instead of public `/complaint`, 4000-char messages instead of 500, direct `KeepRequest` attention fields instead of the signal projection engine, and updated customer page response instead of `204`. Do not port customer direct no-longer-needed terminal action, contact preferences/opt-out, notification/email delivery, attachments/photos, page-opened beacon, full signal/projection attention engine, or customer direct status/cancel/close/reopen | Implemented | build-log/032, 034 |
| ADR-134 | _(Phase8B3-017)_ Closed-request feedback is deferred out of B3 but remains core Keep product direction, not an add-on. Basic "was this resolved?" feedback is part of closing the Keep communication loop. Advanced feedback analytics, CSAT/NPS/review generation, public review prompts, dashboards, and automated follow-up campaigns are deferred separately and may be packaged differently later | Implemented | build-log/032, 034, 035 |

| ADR-135 | _(Phase8B3+-001)_ Basic closed-request feedback is core Keep resolution feedback, not an add-on. Feedback is a binary resolution check (`wasResolved`) plus optional comment capped at 2000 characters. `wasResolved=false` is the actionable unresolved signal; no separate "needs improvement" flag is introduced. UI should strongly prompt for a comment when unresolved, but the server does not require one. Ratings, CSAT/NPS, reviews/testimonials, analytics dashboards, and automated feedback campaigns remain deferred | Implemented | build-log/033, 035 |
| ADR-136 | _(Phase8B3+-002)_ Feedback endpoint and eligibility: `POST /keep/r/{pageToken}/feedback` is allowed only for unexpired `Closed` requests that pass the public customer access guard and have not already received feedback. Feedback is not allowed on `Received`, `Scheduled`, `InProgress`, `PendingCustomer`, `Resolved`, or `Cancelled`. `Resolved` remains pre-close customer communication; `Cancelled` means not proceeding, not resolved; feedback is one-time to avoid repeated state changes | Implemented | build-log/033, 035 |
| ADR-137 | _(Phase8B3+-003)_ Feedback stores request-level fields, not timeline events, in the first feedback slice. Store trimmed optional comment (blank/missing → null), `FeedbackWasResolved`, and `FeedbackSubmittedAtUtc = now` on `KeepRequest`. Do not append `KeepRequestEvent` or add a feedback event type yet. Feedback is one-time and not editable, so request-level fields are sufficient for pilot rendering on customer/operator pages | Implemented | build-log/033, 035 |
| ADR-138 | _(Phase8B3+-004)_ Negative feedback raises priority business attention without reopening. `wasResolved=false` sets `AttentionLevel=Waiting`, `WaitingDirection=Business`, `AttentionReason=UnresolvedFeedback`, `PriorityBand=Priority`, `AttentionSinceUtc=now`, and `NextAttentionAtUtc` from the priority response target. Do not change status from `Closed`, do not update `TerminatedAtUtc`, and do not reopen automatically. `wasResolved=true` stores feedback only and does not mutate attention, including any legacy attention | Implemented | build-log/033, 035 |
| ADR-139 | _(Phase8B3+-005)_ Feedback returns the updated `KeepCustomerPageResult` and updates allowed actions. Eligible unexpired `Closed` pages expose `AllowedActions = [\"feedback\"]` before submission; successful feedback returns updated page data with feedback fields populated and `AllowedActions = []`. `Cancelled` pages expose `[]`; expired terminal pages expose safe tombstone context with `AllowedActions = null`. Returning updated page avoids a follow-up GET and immediately removes the one-time action | Implemented | build-log/033, 035 |
| ADR-140 | _(Phase8B3+-006)_ Feedback error contract: blank/unknown token → `404 KeepRequest.NotFound`; account blocked or feature unavailable → `404 KeepRequest.NotFound`; expired terminal token → `410 Gone` safe expired-page context; missing/invalid `wasResolved` → `400 KeepRequest.FeedbackResolutionRequired`; comment over 2000 chars → `400 KeepRequest.FeedbackCommentTooLong`; feedback on non-Closed or Cancelled request → `409 KeepRequest.FeedbackUnavailable`; duplicate feedback → `409 KeepRequest.FeedbackAlreadySubmitted`; rate-limited anonymous writes → `429` | Implemented | build-log/033, 035 |
| ADR-141 | _(Phase8B3+-007)_ Feedback uses the same `customer-write` rate limit policy as B3 customer messages: partition by composite `client IP + pageToken`, 10 writes per 1 minute, `QueueLimit = 0`, and keep the `Testing` environment bypass. Duplicate feedback returns `409`, not silent success. Feedback is an anonymous bearer-link write, so even invalid/duplicate/unknown-token attempts share the anonymous write abuse posture | Implemented | build-log/033, 035 |
| ADR-142 | _(Phase8B3+-008)_ Feedback implementation split and timing: implement as `Phase 8-B3+ — Closed-Request Feedback` immediately after B3 customer-submitted messages. Scope includes domain method, request body, service/persistence route, errors and HTTP mappings, closed-page `AllowedActions`, negative feedback attention, integration tests, and docs. Out of scope: feedback timeline events, editing/deletion, ratings/CSAT/NPS, public reviews, analytics dashboards, automated campaigns, and customer reopen | Implemented | build-log/033, 035 |
| ADR-143 | _(Phase8B3+-009)_ Feedback integration test matrix: closed unexpired page exposes `feedback`; feedback on closed request succeeds and returns updated page; positive feedback stores fields and creates no attention; negative feedback stores fields and creates priority `UnresolvedFeedback` attention; duplicate feedback returns 409; feedback on Received/Resolved/Cancelled returns 409; expired closed token returns 410; missing/invalid `wasResolved` returns 400; over-limit comment returns 400; response exposes no internal IDs or attention internals; after feedback `AllowedActions = []` | Implemented | build-log/033, 035 |
| ADR-144 | _(Phase8B3+-010)_ Feedback reference-app lessons: adopt public guard before mutation, no side effects on denied access, token-only request/account resolution, rate limiting, terminal-only expiry, and customer-safe public response. Do not port reference `no-longer-needed`, customer email opt-out, contact preference mutation, page-opened beacon, direct customer lifecycle mutation, or an event-heavy feedback model. Modify behavior so feedback is not customer cancellation; negative feedback raises attention without reopening; feedback is request-level fields and one-time only | Implemented | build-log/033, 035 |

| ADR-145 | _(Phase8B4-001)_ Request list is the triage and common-action surface; service request detail is the full-context workbench for careful action. Operators should not have to open detail for routine confident actions, while detail owns history, internal notes, participants, customer page link, feedback context, and careful state changes. Request movement indicators stay with request-list work | Locked | build-log/036 |
| ADR-146 | _(Phase8B4-002)_ Service request participant display names prefer linked `User.Name` when nonblank, with `AccountUser.Email` fallback. This applies to request participants only, does not rewrite denormalized event actor snapshots, and does not expose participant identity on the customer page | Locked | build-log/036 |
| ADR-147 | _(Phase8B4-003)_ `ParticipationType` remains intentionally small: `Responsible` and `Watching` only. Participation is intentional involvement and future notification routing; commenting, adding an internal note, or taking an action does not automatically make someone a participant | Locked | build-log/036 |
| ADR-148 | _(Phase8B4-004)_ Closed-request closeout review and archive are real lifecycle concepts but deferred and must not be modeled as `ParticipationType`. Future closeout/admin workflow should distinguish `Closed` customer work from internal review and archival state | Locked | build-log/036; deferred-topics |
| ADR-149 | _(Phase8B4-005)_ Service request detail exposes customer contact fields plus structured contact action metadata (`call` when phone exists, `email` when email exists). Contact action metadata is an operator affordance only; B4 does not log contact, clear attention, count first response, or send email/SMS/push | Locked | build-log/036; DEF-018 |
| ADR-150 | _(Phase8B4-006)_ `NewRequestUrl` remains `null` in B4 until a safe customer-facing intake URL contract exists. Do not construct new-request URLs from raw public-intake tokens, expose intake token/hash, or introduce branded slug URLs in this slice | Locked | build-log/036; DEF-016 |
| ADR-151 | _(Phase8B4-007)_ Service request detail exposes closed-request feedback state to authenticated request viewers, but `FeedbackComment` is Owner/Admin-only. Operators/Viewers may see `FeedbackWasResolved` and `FeedbackSubmittedAtUtc`; they receive no comment content and need metadata to distinguish redacted from absent comments | Locked | build-log/036 |
| ADR-152 | _(Phase8B4-008)_ Negative feedback creates post-close business attention without reopening. Detail must show `status=closed` together with `AttentionReason=unresolved_feedback`, `WaitingDirection=business`, and `PriorityBand=priority`; do not change `TerminatedAtUtc` or add a parallel feedback status | Locked | build-log/036 |
| ADR-153 | _(Phase8B4-009)_ Closed requests with active unresolved-feedback attention reappear in the default Owner/Admin request-list attention surface. Normal closed requests remain hidden unless closed filters are selected; these items are visually distinct post-close follow-up, not reopened work, and sort above non-attention active requests. Exact list ranking is deferred to request-list implementation | Locked | build-log/036 |
| ADR-154 | _(Phase8B4-010)_ Future Owner/Admin `Mark feedback reviewed` is the clear-from-default-list action for post-close unresolved feedback. It stores reviewer/timestamp when implemented, does not reopen, delete feedback, hide detail feedback, notify the customer, or count as resolving the issue; implementation is deferred out of B4 unless explicitly pulled in | Locked | build-log/036; deferred-topics |
| ADR-155 | _(Phase8B4-011)_ Service request detail and customer page have separate field boundaries. Operator detail may expose internal operational context; the public customer page must not expose IDs, page token in body, customer contact fields, contact actions, participants, internal/system events, attention/first-response internals, feedback comments, operator actions, or operator validation hints | Locked | build-log/036 |
| ADR-156 | _(Phase8B4-012)_ B4 service request detail enrichment requires role-aware authorization, redaction, mapping, and customer-boundary tests. Coverage must include 401/403/404 access cases, Owner/Admin-only feedback comment visibility, participant display fallbacks, contact action metadata, explicit unresolved-feedback attention mapping, and public customer page non-exposure of internal/operator fields | Locked | build-log/036 |
| ADR-157 | _(Phase8B4-013)_ B4 implements service request detail enrichment only. Request-list unresolved-feedback resurfacing, ranking, and future reviewed/clear behavior are locked product direction but are implemented in request-list/follow-up slices, not in B4 | Locked | build-log/036 |
| ADR-158 | _(Phase8B4-014)_ B4 explicitly defers adjacent workflow, list, notification, telemetry, and analytics work: request-list resurfacing/ranking, mark feedback reviewed, closeout/archive, assignment/watch writes, contributor chips, external contact logging, contact preferences, outbound notifications, new-request URLs, identity-bound customer access, feedback events/editing/analytics, attachments, full signal engine, business-hours SLA math, and page-visit tracking | Locked | build-log/036; deferred-topics |
| ADR-159 | _(Phase8B4-015)_ B4 is a no-migration read-model enrichment slice. `ContactActions` and `FeedbackCommentVisible` are response DTO fields only; participant names, contact metadata, feedback redaction, and unresolved-feedback display all derive from existing tables and current-user role | Locked | build-log/036 |
| ADR-160 | _(Phase8B4-016)_ B4 adds only two service request detail response-shape additions: `IReadOnlyList<ContactActionItem> ContactActions` and `bool FeedbackCommentVisible`. `ContactActionItem` is `{ type, available, target }`, only available actions are included, and feedback visibility metadata disambiguates absent comments from redacted comments | Locked | build-log/036 |
| ADR-161 | _(Phase8B4-017)_ Viewer remains read-only on service request detail. Viewers may read detail when permitted, receive no write/action affordances, receive `ContactActions=[]`, and receive redacted feedback comments (`FeedbackComment=null`, `FeedbackCommentVisible=false`); B4 does not change raw customer phone/email visibility | Locked | build-log/036 |
| ADR-162 | _(Phase8B4-018)_ Customer page visit tracking is a valid future operator confidence signal but deferred out of B4. No opened endpoint, beacon, telemetry write, or `CustomerPageLastViewedAtUtc` field is added; future semantics must avoid presence/read-receipt claims and define debounce, rate limiting, privacy, and public-token guard behavior | Locked | build-log/036; DEF-030 |
| ADR-163 | _(Phase8B4-019)_ B4 completion gate: no migration; service request detail returns `ContactActions` and `FeedbackCommentVisible`; participant display uses `User.Name` with email fallback; feedback comments are Owner/Admin-only; read-only users receive no contact actions; customer boundary tests remain green; B4 integration matrix and full suite pass; no deferred workflows are pulled in | Locked | build-log/036 |

| ADR-164 | _(Phase8B5-001)_ B5 request list is the command center for scanning, prioritization, movement indicators, attention reason indicators, and common quick-action affordances; request detail remains the full-history workbench for careful action. B5 prepares list/routing state for V1 notifications; basic push/badges are pre-go-live (ADR-289), while full notification preferences/outbox/retries/analytics remain later | Locked | build-log/038; ADR-289 |
| ADR-165 | _(Phase8B5-002)_ Default request list surfaces closed `UnresolvedFeedback` attention for Owner/Admin only. Normal closed/cancelled requests remain hidden by default; these rows are visually distinct post-close follow-up, not reopened work. Operators do not see Owner/Admin-only post-close feedback escalation by default unless a future assignment/watch/visibility workflow explicitly attaches them; feedback comment visibility remains Owner/Admin-only | Locked | build-log/038 |
| ADR-166 | _(Phase8B5-003)_ B5 list ranking is deterministic and attention-first: active overdue business-waiting, active priority business-waiting, closed unresolved feedback for Owner/Admin, active standard business-waiting, then other active requests. Within equivalent groups, priority precedes standard, oldest unresolved `AttentionSinceUtc` wins, and stable activity/creation/id fallbacks apply. Waiting-on-customer does not rise in the business-waiting queue | Locked | build-log/038 |
| ADR-167 | _(Phase8B5-004)_ Quick contact activation alone is not authoritative. Tapping `call`, `email`, or `text` does not by itself count as first response or clear attention. B5 may expose quick action metadata and UI placement, but first-response and attention effects require a server-side logged business action, customer-visible Keep update, or explicit acknowledge action | Locked | build-log/038; DEF-018 |
| ADR-168 | _(Phase8B5-005)_ Only calls collect a post-contact outcome component. Initial call outcomes are `Spoke with customer`, `Left voicemail`, `No answer`, and `Wrong number`. Email/text do not show outcome choices such as resolved/question answered/customer handled because sending a message does not verify the customer saw, understood, or accepted it | Locked | build-log/038; DEF-031 |
| ADR-169 | _(Phase8B5-006)_ Call outcome effects depend on reach plus follow-up need. `Spoke with customer` counts as first response and may clear attention when no follow-up is needed. `Left voicemail` also counts as first response, does not prove resolution, and may clear attention when no follow-up is needed. `No answer` and `Wrong number` log attempts/issues but do not count first response or clear attention. No call outcome changes request status or resolves closed unresolved feedback | Locked | build-log/038; DEF-031 |
| ADR-170 | _(Phase8B5-007)_ Email/text log sent activity, not customer outcome. A substantive sent email/text can count as first response and can clear business-waiting attention when the operator indicates it addressed the customer and no follow-up is needed, but it must use sent-activity language and must not infer resolved/question answered/customer handled. Keep-owned SMS sending remains excluded/deferred | Locked | build-log/038; DEF-018; DEF-022 |
| ADR-171 | _(Phase8B5-008)_ Operators can log inbound external customer contact: customer called, texted, emailed, spoke in person, or other contact. Inbound external contact is internal-only by default, logs customer-provided request information, does not count as business first response, and may raise/preserve business-waiting attention when follow-up is required or simply record context when no follow-up is needed | Locked | build-log/038; DEF-032 |
| ADR-172 | _(Phase8B5-009)_ External contact logging is the immediate post-B5 workflow slice, not vague post-foundation polish. B5 prepares list quick action metadata/placement; the next external-contact slice implements outbound call outcome logging, outbound email/text sent logging, inbound customer contact logging, applicable first-response effects, explicit attention clearing/creation, internal audit/timeline events, and optional notes. B5 defers persistence of those contact logs, undo/revert, customer-visible receipts, SMS sending, notifications, assignment/watch writes, and customer page telemetry | Locked / deferred implementation | build-log/038; deferred-topics |
| ADR-173 | _(Phase8B5-010)_ B5 scope is the default command-center list only: ranking, attention/movement indicators, Owner/Admin closed unresolved-feedback surfacing, quick action metadata, contact affordances, list-safe available actions, and role/ranking tests. Filters, search, pagination, closed history, and history workflows are deferred to separate post-B5/pre-go-live sessions. SSE/WebSockets/list streaming are explicitly post-v1 unless pilot usage proves freshness insufficient (ADR-290) | Locked | build-log/038; ADR-290 |
| ADR-174 | _(Phase8B5-011)_ Visibility, participation, notification eligibility, and notification enabled state are separate concepts. B5 preserves current active-request visibility while preparing metadata for future assignment/watch and notification-fatigue controls. Viewer is notification-ineligible by default; off-season suppresses request notifications account-wide; Owner/Admin broad visibility does not imply all-request notifications | Locked | build-log/038; DEF-012 |
| ADR-175 | _(Phase8B5-012)_ Default B5 list includes Received, Scheduled, InProgress, PendingCustomer, Resolved, and Closed only when unresolved-feedback attention exists for Owner/Admin. Normal Closed and Cancelled are excluded from the default list; Resolved remains because it is pre-terminal | Locked | build-log/038 |
| ADR-176 | _(Phase8B5-013)_ Request participation model: zero or one active Responsible participant; many Watching participants; new requests are unassigned by default unless a future account setting auto-assigns; Owner/Admin visibility is not assignment; unassigned routing uses Owner/Admin fallback/default inbox; responsibility transfer replaces the current Responsible participant | Locked | build-log/038; deferred-topics |
| ADR-177 | _(Phase8B5-014)_ Exact default ranking: active overdue business-waiting, active priority business-waiting, closed unresolved feedback for Owner/Admin, active standard business-waiting, first-response pending, PendingCustomer/waiting-on-customer, Resolved without attention, other active. Tie-breakers use oldest attention, due time, activity timestamps, created time, and stable request id as documented | Locked | build-log/038 |
| ADR-178 | _(Phase8B5-015)_ B5 attention indicators expose both reason label and next-action prompt. Initial mapping includes Reply needed/Update customer, Update requested/Update customer, Schedule change/Review schedule, Change/cancel request/Review request, Issue reported/Respond, Response overdue or New request/Send first response, and Still needs help/Review feedback. Mappings must be exhaustive and tested; unknown client codes fall back to Needs attention/Review request/open detail only | Locked | build-log/038 |
| ADR-179 | _(Phase8B5-016)_ B5 movement indicators explain current rank using reason, severity, elapsed waiting time, due/overdue timing, post-close context, and waiting direction. Do not implement rank-change arrows, numeric movement deltas, rank history, new-since-last-view, or customer-viewed-after-update signals in B5 | Locked | build-log/038; deferred-topics |
| ADR-180 | _(Phase8B5-017)_ B5 exposes a simple Contact customer affordance on active request rows when contact info exists and the user can operate. It reduces field-operator friction and supports both response and proactive contact; future logging infers default contact purpose rather than requiring upfront operator classification | Locked | build-log/038 |
| ADR-181 | _(Phase8B5-018)_ B5 list quick actions are Contact customer, Update customer, Mark handled, Open detail, and Review feedback for closed unresolved-feedback rows. Update customer posts a customer-visible Keep update and uses explicit action code such as post_customer_update. List status changes, close/cancel, assignment/watch/mute, notification toggles, and mark feedback reviewed are deferred | Locked | build-log/038 |
| ADR-182 | _(Phase8B5-019)_ Mark handled is the secondary/overflow list label for acknowledge_attention. It requires operate permission, active eligible attention, confirmation, and reason; it is unavailable for closed unresolved-feedback and first-response-overdue/no-response rows; it clears attention through internal audit only and never notifies the customer, counts first response, changes status, or resolves feedback | Locked | build-log/038 |
| ADR-183 | _(Phase8B5-020)_ B5 list is mobile-first with 2-3 line default previews and expand-on-demand for long messages. Expanded rows show a larger useful preview, not full timeline/history. Detail remains the place for full history, internal notes, feedback comments, participants, customer page link, and careful lifecycle decisions | Locked | build-log/038 |
| ADR-184 | _(Phase8B5-021)_ B5 locks conceptual list summary metadata: identity/contact, status/lifecycle, attention, display/ranking, actions, and participation/notification fields. Backend sends stable codes/state metadata; frontend owns human labels/prompts with exhaustive mapping tests and safe unknown-code fallback | Locked | build-log/038 |
| ADR-185 | _(Phase8B5-022)_ B5 includes compact read-only participant and notification metadata: responsible/watching counts, hasResponsible/isUnassigned, current user participation and notification state, eligible/enabled/suppressionReason. Assignment/watch/mute/toggle writes and notification delivery remain deferred | Locked | build-log/038; deferred-topics |
| ADR-186 | _(Phase8B5-023)_ Closed unresolved-feedback rows are review-only from the list: Still needs help, Review feedback, Closed context, post-close styling. Review feedback opens detail focused on feedback; Contact customer, Update customer, Mark handled, Mark feedback reviewed, and status changes are not exposed on those rows in B5. G7b adds a narrow Owner/Admin exception: outbound external-contact logging is permitted on Closed requests with active `UnresolvedFeedback` review attention | Locked | build-log/038; DEF-029; G7b; ADR-300; build-log/058 |
| ADR-187 | _(Phase8B5-024)_ Filters, search, and batch queues are deferred but pre-go-live: search, status filters, closed history, feedback review queue, next/previous detail navigation, pagination, and client freshness behavior. B5 preserves metadata such as status, attentionReason, rankingGroup, isPostCloseFollowUp, needsFeedbackReview, lifecycle/activity timestamps. Realtime list streaming is post-v1 (ADR-290) | Locked | build-log/038; deferred-topics; ADR-290 |
| ADR-188 | _(Phase8B5-025)_ Archive is future closeout/history workflow, not B5. Closed means customer/request work done, closeout reviewed means internal review complete, archived means hidden from normal operational/history surfaces but retained. B5 adds no archive/unarchive/auto-archive actions; normal Closed and Cancelled stay excluded from default list | Locked | build-log/038; DEF-028 |
| ADR-189 | _(Phase8B5-026)_ Resolved remains in the default list because it is pre-terminal. Resolved with active business-waiting attention ranks by attention; Resolved without attention ranks as quiet active work. Allowed list actions are Open detail, Contact customer, Update customer, and Mark handled only if normal active-attention eligibility passes; no close/cancel/status/archive actions in B5 | Locked | build-log/038 |
| ADR-190 | _(Phase8B5-027)_ PendingCustomer means the business is waiting on the customer. It stays in the default list with lower priority and Waiting on customer context; Contact customer, Update customer, and Open detail are allowed. Update customer posts a nudge but does not change status or clear attention by default. A customer reply flips waiting direction to business and raises attention | Locked | build-log/038 |
| ADR-191 | _(Phase8B5-028)_ B5 list actions must communicate effects: customer-visible/internal/external-affordance, clears attention, counts first response, changes status, and status remains unchanged. Quick action metadata should include code, label, visibility, clearsAttention, countsFirstResponse, changesStatus, and effectSummaryCode | Locked | build-log/038 |
| ADR-192 | _(Phase8B5-029)_ Calm attention design: list order is the primary urgency signal; visual indicators support scanning through reason chips, prompts, timing, subtle severity, and sections. Avoid excessive warning icons, stacked red badges, numeric urgency scores, and loud treatment for routine messages. First-response pending uses New request/Response due in; overdue uses Response overdue/Send first response and no Mark handled | Locked | build-log/038 |
| ADR-193 | _(Phase8B5-030)_ Operator completion and Owner/Admin closeout are separate. Operator-facing Mark completed maps to backend Resolved; Owner/Admin Close request maps to backend Closed and enables feedback. B5 adds no list-level close/cancel/resolve/status actions and does not auto-complete/auto-close stale requests. Future pre-go-live closeout tools include Ready to close, Needs feedback review, Needs status check, date filters, next/previous navigation, and Close and next; Ready to close excludes active attention | Locked | build-log/038; deferred-topics |

| ADR-194 | _(Phase8B5-impl-001)_ `ComputeSeverity` returns `"attention"` for first-response-pending rows (not `"muted"`). First-response pending is an actionable operator signal in the same attention family as business-waiting; `muted` would suppress it below PendingCustomer/quiet-Resolved. Method signature takes `bool firstResponsePending` as an explicit parameter | Implemented | B5 Session 1 |
| ADR-195 | _(Phase8B5-impl-002)_ A `Resolved` request ranks as `resolved_quiet` (group 7) only when neither the `firstResponsePending` nor `firstResponseOverdue` checks fire AND `AttentionLevel=None`. A Resolved request with a `FirstResponseDueAtUtc` still in the future ranks in group 5 (`first_response_pending`) instead. `FirstRespondedAtUtc` being set suppresses both first-response checks; Business-origin requests (no `FirstResponseDueAtUtc`) reach group 7 directly | Implemented | B5 Session 1 |
| ADR-196 | _(Phase8B5-S2-001)_ External contact logs are first-class internal `KeepRequestEvent` rows with structured nullable external-contact fields, not a separate table. The event remains the durable request audit/timeline record and carries actor/time data for future list/detail projections | Implemented | build-log/038; Session 2A |
| ADR-197 | _(Phase8B5-S2-002)_ Session 2 exposes one authenticated endpoint/service, `POST /keep/requests/{requestId}/external-contact`, for inbound and outbound external contact. Backend validation rejects invalid direction/channel/outcome combinations and backend logic derives first-response and attention effects rather than trusting client state flags | Implemented | build-log/038; Session 2B |
| ADR-198 | _(Phase8B5-S2-003)_ External contact is internal team-memory/audit. Quick contact launch alone is not durable contact evidence. The effect matrix is backend-derived: spoke/voicemail/email/text sent can count first response and may clear eligible attention when no follow-up is needed; no-answer/wrong-number log only; inbound contact does not count first response and may raise/preserve business-waiting attention when follow-up is required. External contact never changes lifecycle status, updates the customer page automatically, or resolves closed unresolved feedback | Implemented | build-log/038; Session 2A-2B |
| ADR-199 | _(Phase8B5-S2-004)_ Summary requirements balance speed and team memory: outbound phone outcomes can be logged without a required note, outbound email/text sent logs require a summary of what was sent, inbound external customer contact always requires a summary, and all summaries are internal-only in Session 2 | Implemented | build-log/038; Session 2A-2B |
| ADR-200 | _(Phase8B5-S2-005)_ External contact logging is allowed only on non-terminal requests. `Closed` and `Cancelled` reject external contact logs; internal notes remain available on terminal requests. External contact never reopens requests or resolves closed unresolved feedback | Implemented | build-log/038; Session 2A-2B |
| ADR-201 | _(Phase8B5-S2-006)_ Session 2 exposes external contact logs in request detail timeline metadata and updates request state fields that affect list ranking, but does not add default-list recent-activity previews, row expansion, or event loading. Event data must be structured and actor/time-stamped so a later mobile list UX/history slice can show compact last 1-2 activity previews without changing the contact-log model | Implemented | build-log/038; Session 2B; DEF-040 |
| ADR-202 | _(Phase8B5-S2-007)_ `POST /keep/requests/{requestId}/external-contact` returns `KeepRequestDetailResult`, matching existing operator write endpoints. The response includes the new internal timeline event and updated request state/action metadata. Session 2 does not return undo metadata or implement contact-log revert | Implemented | build-log/038; Session 2B |
| ADR-203 | _(Phase8B5-S2-008)_ Session 2 adds explicit external-contact enums for direction and call outcome while reusing existing `CommunicationChannel` for phone/SMS/email/in-person/other. EF stores enum values as strings; API uses stable lowercase codes. Outcomes are valid only for outbound phone contact. Outbound contact initially supports phone/SMS/email; inbound contact supports phone/SMS/email/in-person/other | Implemented | build-log/038; Session 2A-2B |
| ADR-204 | _(Phase8B5-S2-009)_ Inbound external contact with `requiresBusinessFollowUp=true` raises or preserves business-waiting attention using the same response-policy timing rules as customer messages, standard priority by default. It updates last customer activity, does not count as business first response, preserves oldest attention start when already business-waiting, and flips waiting-on-customer to business-waiting from the inbound contact time | Implemented | build-log/038; Session 2A-2B |
| ADR-205 | _(Phase8B5-S2-010)_ When external contact is logged in Keep, activity timestamps update by direction: all logged outbound contact attempts update `LastBusinessActivityAt`, and logged inbound customer contact updates `LastCustomerActivityAt`. These timestamps describe logged activity only and do not imply every off-platform call/text/email was captured | Implemented | build-log/038; Session 2A-2B |
| ADR-206 | _(Phase8B5-S2-011)_ Session 2 external contact logs are append-only and do not support undo/revert or mistake-flagging. Mistakes can be corrected with internal notes. Future mistake/ignore markers may visually mark logs without reversing state effects; true revert requires compensating events and explicit first-response/attention/activity/projection semantics | Implemented | build-log/038; Session 2A; DEF-041 |
| ADR-207 | _(Phase8B5-S2-012)_ Session 2 uses explicit contact-specific validation errors for invalid external-contact direction/channel/outcome combinations, missing required summaries, and missing follow-up answers. Existing auth, permission, account-access, feature, not-found, and terminal-state errors remain reused; validation fails before domain mutation or event persistence | Implemented | build-log/038; Session 2A-2B |
| ADR-208 | _(Phase8B5-S2-013)_ OffSeason is a frozen/read-mostly Keep posture. Normal role-based reads remain available, but normal operator/customer/background writes are blocked. Existing requests remain in their current status and are not auto-closed; Owner/Admin may perform a narrow closeout action for non-terminal requests with internal audit only. Public intake links remain reachable but show an unavailable/read-only response instead of 404 and do not create customer/request records or start workflows. Business-approved fallback message/contact/social settings are deferred | Locked | build-log/038; DEF-042; DEF-043 |
| ADR-209 | _(Phase8B5-S2-014)_ External-contact logging uses the existing operator-write authorization stack and applies the OffSeason freeze. Authenticated active account membership, `Keep.RequestsOperate`, `Keep.OperatorQueue`, account-scoped request lookup, and non-blocked account access are required; Viewer/read-only users cannot log contact; OffSeason blocks external-contact logging because normal Keep writes are frozen | Implemented | build-log/038; Session 2B |
| ADR-210 | _(Phase8B5-S2-015)_ External contact events use existing `OccurredAtUtc` as the time the contact log is created in Keep. Session 2 does not add `ExternalContactOccurredAtUtc` or `ExternalContactSource`; future analytics/backdating/provenance may add occurred-time and source fields together if pilot usage shows a need | Implemented | build-log/038; Session 2A; DEF-044 |
| ADR-211 | _(Phase8B5-S2-016)_ Session 2 uses one API/application service but two domain methods on `KeepRequest`: `LogOutboundExternalContact` and `LogInboundExternalContact`. The split keeps first-response, attention-clearing, inbound follow-up, summary requirements, and outcome validation explicit while preserving one client-facing command | Implemented | build-log/038; Session 2B |
| ADR-212 | _(Phase8B5-S2-017)_ Session 2 does not distinguish Keep-initiated contact from manually logged contact. Both create the same `ExternalContactLogged` event after operator confirmation. Future native-app/analytics work may add `ExternalContactSource` together with occurred-time and provenance rules | Implemented | build-log/038; Session 2A; DEF-044 |
| ADR-213 | _(Phase8B5-S2-018)_ When an external contact log counts as the first business response, `FirstRespondedAtUtc`, `FirstResponderAccountUserId`, and `FirstResponseEventId` are set from the `ExternalContactLogged` event. No-answer, wrong-number, inbound contact, business-origin requests, and already-first-responded requests do not update first-response fields | Implemented | build-log/038; Session 2A |
| ADR-214 | _(Phase8B5-S2-019)_ When outbound external contact clears active eligible attention, `AttentionClearReason` stores stable internal code `external_contact_no_follow_up`. Channel, outcome, and summary remain on the event; no-answer, wrong-number, inbound contact, and contact that does not clear attention do not set an attention clear reason | Implemented | build-log/038; Session 2A |
| ADR-219 | _(Phase8B5-S2B-001)_ `GetResponsePolicyAsync` added to `IKeepRequestOperatePersistence` (not injecting `IKeepIntakePersistence` into `LogExternalContactService`). Reason: external contact is an operator-write workflow; inbound follow-up needs response-policy standard minutes. Pulling `IKeepIntakePersistence` into an operator-write service couples public-intake persistence to operator writes. Fallback: `policy?.StandardResponseTargetMinutes ?? 240` | Implemented | Session 2B |
| ADR-220 | _(Phase8B5-S2B-002)_ `CanLogExternalContact` added to `AvailableActionsMetadata` now, not deferred. Rule for 2B: `canOperate && !request.IsTerminal`. OffSeason action-metadata tightening deferred to Session 2C alongside other write-policy metadata corrections. All 5 existing services that construct `AvailableActionsMetadata` updated in the same pass | Implemented | Session 2B |
| ADR-221 | _(Phase8B5-S2C-001)_ OffSeason write freeze: all Keep operator write services set `RequestImplementsAllowedInOffSeason: false` and check `decision.IsReadOnly` → 403. `GetKeepRequestDetailService` computes `canWrite = canOperate && !isOffSeason` for all action-metadata flags. Customer-page writes (`AddCustomerMessage`, `SubmitFeedback`) blocked via `context.IsOffSeason` → `KeepRequest.OffSeasonUnavailable` → 409. Owner/Admin narrow closeout deferred to DEF-042. `LogExternalContactService` `IsReadOnly` check also corrected here | Implemented | Session 2C |
| ADR-215 | _(Phase8B5-S2-020)_ Request detail timeline events expose nullable external-contact metadata fields on the existing event DTO: direction, channel, outcome, requiresFollowUp, setFirstResponse (renamed from countsFirstResponse), and clearedAttention. Non-contact events return null for these fields. Customer page timelines continue to exclude internal external-contact logs | Implemented | build-log/038; Session 2B |
| ADR-216 | _(Phase8B5-S2-021)_ External-contact request body has `direction`, `channel`, `outcome`, `requiresBusinessFollowUp`, and `summary`. Direction/channel are required. Outcome is required only for outbound phone and rejected otherwise. `requiresBusinessFollowUp` is required for inbound, outbound SMS/email, and outbound phone spoke/voicemail, and rejected for no-answer/wrong-number where it is null/not applicable in domain/event data. Summary is required for inbound and outbound SMS/email, optional for outbound phone, max 4000 chars; summary rules are domain invariants as well as API validation | Implemented | build-log/038; Session 2B |
| ADR-217 | _(Phase8B5-S2-022)_ Session 2 migration scope is limited to adding `ExternalContactLogged` event support and structured nullable external-contact fields on `KeepRequestEvent`. It does not add separate contact tables, occurred/source timestamps, mistake/revert fields, list preview fields, or denormalized last-contact columns. OffSeason freeze should use existing account operating-mode data unless implementation proves a schema gap | Locked | build-log/038 |
| ADR-218 | _(Phase8B5-S2-023)_ Session 2 completion requires focused domain/unit tests for the external-contact effect matrix and integration tests for endpoint auth, validation, persistence, detail response metadata, customer-page exclusion, OffSeason blocking, and OffSeason public-intake unavailable behavior. Full suite should pass if feasible and docs/session-log must record exact tests run | Locked | build-log/038 |
| ADR-222 | _(Phase8B5-S3-001)_ Session 3 participation writes are controlled internal routing actions for non-terminal requests only: assign, transfer, clear responsible, watch, unwatch, mute, and unmute. Closed unresolved-feedback remains review-only; OffSeason blocks participation writes; notification delivery, quiet hours/preferences, Operator Unassigned queue, auto-assignment settings, realtime list refresh, and customer-visible receipts are out of scope | Implemented | Sessions 3A+3B |
| ADR-223 | _(Phase8B5-S3-002)_ Owner/Admin may assign, transfer, and clear responsibility. Operators may self-assign only unassigned/effectively unassigned requests they can legitimately access; Operators may not assign others, transfer, clear another user's responsibility, or clear themselves as Responsible. Owner/Admin must clear or transfer responsibility when an Operator needs off a request | Implemented | Sessions 3A+3B |
| ADR-224 | _(Phase8B5-S3-003)_ Responsible and Watching are mutually exclusive for the same user, with one active participation row per user/request. Clear and transfer detach the previous Responsible and do not auto-watch them. Unwatch applies only to active Watching participants and never clears responsibility. Enforce one active Responsible with application invariant and likely filtered unique index | Implemented | Sessions 3A+3B |
| ADR-225 | _(Phase8B5-S3-004)_ Watch creates/reactivates Watching with notifications enabled. Mute requires active current-user participation and only sets `NotificationsEnabled=false`; unmute sets it true. Mute does not detach, hide, or remove visibility. Mute/unmute is current-user only in Session 3, while internals should remain future-friendly for delegated controls | Implemented | Sessions 3A+3B; DEF-046 |
| ADR-226 | _(Phase8B5-S3-005)_ Eligible assignees/watchers are Active Owner/Admin/Operator only. Viewer, Invited, Suspended, Removed, and cross-account users are ineligible. Later-ineligible participants are notification-ineligible; stale Responsible users are effectively unassigned for routing/notifications but not auto-detached. Owner/Admin surfaces should expose stale responsibility indicators | Implemented | Sessions 3A+3B; DEF-047 |
| ADR-227 | _(Phase8B5-S3-006)_ Operator default list hides unassigned requests. Owner/Admin see unassigned active requests by default. Operators may access unassigned work only through an explicit future Unassigned/Available queue or allowed surface; Session 3 does not build that queue or decide hidden-return vs on-demand fetch | Implemented | Sessions 3A+3B; DEF-045 |
| ADR-228 | _(Phase8B5-S3-007)_ Request detail is the primary participation-control surface. The list shows responsible person name and compact metadata, but no full watcher list. Owner/Admin may assign/transfer Responsible from the list; list clear/watch/mute are excluded from Session 3. Operators do not see list-level assignment controls | Implemented | Sessions 3A+3B |
| ADR-229 | _(Phase8B5-S3-008)_ Participation writes create internal-only audit/timeline events and are customer-page excluded. They do not update customer/business activity timestamps, set/clear attention, count as first response, change status, or affect ranking except through participation/notification metadata used by filters or future visibility rules | Implemented | Sessions 3A+3B |
| ADR-230 | _(Phase8B5-S3-009)_ Session 3 uses explicit idempotent endpoints for responsible, watchers, current-user watch, and current-user mute. Writes return success with updated detail on safe no-ops, without duplicate timeline events or notification intent; invalid actions still fail. `PUT /responsible` accepts `{ accountUserId }`; mute routes require no body | Implemented | Sessions 3A+3B |
| ADR-231 | _(Phase8B5-S3-010)_ Participation write endpoints return updated `KeepRequestDetailResult`, including participant summary, current-user participation/notification state, and internal timeline events visible to the caller. List refresh remains client-owned; no realtime invalidation stream or separate list write response is added | Implemented | Sessions 3A+3B |
| ADR-232 | _(Phase8B5-S3-011)_ Participation writes do not require notes. Session 3 supports optional internal notes for Owner/Admin assignment, transfer, clear responsible, add watcher, and remove watcher actions. Self-service watch/unwatch/mute/unmute do not expose notes. Notes are internal-only, stored with the event, and never customer-visible | Implemented | Sessions 3A+3B |
| ADR-233 | _(Phase8B5-S3-012)_ Assignment/watch notification intent is recorded as structured metadata on internal participation events, not delivered. Intent is created for Owner/Admin assignment/transfer to a new Responsible user and adding another watcher; no intent for idempotent no-ops, clear, self-assignment, self-watch, remove watcher, mute/unmute, or blocked OffSeason writes. No outbox, device, push/email job, retry, or badge state is added | Implemented | Sessions 3A+3B; DEF-012 |
| ADR-234 | _(Phase8B5-S3-013)_ Session 3 uses one internal `ParticipationChanged` event type with structured action codes (`responsible_assigned`, `responsible_transferred`, `responsible_cleared`, `watcher_added`, `watcher_removed`, `self_watched`, `self_unwatched`, `muted`, `unmuted`) rather than many separate event types | Implemented | Sessions 3A+3B |
| ADR-235 | _(Phase8B5-S3-014)_ Session 3 should add or reuse a compact eligible participant lookup, preferably `GET /keep/requests/participant-candidates`, returning only accountUserId, displayName, and role for Active Owner/Admin/Operator users. Request-list rows must not include full candidate arrays; writes still validate eligibility authoritatively | Implemented | Sessions 3A+3B |
| ADR-236 | Mobile native app stack recommendation: React Native + Expo + TypeScript for the field/operator app, with direct APNs/FCM notification delivery owned by OpHalo backend rather than permanent Expo Push Service dependency | Proposed | [ADR-236](ADR-236-mobile-native-app-technology-stack.md) |
| ADR-237 | _(Phase8B5-S4-001)_ Session 4 extends existing `GET /keep/requests` with optional query parameters for named views, filters, search, history, and pagination. Plain no-query `GET /keep/requests` remains the B5 default command-center list; no parallel list endpoints are introduced | Implemented | Sessions 4A-4C |
| ADR-238 | _(Phase8B5-S4-002)_ No-query `GET /keep/requests` continues to return active operational requests with B5 attention-first ranking plus Owner/Admin closed unresolved feedback only. Normal Closed, Cancelled, search, and filter/history modes appear only when query params are supplied | Implemented | Sessions 4A-4C |
| ADR-239 | _(Phase8B5-S4-003)_ Session 4 uses named views first (`default`, `assigned_to_me`, `watching`, `unassigned`, `needs_attention`, `feedback_review`, `closed_history`, `cancelled_history`, `all_history`) with limited filters layered on top (`status`, `attentionReason`, `assignedAccountUserId`, `q`, created/closed date ranges) | Implemented | Sessions 4A-4C |
| ADR-240 | _(Phase8B5-S4-004)_ Operators get an explicit `unassigned`/available view while unassigned work stays out of the Operator default list. Operator unassigned view returns only requests eligible for self-assignment, excludes terminal/post-close feedback rows, and should narrowly re-enable Operator self-assign for rows visible through this allowed surface | Implemented | Sessions 4A-4C; DEF-045 |
| ADR-241 | _(Phase8B5-S4-005)_ Session 4 exposes lightweight role-aware operational view counts for `default`, `assigned_to_me`, `watching`, `unassigned`, `needs_attention`, and `feedback_review`. Counts are current-user visible/actionable snapshots, not global account totals or realtime subscriptions | Implemented | Sessions 4A-4C |
| ADR-242 | _(Phase8B5-S4-006)_ Closed unresolved feedback appears for Owner/Admin in default as post-close follow-up, in `feedback_review` as a focused review queue, and in `closed_history` as retained history. Operators do not see closed unresolved feedback in these views in Session 4 | Implemented | Sessions 4A-4C |
| ADR-243 | _(Phase8B5-S4-007)_ Request-list rows expose stable `rowContext` metadata (`active_work`, `needs_attention`, `first_response`, `waiting_on_customer`, `feedback_review`, `closed_history`, `cancelled_history`, `unassigned_available`) so UI can visually distinguish row types. `ready_to_close` remains out of Session 4 | Implemented | Sessions 4A-4C |
| ADR-244 | _(Phase8B5-S4-008)_ Session 4 adds `feedback_review` view/queue but does not implement `Mark feedback reviewed`. Review completion, attention clearing/hiding behavior, notes, undo/reopen-review, timeline event shape, and closed-history effects remain deferred | Implemented | Sessions 4A-4C; DEF-029 |
| ADR-245 | _(Phase8B5-S4-009)_ Session 4 supports closed/cancelled/all history browsing but does not implement archive, unarchive, auto-archive, include-archived, hide-from-history, or archive notes/reasons | Implemented | Sessions 4A-4C; DEF-028 |
| ADR-246 | _(Phase8B5-S4-010)_ Cancelled requests are retained history only: excluded from default, shown in `cancelled_history`, and included in `all_history`; `closed_history` remains Closed-only | Implemented | Sessions 4A-4C |
| ADR-247 | _(Phase8B5-S4-011)_ Search starts with visible request/customer fields: reference code, customer name/phone/email, request title/summary/description if present, visible list preview text if already available, and Owner/Admin-only feedback comment. Internal notes, full timeline, participation notes, external-contact summaries, tokens, raw IDs, and private staff fields are excluded | Implemented | Sessions 4A-4C; DEF-051 |
| ADR-248 | _(Phase8B5-S4-012)_ Broad terminal history (`closed_history`, `cancelled_history`, `all_history`) is Owner/Admin-only in Session 4. Operators search only operationally visible active/work views; Viewers receive no broad history expansion | Implemented | Sessions 4A-4C; DEF-048 |
| ADR-249 | _(Phase8B5-S4-013)_ Session 4 uses cursor pagination with client-selected `limit` and opaque `cursor`; default limit is 50 and max is 100. The API is device-neutral: web/PWA and native mobile choose appropriate limits/views while backend enforces caps and visibility | Implemented | Sessions 4A-4C |
| ADR-250 | _(Phase8B5-S4-014)_ All Session 4 views/search/filters/pagination are server-side. Backend resolves user/account/role, applies authorization, view, filters/search, deterministic sort, fetches `limit + 1`, and returns rows/pageInfo/counts. Clients do not fetch all requests and filter/search locally | Implemented | Sessions 4A-4C |
| ADR-251 | _(Phase8B5-S4-015)_ Session 4 does not expose exact arbitrary totals such as `totalResults`, `totalClosedHistory`, or exact q/status/date totals. It exposes operational `viewCounts` plus `pageInfo.hasMore`, `nextCursor`, and `limit`; reporting-like totals are deferred until real usage justifies them | Implemented | Sessions 4A-4C; DEF-049 |
| ADR-252 | _(Phase8B5-S4-016)_ Sorting depends on selected view: default/needs_attention and participation views use B5 ranking within the subset; `feedback_review` sorts oldest unresolved feedback attention first; closed/cancelled/all history sorts most recent terminal first | Implemented | Sessions 4A-4C |
| ADR-253 | _(Phase8B5-S4-017)_ Session 4 evolves list response shape to include `requests`, `pageInfo`, `viewCounts`, and `listContext`. `viewCounts` are request-time snapshots refreshed by clients after writes/refetch; cross-user realtime invalidation remains deferred | Implemented | Sessions 4A-4C; DEF-017 |
| ADR-254 | _(Phase8B5-S4-018)_ History rows expose minimal read/review actions only: `open_detail` for closed/cancelled/all history and `review_feedback`/focused detail open for feedback_review. No contact/update/acknowledge/mark-reviewed/archive/reopen/assign/watch/mute/contextual-history actions appear on Session 4 history rows | Implemented | Sessions 4A-4C |
| ADR-255 | _(Phase8B5-S4-019)_ Internal/team-memory search is deferred. Session 4 excludes internal notes, participation notes, external-contact summaries, and full internal timeline search pending role/privacy/indexing/result-display decisions | Implemented | Sessions 4A-4C; DEF-051 |
| ADR-256 | _(Phase8B5-S4-020)_ Session 4 is backend/foundation only. It provides stable query params, named views, server-side search/filtering, cursor pagination, pageInfo, role-aware counts, list/row context metadata, role-safe actions, history access rules, feedback_review queue, tests, and docs; web/PWA, native UI, swipe actions, client state, and realtime refresh remain out of scope | Implemented | Sessions 4A-4C |
| ADR-257 | _(Phase8B5-S4-021)_ Invalid list query parameters fail explicitly with 400: unknown view, invalid status/attentionReason/date/limit/cursor, contradictory view/filter combinations, or cursor reuse with a different query shape. `status` never expands visibility; terminal statuses require explicit history views | Implemented | Sessions 4A-4C |
| ADR-258 | _(Phase8B5-S4-022)_ Search/date validation is bounded: trim `q`, blank q behaves like no search, case-insensitive matching, digit-normalized phone search, max q length 200; created/closed from filters are inclusive, to filters exclusive; date filters require ISO-8601/RFC3339 timestamps with UTC or explicit offset; closed filters use `TerminatedAtUtc` | Implemented | Sessions 4A-4C |
| ADR-259 | _(Phase8B5-S4-023)_ `viewCounts` are base operational counts for the current user, not query-specific totals. A search within `assigned_to_me` filters returned rows, but `viewCounts.assigned_to_me` remains the user's total assigned-to-me operational count | Implemented | Sessions 4A-4C |
| ADR-260 | _(Phase8B5-S4-024)_ Session 4 indexing is narrow and implementation-driven. Use existing indexes where possible; add narrowly scoped indexes only if needed for implemented server-side history/search pagination; do not add full-text search or event-history search indexes in Session 4 | Implemented | Sessions 4A-4C |
| ADR-261 | _(Phase8B5-S5-001)_ Session 5 excludes active complaint/issue workflow changes. Customer-facing `/issue` remains normal active/pre-close complaint-priority attention; management-only complaint mode and new active complaint visibility rules remain out of scope | Implemented | Sessions 5A-5C |
| ADR-262 | _(Phase8B5-S5-002)_ Customer feedback remains one-time and Closed-only for v1. `Resolved` stays active/pre-close and supports normal messages/issues; `Closed` enables the single feedback action when eligible. Closing enables feedback but sends no automatic prompt | Implemented | Sessions 5A-5C |
| ADR-263 | _(Phase8B5-S5-003)_ Positive feedback is stored/history only and broadly visible to authenticated request viewers with access. Negative feedback creates `UnresolvedFeedback` review work; full negative feedback comments remain Owner/Admin-only while lower roles receive only safe metadata under existing visibility rules | Implemented | Sessions 5A-5C; build-log/047; ADR-300; build-log/058 |
| ADR-264 | _(Phase8B5-S5-004)_ Only Owner/Admin can mark post-close unresolved feedback reviewed. Operators and Viewers cannot clear the feedback review queue, even if responsible/watching/involved | Implemented | Sessions 5A-5C |
| ADR-265 | _(Phase8B5-S5-005)_ Mark feedback reviewed is internal acknowledgement only: it does not reopen, change Closed status, delete/edit/hide feedback, notify the customer, count as resolving the customer's issue, or overwrite negative feedback | Implemented | Sessions 5A-5C |
| ADR-266 | _(Phase8B5-S5-006)_ Unresolved negative feedback never auto-clears or times out. It remains in Owner/Admin review surfaces until explicitly reviewed; aging affects labels/urgency only | Implemented | Sessions 5A-5C |
| ADR-267 | _(Phase8B5-S5-007)_ Mark reviewed clears only current `UnresolvedFeedback` review attention, removes the row from default post-close follow-up and `feedback_review` counts/views, preserves Closed status, and keeps feedback/review history in detail and closed history | Implemented | Sessions 5A-5C |
| ADR-268 | _(Phase8B5-S5-008)_ Feedback review persists request-level `FeedbackReviewedAtUtc`, `FeedbackReviewedByAccountUserId`, and optional internal `FeedbackReviewNote` capped at 2000 chars. Original feedback fields are not modified and no review status enum is added | Implemented | Sessions 5A-5C |
| ADR-269 | _(Phase8B5-S5-009)_ Mark reviewed creates an internal-only request history/timeline event with actor, timestamp, and optional note. The event remains available for historical reference, is never customer-visible, and sends no notification | Implemented | Sessions 5A-5C |
| ADR-270 | _(Phase8B5-S5-010)_ Feedback review notes are optional, trimmed, internal-only, capped at 2000 chars, and separate from the original customer feedback comment | Implemented | Sessions 5A-5C |
| ADR-271 | _(Phase8B5-S5-011)_ Unreviewed negative feedback is prominent in review surfaces. Reviewed feedback remains visible in closed history/detail as compact collapsed context by default, expandable for Owner/Admin under existing role visibility rules | Implemented | Sessions 5A-5C |
| ADR-272 | _(Phase8B5-S5-012)_ Session 5 does not add undo/reopen-review. Mark reviewed is append-only for pilot; correction/context can be recorded with internal notes while restoring review attention remains deferred | Implemented | Sessions 5A-5C |
| ADR-273 | _(Phase8B5-S5-013)_ Mark reviewed is valid only for Closed, negative, unreviewed feedback that is still in unresolved-feedback review state. Positive/missing/non-Closed/Cancelled/already-reviewed/missing-review-state cases are rejected | Implemented | Sessions 5A-5C |
| ADR-274 | _(Phase8B5-S5-014)_ Session 5 adds `POST /keep/requests/{requestId}/feedback-review` with optional `{ note }`, returning updated `KeepRequestDetailResult`. Clients may implement review-and-next with the same endpoint plus queue navigation; no separate mutation endpoint is added | Implemented | Sessions 5A-5C |
| ADR-275 | _(Phase8B5-S5-015)_ Feedback review is single-shot. Already-reviewed feedback returns conflict, does not overwrite review metadata, and does not create a duplicate timeline event | Implemented | Sessions 5A-5C |
| ADR-276 | _(Phase8B5-S5-016)_ Feedback review uses explicit state/validation errors: `FeedbackReviewUnavailable` for invalid review state, `FeedbackAlreadyReviewed` for sequential duplicates, and `FeedbackReviewNoteTooLong` for note validation. Authentication/access and OffSeason/read-only guards follow the existing Keep write convention and return `403 auth.forbidden`; Session 5 does not introduce a one-off `KeepRequest.ReadOnly` error | Implemented | Sessions 5A-5C |
| ADR-277 | _(Phase8B5-S5-017)_ OffSeason/read-only blocks customer feedback submission and mark-reviewed writes. Closed customer pages remain safely readable but omit `feedback` from `AllowedActions`; existing unreviewed feedback remains visible read-only until writes are enabled | Implemented | Sessions 5A-5C |
| ADR-278 | _(Phase8B5-S5-018)_ Future OffSeason entry should warn Owner/Admin about unresolved feedback and other open Keep work. Session 5 records the pre-flight checklist direction but does not implement OffSeason activation UI or block activation | Implemented | Sessions 5A-5C |
| ADR-279 | _(Phase8B5-S5-019)_ Feedback review exposes server-computed aging metadata for unreviewed negative feedback using centralized pilot defaults: new under 24h, aging 24-72h, overdue after 72h. API returns `feedbackReviewAgeBucket` and `feedbackReviewDueAtUtc`; thresholds can later become account settings | Implemented | Sessions 5A-5C |
| ADR-280 | _(Phase8B5-S5-020)_ Owner/Admin feedback review surfaces include customer contact context/actions, responsible person, participant involvement summary, feedback context, aging metadata, and closed/request context. Contact does not mark reviewed; full customer/address history remains deferred | Implemented | Sessions 5A-5C |
| ADR-281 | _(Phase8B5-S5-021)_ Backend owns feedback-review UI-ready metadata and action eligibility. List rows keep `review_feedback` as focused-open action; detail exposes `mark_feedback_reviewed` only when allowed. Web/native clients render server actions instead of duplicating workflow logic | Implemented | Sessions 5A-5C |
| ADR-282 | _(Phase8B5-S5-022)_ Feedback review supports context-based next/previous and optional client-side "mark reviewed and next" using the same command and current queue context. No bulk review or heavy batch workflow is added | Deferred — Session 6 | Sessions 5A-5C; navigation/next-previous not yet implemented |
| ADR-283 | _(Phase8B5-S5-023)_ Session 5 adds no customer-visible feedback replies, receipts, reopened customer conversation, or feedback follow-up delivery. Direct follow-up can happen through existing contact channels and external-contact logging | Implemented | Sessions 5A-5C; G7b Owner/Admin external-contact exception for active unresolved-feedback; build-log/058 |
| ADR-284 | _(Phase8B5-S5-024)_ Session 5 adds no reopen, callback/rework status, or linked-request model. Negative feedback never reopens automatically; follow-up work uses manually created new requests plus internal notes until formal callback/rework is designed | Implemented | Sessions 5A-5C |
| ADR-285 | _(Phase8B5-S5-025)_ Session 5 stores review metadata but adds no feedback analytics, CSAT/NPS, dashboards, public review prompts, automated campaigns, or notification delivery | Implemented | Sessions 5A-5C |
| ADR-286 | _(Phase8B5-S5-026)_ Session 5 implementation must follow existing Keep code standards: domain-owned state changes, application services, explicit API command, Result errors, OffSeason access policy, updated detail responses, internal-only events, server-computed actions, customer-page exclusion, focused tests, and implementation self-review | Implemented | Sessions 5A-5C |

| ADR-287 | _(Phase8B5-S4A-001)_ `KeepRequestListQueryBinding` is a static binder class in `OpHalo.Api.Keep` that handles HTTP structural concerns (unknown params, duplicate scalar params, GUID/integer type parsing) before passing typed `KeepRequestListQuery` to the service. Unknown or duplicate query params return explicit 400 errors; case-insensitive key normalization catches `?view=a&View=b` duplicates. `Program.cs` stays thin | Implemented | Session 4A |
| ADR-288 | _(KeepV1-001)_ Keep V1 product boundary is locked as an active request accountability loop: intake/customer pages, triage/detail/history/search, routing, external-contact logging, customer updates/replies, feedback review, ready-to-close/stale queues, basic push/badges, and spam/test handling are in; SSE/WebSockets, backend SMS, full notification platform, attachments, branded URLs, identity-bound portals, full analytics/SLA, archive, full signal engine, internal memory search, and formal rework/callback are out | Locked | build-log/043 |
| ADR-289 | _(KeepV1-002)_ Basic native push and server-derived badges are V1 core. Add a small account-user device/notification-device model, minimal APNs/FCM push with deep links, actor exclusion, eligibility/routing/mute/OffSeason suppression, and fail-soft delivery. Full preferences, quiet hours, notification rows/outbox/retries/dead-letter, delivery analytics, and email/push channel matrices remain deferred | Locked | build-log/043; DEF-012; DEF-021 |
| ADR-290 | _(KeepV1-003)_ V1 is fresh, not realtime: no SSE/WebSockets/live list streaming. Freshness comes from refetch-after-write, focus/resume sync, pull-to-refresh, 30-60s active-list polling, server-derived counts/badges, and push/deep links for urgent off-screen work | Locked | build-log/043; DEF-017 |
| ADR-291 | _(KeepV1-004)_ V1 contact posture uses native launchers plus explicit logs: `tel:`, `mailto:`, and `sms:` may open device apps, but launching does not prove contact or change state. Only explicit external-contact logs create durable audit/first-response/attention effects. Keep does not send platform SMS or ingest SMS replies in V1; avoiding automated SMS also avoids A2P 10DLC/TCPA/STOP-loop compliance until a future platform-SMS decision explicitly reopens it | Locked | build-log/043; DEF-022 |
| ADR-292 | _(KeepV1-005)_ Go-live coding order: finish Session 4 list navigation, Session 5 feedback review, Session 6 closeout hygiene, V1 notification/device foundation, spam/test handling, then web/native UI freshness and pilot QA. Do not pull in realtime streaming, backend SMS, full notification platform, attachments, branded URLs, customer identity portals, or full analytics/reporting before go-live | Locked | build-log/043 |
| ADR-293 | _(PilotSupport-001)_ V1 includes a 1-tap in-app `Report friction` hook for pilot users to send bugs, confusion, missing needs, and slow/frustrating moments without leaving the app. Client posts to an authenticated OpHalo API endpoint; backend forwards compact context to a private founder/internal channel. No direct client webhook, no heavy ticketing/helpdesk dashboard, no broad logs/customer PII auto-attached, and delivery is fail-soft | Locked | build-log/044 |
| ADR-294 | _(PilotSupport-002)_ V1 includes an in-app `Pilot Updates` / `Help & Updates` page with Known Issues, What's New, Coming Soon, and Report Friction entrypoint. Content is founder-maintained plain markdown/JSON or equivalent simple API-served content; no CMS, public status page, roadmap portal, feature voting, or admin editor before pilot go-live unless manual updates become a proven blocker | Locked | build-log/044 |
| ADR-295 | _(Gap-001/015, revised 2026-06-20)_ Keep onboarding automatically provisions one durable public intake link after the business profile/name exists, through the Keep setup boundary rather than direct Foundation registration coupling. Exactly one current link exists per account. It is never rotated automatically; business-name changes do not invalidate it. V1 has no ordinary Owner/Admin pause/resume control or `PausedAtUtc`: the distributed URL is a customer-acquisition asset and normal operation continues capturing requests. Account/access/OffSeason gates remain system-level exceptions. Owner/Admin permanent replacement is exceptional recovery, atomically revokes the old link and creates the successor in one database transaction, returns the new raw URL once, invalidates the old URL, and requires an explicit stale-link warning | Locked | build-log/047; session-log G3 working decision |
| ADR-296 | _(Gap/DEF-061, revised 2026-06-20)_ Before pilot, Owner/Admin can classify requests distinctly as `Spam` or `Test`. Classification is confirmed, may include a short internal reason, preserves auditable history, removes the request from operational queues, disables further customer-page activity, and excludes it from response-time, intake, stale-work, and impact metrics. Operators cannot classify in V1. Pre-pilot abuse posture also requires bounded validation, trusted-IP/rate-limit proof, token-safe logs, and an internal emergency runbook. Customer-facing source blocking is not pre-pilot scope. Expiring keyed-hash source blocks, adaptive Turnstile, duplicate/pattern detection, anomaly alerts, and optional phone verification remain post-pilot/V1.1 candidates to pull before broad launch only if pilot evidence justifies them; raw customer IP is not exposed to businesses | Locked | build-log/047; DEF-061; session-log G8 |
| ADR-297 | _(Gap-011)_ Customer pages expire 30 days after a request enters `Closed` or `Cancelled`; active and `Resolved` requests do not expire. This restores the reference product's 30-day-from-terminal-event contract while retaining ADR-120's terminal-only enforcement. `Spam`/`Test` classification disables customer-page activity immediately | Implemented | build-log/047; build-log/057 (Cancelled expiry); Closed expiry: Session 6; Spam/Test: S7d/S7e; reference customer-request-page model |
| ADR-298 | _(Gap-014)_ Greenfield Keep removes the undeployed `/continuity/public-intake/...` alias and accepted-but-ignored `emailNotificationsEnabled` request field before frontend implementation. Notification consent will be introduced only alongside real behavior and approved semantics | Locked | build-log/047; supersedes ADR-051 and part of ADR-059 |
| ADR-299 | _(Gap-005)_ Keep persistence enforces account, customer, request, event, participant, policy, and intake-link relationships with database foreign keys. Account-aware composite relationships prevent cross-account references where simple IDs are insufficient; restricted deletion preserves history; scalar IDs do not require convenience navigation properties; PostgreSQL integration tests prove cross-account rejection | Locked | build-log/047 |
| ADR-300 | _(Gap-020)_ ADR-263 remains the policy: positive feedback comments are visible to authenticated users who pass request-row access, while full negative comments remain Owner/Admin-only and lower roles receive safe metadata only. The mapper's current all-comments restriction is an implementation defect to correct | Implemented | build-log/047; build-log/058 |

| ADR-301 | _(Gap-G1-D1)_ Composite alternate keys `KeepCustomer (AccountId, Id)`, `KeepRequest (AccountId, Id)`, and `AccountUser (AccountId, Id)` enable composite FK references from Keep entities to principals in their own and other tables, preventing cross-account graph wiring at the database level | Implemented | build-log/048 |
| ADR-302 | _(Gap-G1-D2)_ Composite AccountUser FKs (all nullable, Restrict) added to: `KeepRequest` (FirstResponder, AttentionClearedBy, FeedbackReviewedBy), `KeepRequestEvent` (Actor, ParticipationTarget, PreviousResponsible, NotificationIntendedRecipient), `KeepRequestParticipant` (AccountUserId). Prevents cross-account user references; `KeepRequestParticipant` with a non-existent AccountUserId is prohibited — stale participants use an ineligible-role user, not a missing one | Implemented | build-log/048 |
| ADR-303 | _(Gap-G1-D3)_ `LastBusinessActivityAt` is `DateTime?`; API exposes factual null. Creation semantics: `CreateFromCustomerIntake` sets `LastCustomerActivityAt=now, LastBusinessActivityAt=null`; `CreateByBusiness` is the inverse. Ranking fallback: `LastBusinessActivityAt ?? LastCustomerActivityAt ?? CreatedAtUtc` | Implemented | build-log/048 |
| ADR-304 | _(Gap-G1-D4)_ `PhoneNormalizer.Normalize` strips all non-ASCII-digit chars; 7–15 digit length enforced by `KeepCustomer.Create`. Stores `PrimaryPhone` (display) and `CanonicalPhone` (identity, max 15). Account-scoped unique index moves from `(AccountId, PrimaryPhone)` to `(AccountId, CanonicalPhone)` | Implemented | build-log/048 |
| ADR-305 | _(Gap-G1-D5)_ `KeepRequest.Create` removed; replaced by `CreateFromCustomerIntake` (10 params, sets customer activity, wires first-response timer) and `CreateByBusiness` (9 params, no firstResponseTargetMinutes, sets business activity). Both share a private `CreateCore` | Implemented | build-log/048 |

| ADR-306 | _(Gap-G2-D1)_ Public intake validation runs fail-fast in strict order before any token lookup or DB access: required fields → maximum lengths → phone character check → phone digit count → email syntax. Validation errors return named 400s without exposing account/token state; no DB mutation occurs on any validation failure | Implemented | build-log/049 |
| ADR-307 | _(Gap-G2-D2)_ Phone character allowlist: ASCII digits, space, `-`, `(`, `)`, `.` are allowed anywhere; `+` is only allowed as the first character (international prefix). Letters and all other symbols are rejected as `CustomerPhoneInvalidCharacters`. Digit-count out of range (< 7 or > 15 after normalization) is a separate `CustomerPhoneInvalidFormat` error | Implemented | build-log/049 |
| ADR-308 | _(Gap-G2-D3)_ `KeepCustomer.UpdateContactInfo` email preservation: always updates the trimmed name; replaces email only when the incoming value is nonblank after trimming. Null or blank email is not a clear-email command — it preserves the existing known email. Applies to both repeat-intake and post-collision re-read paths | Implemented | build-log/049 |
| ADR-309 | _(Gap-G2-D4)_ Concurrent customer-identity race is recovered by catching `ix_keep_customers_account_canonical_phone` (23505) in `CommitPublicIntakeAsync`, returning `CustomerCanonicalPhoneCollision`, detaching all failed entities, re-reading the winning customer, applying safe contact-update rules, and retrying. Customer and token collisions share the same five-attempt ceiling; unrelated DB failures propagate | Implemented | build-log/049 |
| ADR-310 | _(Gap-G2-D5)_ `EmailAddressAttribute` (System.ComponentModel.DataAnnotations) is used for public intake email format validation rather than `MailAddress` (which is a parser and accepts display-name forms not appropriate for a plain email field) | Implemented | build-log/049 |

| ADR-311 | _(Gap-G3a-D1)_ Intake-link setup endpoints (`GET /keep/setup/intake`, `POST /keep/setup/intake/ensure`, `POST /keep/setup/intake/replace`) require `keep.settings.manage` (Owner/Admin only), use `RequestImplementsAllowedInOffSeason: true` (setup stays accessible in OffSeason), and check only `decision.IsBlocked` — not `IsReadOnly` | Implemented | build-log/050 |
| ADR-312 | _(Gap-G3a-D2)_ `EnsureAsync` is idempotent: fast-path returns existing link without issuing a token; concurrent losers catch `ix_keep_public_intake_links_account_active` (23505) → re-read winning link → return `created=false, rawToken=null`. `ReplaceAsync` uses two `SaveChanges` inside `BeginTransactionAsync`/`CommitAsync`; on failure the transaction auto-rolls back, preserving the old active link | Implemented | build-log/050 |
| ADR-313 | _(Gap-G3a-D3)_ Slug generation is kebab-case from business name (lowercase, non-alphanumeric → `-`, collapse, truncate at 60); numeric suffix loop resolves collisions; `SlugExistsAsync` scopes to active slugs matching the unique index filter, so revoked slugs can be reused | Implemented | build-log/050 |
| ADR-314 | _(Gap-G3a-D4)_ `KeepPublicIntakeLink.Create` and `.Revoke` gain optional `createdByUserId` / `modifiedByUserId` params (backward-compatible; all existing callers compile unchanged). No migration needed — `BaseEntityConfiguration` already maps `CreatedByUserId` and `ModifiedByUserId` | Implemented | build-log/050 |

| ADR-315 | _(Gap-G3b-D1)_ Two-interface pattern for business-request creation: `IKeepRequestOperatePersistence` provides auth snapshots and actor lookup; `IKeepBusinessRequestPersistence` provides customer find, token existence checks, and commit — separated so the commit path is not coupled to the operate path and `KeepIntakeCommitHelper` can be shared with the public intake path | Implemented | build-log/051 |
| ADR-316 | _(Gap-G3b-D2)_ `POST /keep/requests` creates a business-origin request; returns `201 Created` with `KeepRequestDetailResult` (no wrapper). Location header: `/keep/requests/{requestId}` | Implemented | build-log/051 |
| ADR-317 | _(Gap-G3b-D3)_ Viewer role is rejected immediately after userSnapshot load (before account/feature/OffSeason checks); Owner/Admin/Operator proceed through the standard permission/access/feature stack | Implemented | build-log/051 |
| ADR-318 | _(Gap-G3b-D4)_ Business-created requests use `RequestImplementsAllowedInOffSeason: false` and check `decision.IsBlocked || decision.IsReadOnly` — OffSeason blocks write intent. Feature gate: `FeatureKeys.Keep.OperatorQueue`. Actor event uses 5-param `KeepRequestEvent.CreateRequestCreated` with `Visibility=System`, `ActorType=AccountUser`, and the authenticated `AccountUserId`/display name | Implemented | build-log/051 |

| ADR-319 | _(Gap-G4-D1)_ Keep authorization separates account/feature/role permission, request-row visibility, and action-specific permission. Passing a broader gate never implies passing a narrower one; UI action metadata is advisory and server endpoints remain authoritative | Locked | build-log/047; session-log G4 |
| ADR-320 | _(Gap-G4-D2)_ G4 row policy: Owner/Admin have account-wide operational visibility; Viewer is a trusted account-wide read-only observer with no write affordances; Operator full-detail visibility requires active eligible Responsible or Watching participation. Cross-account and same-account-invisible direct IDs return `404 Not Found` | Locked | build-log/047; session-log G4 |
| ADR-321 | _(Gap-G4-D3)_ Operator Available work remains a distinct, intentional discovery surface rather than entering Default or Needs Attention. It contains only active, eligible, effectively unassigned requests and exposes a privacy-limited summary. Full customer detail, internal notes, and timeline require an explicit audited self-assign or watch action; opening a read endpoint never silently changes participation or ownership | Locked | build-log/047; session-log G4; reaffirms ADR-227/240 |
| ADR-322 | _(Gap-G4-D4)_ G4 uses a small explicit policy vocabulary selected by Application services and translated to composable EF queries in Infrastructure; it is not an implicit/global EF query filter. Participation/eligibility predicates are defined once and reused by lists, counts, detail, and tracked mutation loads. Unknown roles and stale/detached/ineligible participation fail closed | Locked | build-log/047; session-log G4 |
| ADR-323 | _(Gap-G4-D5)_ Row visibility and action permission remain separate shared policies. Available-summary visibility is not general mutation authority; action metadata and mutation endpoints consume the same action-rule primitives, while endpoints re-evaluate current server state and may reject stale UI intent | Locked | build-log/047; session-log G4 |
| ADR-324 | _(Gap-G4-D6)_ Operator Available uses a dedicated `GET /keep/requests/available` contract rather than returning a role-dependent shape from `view=unassigned`. The summary includes customer name for field recognition plus bounded operational context and explicit self-assign/watch affordances; it withholds phone, email, full description, internal history, page token/link, feedback, participation, and notification details. Existing `view=unassigned` remains the Owner/Admin full-summary surface | Locked | build-log/047; session-log G4 |
| ADR-325 | _(Gap-G4-D7)_ Named row scopes are capability-oriented: `AccountWide`, `MyWork`, and the narrow `ParticipationEntry` scope used only by audited Operator self-assign/watch transitions. A standalone `KeepRequestActionPolicy` owns shared action rules; authorization logic does not move into the detail mapper. G4 is delivered in bounded migration slices, and the old account-only mutation loader remains obsolete until all callers migrate, then is removed before completion | Locked | build-log/047; session-log G4 |

| ADR-326 | _(Gap-G4e-D1)_ `KeepRequestActionPolicy` is a pure O(1) Application-layer policy. It accepts the request plus an explicit role, server-derived `CanWrite`, current active participation, and nullable notification state; it has no EF, current-user, HTTP, network, or clock dependency. Unknown/future roles, Viewer, disabled writes, and inconsistent actor context return an immutable deny-all decision. Row authorization and concurrency remain separate | Locked | session-log G4e |
| ADR-327 | _(Gap-G4e-D2)_ The shared action decision owns operational, responsibility, watcher/notification, feedback-review, and ordered enum-valued status-transition primitives. Terminal state is capability-specific rather than blanket deny-all: internal notes, active-attention cleanup, and qualifying Closed unresolved-feedback review remain possible. `AllowedStatuses` means actual transitions and excludes current status; existing same-status no-op/message command semantics remain domain-authoritative | Locked | session-log G4e |
| ADR-328 | _(Gap-G4e-D3)_ One Application mapper converts the shared decision to `AvailableActionsMetadata`; services may not reconstruct its booleans independently. Existing auth/account/feature/role/row gates remain authoritative, while domain methods own executable mutation eligibility, validation order, same-status semantics, and stable errors. Mutation services do not add a coarse policy-boolean rejection that would duplicate domain rules, replace role-before-row or target-specific participation checks, or flatten established outcomes to generic 403; after successful mutation or valid no-op they evaluate the resulting request and actor-participation state for response metadata. Lists consume the same decision for write affordances while row-open, contact-data presence, overdue-response suppression, ranking, and labels remain read/presentation concerns. The Operator-only Available summary (`GetAvailableKeepRequestsService`) is a narrow-projection exception: it does not load full entities or surface `AvailableActionsMetadata`, but must still reproduce the equivalent coarse policy primitives over its bounded row — `CanSelfAssign` reduces to `canWrite` (ApplyAvailable guarantees non-terminal, no-eligible-Responsible rows on which the eligible viewer is never the Responsible), and `CanWatch = canWrite && !CurrentUserIsWatching` using an internal SQL-computed, never-serialized Watching flag so an already-Watching viewer is denied the watch affordance exactly as the policy would | Locked | session-log G4e; clarified before G4e-2/G4e-3; Available-projection parity added in G4e-3 |
| ADR-329 | _(Gap-G4e-D4)_ G4e ships in three bounded, sequentially reviewed batches: policy/read surfaces; operational mutations; participation/feedback plus cleanup. Completion requires matrix and parity tests, zero superseded helper callers, zero duplicated action-metadata construction, a complete G4 path inventory, and green build/unit/architecture/integration/full suites. Spam/Test remain separate later V1 classifications under DEF-061, not Keep lifecycle statuses or G4e capabilities | Locked | session-log G4e |
| ADR-330 | _(Gap-G5-D1)_ `KeepRequest` optimistic concurrency uses a required application-managed `Guid ConcurrencyVersion`, stored as PostgreSQL `uuid`, configured with `IsConcurrencyToken()`/`ValueGeneratedNever()`, initialized with a random nonempty GUID for new rows, and uniquely backfilled with `gen_random_uuid()` before the migration makes the column non-null. The opaque API `version` has no ordering semantics; do not use `xmin`, timestamps, counters, triggers, or a database default | Implemented | session-log G5; build-log/056 |
| ADR-331 | _(Gap-G5-D2)_ A KeepRequest row, its events, and its participants are one concurrency aggregate. Every successful non-no-op request, event, customer, feedback, responsible, watcher, self-watch, or mute mutation rotates the request version in the same atomic commit; event-only and participation-only paths must carry the tracked request. Reads, failures, and valid idempotent no-ops do not rotate, and G5 has no current append-only exclusions | Implemented | session-log G5; build-log/056 |
| ADR-332 | _(Gap-G5-D3)_ Every existing-request mutation requires exactly one strict `X-Keep-Request-Version` GUID-D header, parsed explicitly at the API handler and passed as a typed `Guid`; missing returns `400 KeepRequest.ExpectedVersionRequired`, while blank, malformed, empty, duplicate/comma-combined, wildcard, quoted, or braced values return `400 KeepRequest.ExpectedVersionInvalid`. Creates require no header, Application/Core remain transport-neutral, and CORS permits the header | Implemented | session-log G5; build-log/056 |
| ADR-333 | _(Gap-G5-D4)_ Authenticated detail/list/Available/business-created detail, customer page, and all successful mutation responses expose opaque `version`. Authorized row access precedes expected-version comparison so invisible/cross-account requests remain 404; customer token/account/expiry/OffSeason guards likewise precede comparison. A changing success returns the rotated version, a no-op returns the unchanged version, and errors—especially conflicts—do not reveal a current version or request state | Implemented | session-log G5; build-log/056 |
| ADR-334 | _(Gap-G5-D5)_ Application compares expected version after authorized tracked load and before domain mutation; persistence rotates immediately before atomic save and converts only `DbUpdateConcurrencyException` into a typed `Conflict` commit outcome. Both a pre-mutation stale value and a save-time race map to `409 KeepRequest.RequestChanged` (`The request changed. Refresh and try again.`) with no side effects, retry, reload/reapply, merge, or lock; unrelated database failures propagate normally | Implemented | session-log G5; build-log/056 |
| ADR-335 | _(Gap-G5-D6)_ G5 ships as four independently compiling, focused-test batches: G5a foundation/migration/header parser/response exposure; G5b operational mutations; G5c participation mutations; and G5d customer writes, cross-path races, full regression, and completion docs. Verification covers strict header errors, current/stale/no-op behavior, preserved 404/domain-error ordering, two-DbContext and cross-path first-writer-wins races with no losing side effects, every version-bearing response, and new/upgrade migration behavior | Implemented | session-log G5; build-log/056 |
| ADR-336 | _(PilotScope-001)_ Business-first request capture is a pilot must-have, not optional polish. Keep must let Owner/Admin/Operator create a request immediately from a phone call, voicemail, text, email, walk-in, referral, or other customer contact; the customer request page/intake link is optional enrichment and collaboration, never the gate for work to enter Keep. Pilot PWA/native UI must make this fast and prominent (minimal customer name/phone + issue, then optional share customer link) so Keep remains the single accountability loop even when the customer will not fill out a form | Locked — backend implemented | ADR-315..318; build-log/051; DEF-076 |
| ADR-337 | _(Phase8B5-S6P-001)_ Follow Up On is staff-owned active-request communication follow-up, not scheduling or a general callback tool. V1 is date-only; Owner/Admin and operating Operators can set it with reason weather/parts/customer delay/business-operator availability/third party/other, note required for other. It suppresses stale/status-check until due, shows scan-friendly markers, resurfaces as follow-up attention on/after the date, and never automatically notifies the customer, though the UI should make explicit customer update/contact easy | Locked | build-log/060; DEF-067 |
| ADR-338 | _(Phase8B5-S6P-002)_ Planned For is lightweight staff-owned active-request timing context, not dispatch, scheduling, booking, or customer self-service appointment management. V1 is optional/date-only/internal by default, customer cannot edit it, and it supports scan/priority labels such as Planned today/tomorrow/Fri/date passed. Planned-date changes do not automatically notify the customer, but explicit customer updates should be easy | Locked | build-log/060 |
| ADR-339 | _(Phase8B5-S6P-003)_ Needs-status-check is an account-policy-driven quiet human-review queue with a 5-calendar-day pilot default when unset. It applies only to active non-Resolved/non-terminal requests with no active business attention, no future Follow Up On, and no future Planned For. Latest meaningful activity must be centralized and signal-extensible, starting with created, customer intent/message, business update, external contact, status change, Follow Up On changes, and Planned For changes; no auto-close/resolve/customer update | Implemented | build-log/060; P6d-1, P6d-2A |
| ADR-340 | _(Phase8B5-S6P-004)_ Push notifications are reserved for immediate/time-sensitive routed work; badges/counts and the server-ranked request list carry personal actionable backlog and prioritization. P6e locks staff/operator notification boundaries: smallest-accountable-audience routing, actor/mute/OffSeason/stale-participant suppression, post-commit fail-soft candidate generation outside domain methods, minimal non-sensitive deep-link payloads, fresh-not-offline client posture, and no automatic customer-facing platform notifications. APNs/FCM push is a basic developer-configuration/cost posture, distinct from the A2P 10DLC/TCPA/opt-out burden of automated backend customer SMS/email. Delivery/device/outbox/preference implementation remains later | Implemented | build-log/060; DEF-012; DEF-021 |
| ADR-341 | _(Phase8B5-S6P-005)_ Customer page viewed is pulled forward as a pre-pilot confidence/adoption signal, not presence or a read receipt. Expose cautious metadata such as Last viewed, Viewed after latest update, and Never viewed; debounce/rate-limit page views; do not raise business attention from views alone; make it available for future signal logic but do not let it suppress stale/status-check without a later decision | Implemented | build-log/060; DEF-030; ADR-162 |
| ADR-342 | _(Phase8B5-S6P-006)_ Customer page actions use simplified customer-language intent buttons with shared customer-message/event handling: Ask a question, Ask for an update, Send info, Call me, Need to change timing, Need to cancel?. Suggested intent codes are question, update_request, information_added, call_requested, timing_change_requested, cancellation_requested. None directly mutate lifecycle, scheduling, cancellation, closeout, or Follow Up On; Need to cancel? is secondary and should prompt for note/confirmation | Implemented | build-log/060; ADR-088; ADR-121 |
| ADR-343 | _(Phase8B5-S6P-007)_ Close permission is account-policy-shaped with Owner/Admin default. Operators mark work complete as Resolved; Owner/Admin close by default. A later account policy may allow Operators to close requests they can operate/are responsible for. Backend design must not block this future setting; closing still requires row access, expected version, eligible state, no active blocking attention, enables feedback, and starts 30-day terminal page expiry | Locked | build-log/060; ADR-193; ADR-297 |
| ADR-344 | _(Phase8B5-S6P-008)_ Phase 8-B5 Session 6 proper is paused behind Session 6 prerequisites. Prerequisites own Follow Up On, Planned For, needs-status-check semantics, notification boundaries, customer page viewed, and customer intent menu refinement. Session 6 closeout resumes after prerequisites with ready-to-close, closeout/Closed expiry, context navigation, and final ledger work; archive, dispatch/calendar scheduling, customer self-scheduling, broad analytics, backend SMS, realtime, and spam/test remain out | Locked | build-log/060 |
| ADR-345 | _(S7-001)_ Persistent local PostgreSQL runbook is required before notification delivery. The runbook must cover applying migrations, running a small production-like smoke check, and safely resetting local pilot/dev data. Broad demo seed data is excluded from normal setup and deferred behind Demo/InternalTest account classification plus safe reset and role-demo rules | Locked | build-log/062; DEF-079; DEF-080 |
| ADR-346 | _(S7-002)_ Rate limiting requires automated production-like proof outside the normal `Testing` host path. Standard integration tests may continue skipping runtime rate limiting, but Session 7 must add a focused smoke/integration path with tiny deterministic thresholds and `429` assertions for protected public/auth routes | Locked | build-log/062 |
| ADR-347 | _(S7-003)_ Trusted client IP resolution must be explicit behind Cloudflare/Railway for rate limiting and internal abuse/security logging. Trusted Cloudflare country may be captured/used as optional internal context only; V1 does not expose raw IP/location to businesses or make broad authenticated access decisions from country | Locked | build-log/062 |
| ADR-348 | _(S7-004)_ Token-bearing flows are secret-safe and supportable. Logs, errors, request logging, and test output must not include raw public intake tokens, customer page tokens, invite tokens, auth/session/bearer credentials, exchange codes, raw token URLs, or future notification secrets; diagnostics may use route templates, correlation IDs, reason codes, entity IDs after lookup, expiry/rotation metadata, and stable keyed fingerprints | Locked | build-log/062 |
| ADR-349 | _(S7-005)_ Spam/Test classification is V1 pre-pilot scope before notification delivery. Owner/Admin may classify requests through an explicit confirmed action with optional internal reason; classification removes operational work, disables customer-page activity, excludes operational/impact metrics, and preserves audit/history. Operators cannot classify in V1; deletion, source blocking, adaptive bot checks, phone verification, and abuse dashboards remain later unless evidence pulls them forward | Locked | build-log/062; DEF-061 |
| ADR-350 | _(S7-006)_ `Spam` and `Test` are explicit terminal request statuses, not cancellation reasons layered onto `Cancelled`. They remove the request from operational queues, disable customer-page activity, preserve audit/history, and are excluded from operational/impact metrics; `Test` remains distinct for onboarding, demos, training, and QA | Locked | build-log/062; DEF-061 |
| ADR-351 | _(S8-001)_ Session 8 notification work is staff push/badge infrastructure, not customer contact delivery. Native `tel:`, `sms:`, and `mailto:` launchers help staff contact customers; APNs/FCM push wakes staff when Keep needs attention off-screen. Keep still sends no backend customer SMS/email in V1 | Locked | build-log/063 |
| ADR-352 | _(S8-002)_ First Session 8 delivery slice uses a push adapter abstraction plus no-op/test delivery, not real APNs/FCM. The interface must still support real-provider semantics: target token/platform, minimal deep-link payload, collapse/thread key, priority, TTL/expiry, and delivery result/failure shape | Locked | build-log/063 |
| ADR-353 | _(S8-003)_ Demo/InternalTest notification suppression is a hard gate before real APNs/FCM delivery, but not a blocker for the no-op foundation. Session 8 leaves a delivery-eligibility check point; account classification/scenario packs remain deferred | Locked | build-log/063; DEF-079; DEF-080 |
| ADR-354 | _(S8-004)_ V1 notification hooks are explicit application-layer post-commit calls after successful persistence/version rotation. Domain entities do not know about notifications; no EF SaveChanges interceptor, domain-event bus, or multi-channel dispatcher is added in Session 8. Before real delivery, a P6e push-worthy coverage checklist/test suite must prevent drift | Locked | build-log/063 |
| ADR-355 | _(S8-005)_ Notification devices use a dedicated AccountUser-scoped table, not AccountSession. Store raw push token from day one plus safe fingerprint/last-four, status Active/Revoked/FailedPermanent, created/last-seen/revoked/failure metadata, and token-to-user rebinding revocation; raw tokens are never logged or returned | Locked | build-log/063 |
| ADR-356 | _(S8-006)_ Device APIs are personal `/me/devices/{appInstallationId}` endpoints. PUT registers/upserts by `(AccountUserId, AppInstallationId)` with strict install-id validation, required platform/token, platform mismatch rejection, and no OCC header; DELETE idempotently revokes and returns 204. OffSeason does not block registration | Locked | build-log/063 |
| ADR-357 | _(S8-007)_ `GET /me/badge` returns a server-derived personal OS badge interrupt count, not unread or workload ledger. Response is `{ count, computedAtUtc }`. OffSeason returns 0; count visible active attention and Closed+UnresolvedFeedback Owner/Admin exception; muted rows still count; needs-status-check, future timing, ready-to-close, passive metadata, and history do not | Locked | build-log/063 |
| ADR-358 | _(S8-008)_ Push payloads and display text are non-sensitive enum-derived hints. Payloads may contain type, accountId, requestId, eventKind, deep link, badge, collapse/thread, priority, and TTL metadata; they must not include customer/request/message/internal/feedback/token data. Visible text is a static server-side eventKind mapping with no interpolation | Locked | build-log/063 |
| ADR-359 | _(S8-009)_ V1 push routing is event-type driven; Planned For remains list triage. Push by default for new first-response request, call/cancellation/timing-change intents, assignment/transfer to you, unresolved negative feedback to Owner/Admin, and due/overdue Follow Up On. Ordinary question/update/info/customer-message, future timing, stale/status-check, ready-to-close, page-view metadata, and notes are badge/list-only unless the domain raises a push-worthy reason | Locked | build-log/063 |
| ADR-360 | _(S8-010)_ Request mute suppresses push only. It does not remove rows from badge count or list visibility. Push routing skips a muted Responsible recipient and falls through to eligible unmuted Watchers then Owner/Admin fallback; no V1 urgent mute override or suppression log | Locked | build-log/063; DEF-077 |
| ADR-361 | _(S8-011)_ Badge count is fetched through `GET /me/badge`, not added to mutation/detail/list DTOs in Session 8. Clients refresh badge on foreground/resume, after successful mutations, and after opening push. Native push badge value is a delivery-time hint only | Locked | build-log/063 |
| ADR-362 | _(S8-012)_ Session 8 stores devices, not notification history. No notification ledger, candidate table, delivery table, outbox/retry/dead-letter, suppression log, or analytics. Device-row failure metadata is enough for V1; `LastSeenAtUtc` updates only on registration/upsert, not delivery attempts | Locked | build-log/063; DEF-012; DEF-021 |
| ADR-363 | _(S9-001)_ Account classification replaces `AccountEntitlements.IsPilot` as the single source of truth. Add `AccountClassification` values `Production=1`, `Pilot=2`, `Demo=3`, `InternalTest=4` on `AccountEntitlements`; classification is operational/reporting/safety posture, separate from commercial lifecycle. EF stores classification as string. Migration maps `IsPilot=true` to Pilot, real `IsPilot=false` accounts to Production, internal-purpose accounts to InternalTest, then drops `is_pilot` | Implemented | build-log/064 |
| ADR-364 | _(S9-002)_ `SignupDefaultsSettings` sets account classification, not a pilot boolean. Config uses `SignupDefaults:Classification` with current pilot default `Pilot`; `TrialDurationDays` remains separate; `MaxPilotAccounts` applies only when classification is Pilot. Public signup may create only Pilot or Production accounts; Demo/InternalTest require future explicit admin/internal creation flows | Implemented | build-log/064 |
| ADR-365 | _(S9-003)_ Pilot capacity checks at `/auth/start` and `/auth/exchange` continue only when signup classification is Pilot and `MaxPilotAccounts` has a value. Count source changes from `IsPilot=true` to `Classification=Pilot`; conservative V1 behavior remains that cancelled/expired pilot accounts are not excluded unless a later decision changes it | Implemented | build-log/064 |
| ADR-366 | _(S9-004)_ Demo/InternalTest suppress production push delivery. Before real APNs/FCM can be enabled, production push delivery must be gated by account classification: Production and Pilot are delivery-eligible; Demo and InternalTest are not. Session 9 installs the gate in the device-delivery path without implementing a real provider, notification ledger, analytics, or preferences | Implemented | build-log/064; DEF-079 |
| ADR-367 | _(UX)_ Brand architecture: OpHalo is a branded house with product accents. Differentiation runs on two axes that must not be conflated — brand axis (OpHalo vs Keep vs future product; varies only by accent + personality over a shared foundation) and surface axis (workbench vs field app vs customer pages; varies only by density/form factor, not brand). Keep Web and Keep Mobile are the same Keep identity at two densities, not two app looks. Future products are thin expression layers over the shared system | Locked | ux-design/ux-design-decisions.md |
| ADR-368 | _(UX)_ Type-and-color system lock: Source Serif 4 headlines over Inter body (Poppins = wordmark only); serif-over-sans is what separates a finished surface from a wireframe. Terracotta `#BF6B43` added as parent accent token `--ophalo-accent` (navy stays structural anchor); attention nudged burnt-orange `#B85B16` → amber `#C8741A` for distinctness from terracotta; gold `#c99a34` removed and `--primary` mapped to navy. Supersedes BRAND.md §5 (corrected to wordmark-only) | Locked | ux-design/ux-design-decisions.md; ux-design/ux-design-model-v1.md |
| ADR-369 | `KeepRequestSource` enum: V1 channel values are `Phone=1`, `Voicemail=2`, `Text=3`, `Email=4`, `WalkIn=5`, `Referral=6`, `PublicIntake=7`, `Other=8`. Stored as string in DB. `Source` is nullable on `KeepRequest` (existing rows = null). Required at API for business-created requests; `PublicIntake` is blocked for staff — set automatically by `CreateByCustomer`. Not customer-facing | Locked | build-log/065-session-11-quick-capture-backend-contract.md |
| ADR-370 | `NeedsShare` flag on `KeepRequest`: defaults `true` for `Origin=Business`, `false` for `Origin=Customer`. Cleared only by explicit in-app share intent actions (native SMS/share handoff, copy tracker link, or manual Mark Shared). Reading detail or customer page view does not clear it. `ClearNeedsShare()` domain method; no timestamp in V1 | Locked | build-log/065-session-11-quick-capture-backend-contract.md |
| ADR-371 | Session 11 batch split: S11a (Source/NeedsShare creation + detail response, 10 production files + migration) and S11b (list summary indicators + share intent clearing) are independently compiling vertical slices | Locked | build-log/065-session-11-quick-capture-backend-contract.md |
| ADR-372 | Share intent clearing contract: `POST /keep/requests/{id}/share-intent` accepts `{ method: "copy_link" \| "native_share" \| "manual_mark_shared" }`, clears `NeedsShare`, and persists an internal `ShareIntentRecorded` request event with actor, method, and timestamp. Owner/Admin can clear account-visible rows; Operators can clear MyWork-visible rows; Viewer and OffSeason/read-only access return explicit 403 errors. The endpoint is idempotent and does not affect list ranking/sort in S11b | Implemented | build-log/065-session-11-quick-capture-backend-contract.md |

| ADR-373 | _(S12-001)_ `KeepBusinessProfile` satellite entity in `OpHalo.Keep.Core` holds Keep-specific customer-facing contact: `CustomerFacingPhone` nullable and `CustomerFacingEmail` nullable. One row per account via unique account FK. `Account.BusinessName` and `Account.TimeZone` remain in Foundation because they are account-wide and product-neutral. Customer-facing contact belongs to Keep because it powers Keep intake and customer pages. Future products may have different public contact rules. `Account` stays free of product-specific assumptions | Implemented | build-log/066-session-12-account-settings-and-onboarding.md |
| ADR-374 | _(S12-002)_ `PUT /keep/setup/policy` is an upsert: create `KeepResponsePolicy` if missing, update if present. Policy is required operational configuration that drives attention timing, not a user-created collection resource. Add `KeepResponsePolicy.Update(...)` for in-place mutation. Add `StatusCheckThresholdDays` as required fourth policy field with V1 default of 5 calendar days | Implemented | build-log/066-session-12-account-settings-and-onboarding.md |
| ADR-375 | _(S12-003)_ Onboarding checklist is derived from durable product-ops event rows; no separate checklist state entity in V1. `GET /keep/setup/onboarding` translates event rows into step-complete status at read time. The event log is the onboarding source of truth per INT-002 | Locked | build-log/066-session-12-account-settings-and-onboarding.md |
| ADR-376 | _(S12-004)_ S12 batch split: S12a implements `KeepBusinessProfile`, `KeepResponsePolicy` extension, GET/PUT setup settings API, and migration; S12b implements `KeepProductOpsEvent`, onboarding signal recording, and GET checklist endpoint. Commit separately | Locked | build-log/066-session-12-account-settings-and-onboarding.md |
| ADR-377 | `web/ophalo-app` is the authenticated Keep/PWA workbench and uses Vite + React + TypeScript; `web/ophalo-web` is the public/marketing/auth-entry/customer surface and uses Next.js + React + TypeScript. Operational freshness uses TanStack Query with refetch-after-write, focus/resume sync, explicit invalidation, and limited active polling; no SSE/WebSockets for V1 | Locked | [ADR-377](ADR-377-web-client-surface-and-technology-stack.md) |
| ADR-378 | Pilot auth uses magic link for entry/recovery plus durable opaque sessions for daily browser/PWA use. Browser/PWA sessions are 30-day inactivity / 60-day absolute, with continuous per-request authorization. Production cookie topology uses `.ophalo.com`, Secure, HttpOnly, SameSite=Lax. Public auth/invite entry lives on `ophalo-web`; authenticated work redirects to `ophalo-app`; numeric fallback code and native auth UX are deferred | Locked | [ADR-378](ADR-378-pilot-auth-session-and-domain-topology.md) |
| ADR-379 | Authenticated OpHalo web apps self-host core brand assets by default. S13 `ophalo-app` loads Source Serif 4 and Inter from app-origin `public/fonts/` with `@font-face`, not Google Fonts or another third-party font CDN. Keep is the first consumer of the reusable OpHalo web foundation, not a one-off pilot UI | Locked | [ADR-379](ADR-379-web-foundation-and-self-hosted-assets.md) |
| ADR-380 | S13d request detail workbench binds directly to `KeepRequestDetailResult`: timeline from `events`, permissions from `availableActions`, validation from `validation`, contact affordances from `contactActions`, tracker share from `NeedsShare/PageToken`, share permission from net-new `availableActions.canRecordShareIntent`, and optimistic concurrency from GUID `version` sent as `X-Keep-Request-Version`. S13d includes status, external contact, attention acknowledgement, participation, mute/watch, and share-intent clearing; Follow Up On, Planned For, customer updates, closeout, feedback review, classification, reporting, and settings remain deferred | Locked | [ADR-380](ADR-380-request-detail-workbench-contract.md) |
| ADR-381 | Tracker link footprint is intentionally narrow: post-capture desktop Copy Tracker Link, request detail copy/native/manual share controls, and mobile detail NeedsShare sticky banner. List rows may show NeedsShare status but must not include tracker URL or PageToken. Share methods are exactly `copy_link`, `native_share`, and `manual_mark_shared`; share intent records initiated sharing only, stays separate from business updates, and is rendered from `availableActions.canRecordShareIntent` | Locked | [ADR-381](ADR-381-tracker-link-sharing-footprint.md) |
| ADR-382 | S13f customer update composer is detail-only and uses `POST /keep/requests/{id}/business-updates` with `message`, optional filtered `setStatus`, and `X-Keep-Request-Version`. Render from `availableActions.canSendBusinessUpdate`; optional status choices come from `availableActions.allowedStatuses` filtered to active workflow statuses only (`scheduled`, `in_progress`, `pending_customer`, `resolved` when present). No templates/quick replies, no NeedsShare blocking, no terminal close/cancel/classify through the composer, and drafts are preserved on conflicts/errors | Locked | [ADR-382](ADR-382-customer-update-composer.md) |
| ADR-383 | S13g uses one Owner/Admin-only Settings surface with Company, Team, and Onboarding sections. Company includes profile/contact, timezone, response policy, and intake link controls; Team exposes server-permitted invite/list/resend/role/suspend/reactivate/remove flows using product language, server-provided `seatUsage`, disabled invite-at-limit behavior, and manual-share resend as a deliberate email-deliverability fallback. Team is secondary/low-pressure for solo businesses; Onboarding reads the checklist and exposes existing manual marks. No frontend-only rollout gates beyond server policy; invite accept stays outside `ophalo-app`; primary-owner transfer, billing, and internal support tooling remain deferred | Locked | [ADR-383](ADR-383-settings-and-team-management-surface.md) |
| ADR-384 | S13h extends existing list/detail surfaces for closeout and feedback review: Owner/Admin list views use `ready_to_close` and `feedback_review`; detail close action renders only when `canClose` and `allowedStatuses` includes `closed`; feedback review renders from `canMarkFeedbackReviewed` with optional internal note, age badges `New/Aging/Overdue`, and copy that reviewing negative feedback does not reopen the request. No new top-level route, no new backend endpoint, and dedicated next-review navigation remains deferred | Locked | [ADR-384](ADR-384-closeout-feedback-review-and-attention-explanation.md) |

_Next free ID: **ADR-385**._
